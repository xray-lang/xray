class BenchCounter {
    value: int = 0

    bump(x: int) -> int {
        this.value = this.value + x
        return this.value
    }
}

fn run(rounds: int) -> int {
    var counter = BenchCounter()
    var total = 0
    for (var i = 0; i < rounds; i++) {
        total = total + counter.bump((i % 5) + 1)
    }
    return total + counter.value
}

fn main() {
    var result = run(180000)
    print("oop_dispatch_mono=${result}")
}

main()

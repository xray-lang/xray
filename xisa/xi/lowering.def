; Xi lowering target coverage source.
; Entries describe which backend targets implement a semantic op slice.

(lower xi.const
  :match ()
  :required-targets (vm-bytecode aot-c)
  :vm-bytecode (driver emit_const)
  :aot-c (driver xicgen_const))

(lower xi.param
  :match ()
  :required-targets (vm-bytecode aot-c)
  :vm-bytecode (driver emit_param)
  :aot-c (driver xicgen_param))

(lower xi.copy
  :match ()
  :required-targets (vm-bytecode aot-c)
  :vm-bytecode (driver emit_copy)
  :aot-c (driver xicgen_copy))

(lower xi.move
  :match ()
  :required-targets (vm-bytecode aot-c)
  :vm-bytecode (driver xi_emit_move)
  :aot-c (driver xicgen_move))

(lower xi.add
  :match (value-binary)
  :required-targets (vm-bytecode aot-c)
  :vm-bytecode (driver xi_emit_arith)
  :aot-c (driver xicgen_add))

(lower xi.sub
  :match (value-binary)
  :required-targets (vm-bytecode aot-c)
  :vm-bytecode (driver xi_emit_arith)
  :aot-c (driver xicgen_sub))

(lower xi.mul
  :match (value-binary)
  :required-targets (vm-bytecode aot-c)
  :vm-bytecode (driver xi_emit_arith)
  :aot-c (driver xicgen_mul))

(lower xi.div
  :match (value-binary)
  :required-targets (vm-bytecode aot-c)
  :vm-bytecode (driver xi_emit_arith)
  :aot-c (driver xicgen_div))

(lower xi.mod
  :match (value-binary)
  :required-targets (vm-bytecode aot-c)
  :vm-bytecode (driver xi_emit_arith)
  :aot-c (driver xicgen_mod))

(lower xi.neg
  :match (value-unary)
  :required-targets (vm-bytecode aot-c)
  :vm-bytecode (driver xi_emit_neg)
  :aot-c (driver xicgen_neg))

(lower xi.band
  :match (value-binary)
  :required-targets (vm-bytecode aot-c)
  :vm-bytecode (driver xi_emit_arith)
  :aot-c (driver xicgen_band))

(lower xi.bor
  :match (value-binary)
  :required-targets (vm-bytecode aot-c)
  :vm-bytecode (driver xi_emit_arith)
  :aot-c (driver xicgen_bor))

(lower xi.bxor
  :match (value-binary)
  :required-targets (vm-bytecode aot-c)
  :vm-bytecode (driver xi_emit_arith)
  :aot-c (driver xicgen_bxor))

(lower xi.bnot
  :match (value-unary)
  :required-targets (vm-bytecode aot-c)
  :vm-bytecode (driver xi_emit_bnot)
  :aot-c (driver xicgen_bnot))

(lower xi.not
  :match (value-unary)
  :required-targets (vm-bytecode aot-c)
  :vm-bytecode (driver xi_emit_not)
  :aot-c (driver xicgen_not))

(lower xi.select
  :match ()
  :required-targets (vm-bytecode aot-c)
  :vm-bytecode (driver emit_select)
  :aot-c (driver xicgen_select))

(lower xi.get.shared
  :match ()
  :required-targets (vm-bytecode aot-c)
  :vm-bytecode (driver xi_emit_get_shared)
  :aot-c (driver xicgen_get_shared))

(lower xi.set.shared
  :match ()
  :required-targets (vm-bytecode aot-c)
  :vm-bytecode (driver xi_emit_set_shared)
  :aot-c (driver xicgen_set_shared))

(lower xi.import.ref
  :match ()
  :required-targets (vm-bytecode aot-c)
  :vm-bytecode (driver xi_emit_import_ref)
  :aot-c (driver xicgen_import_ref))

(lower xi.closure.new
  :match ()
  :required-targets (vm-bytecode aot-c)
  :vm-bytecode (driver xi_emit_closure_new :raw-cell-args yes :handles-cell-dst yes)
  :aot-c (driver xicgen_closure_new))

(lower xi.load.upval
  :match ()
  :required-targets (vm-bytecode aot-c)
  :vm-bytecode (driver xi_emit_load_upval)
  :aot-c (driver xicgen_load_upval))

(lower xi.store.upval
  :match ()
  :required-targets (vm-bytecode aot-c)
  :vm-bytecode (driver xi_emit_store_upval)
  :aot-c (driver xicgen_store_upval))

(lower xi.assert
  :match ()
  :required-targets (vm-bytecode aot-c)
  :vm-bytecode (driver xi_emit_assert)
  :aot-c (driver xicgen_assert))

(lower xi.assert.eq
  :match ()
  :required-targets (vm-bytecode aot-c)
  :vm-bytecode (driver xi_emit_assert_eq)
  :aot-c (driver xicgen_assert_eq))

(lower xi.assert.ne
  :match ()
  :required-targets (vm-bytecode aot-c)
  :vm-bytecode (driver xi_emit_assert_ne)
  :aot-c (driver xicgen_assert_ne))

(lower xi.assert.throws
  :match ()
  :required-targets (vm-bytecode)
  :vm-bytecode (driver xi_emit_assert_throws))

(lower xi.typeof
  :match ()
  :required-targets (vm-bytecode aot-c)
  :vm-bytecode (driver xi_emit_typeof)
  :aot-c (driver xicgen_typeof))

(lower xi.get.builtin
  :match ()
  :required-targets (vm-bytecode aot-c)
  :vm-bytecode (driver xi_emit_get_builtin)
  :aot-c (driver xicgen_get_builtin))

(lower xi.get.global
  :match ()
  :required-targets (vm-bytecode)
  :vm-bytecode (driver xi_emit_get_global))

(lower xi.set.global
  :match ()
  :required-targets (vm-bytecode)
  :vm-bytecode (driver xi_emit_set_global))

(lower xi.print
  :match ()
  :required-targets (vm-bytecode aot-c)
  :vm-bytecode (driver xi_emit_print)
  :aot-c (driver xicgen_print))

(lower xi.class.create
  :match ()
  :required-targets (vm-bytecode aot-c)
  :vm-bytecode (driver xi_emit_class_create)
  :aot-c (driver xicgen_class_create))

(lower xi.call
  :match ()
  :required-targets (vm-bytecode aot-c)
  :vm-bytecode (driver xi_emit_call :fresh-dst yes)
  :aot-c (driver xicgen_call))

(lower xi.call.method
  :match ()
  :required-targets (vm-bytecode aot-c)
  :vm-bytecode (driver xi_emit_call_method :fresh-dst yes)
  :aot-c (driver xicgen_call_method))

(lower xi.call.method.direct
  :match ()
  :required-targets (vm-bytecode aot-c)
  :vm-bytecode (driver xi_emit_call_method_direct :fresh-dst yes)
  :aot-c (driver xicgen_call_method))

(lower xi.tail.call
  :match ()
  :required-targets (vm-bytecode)
  :vm-bytecode (driver xi_emit_tail_call))

(lower xi.call.builtin
  :match ()
  :required-targets (vm-bytecode aot-c)
  :vm-bytecode (driver xi_emit_call_builtin)
  :aot-c (driver xicgen_call_builtin))

(lower xi.throw
  :match ()
  :required-targets (vm-bytecode aot-c)
  :vm-bytecode (driver xi_emit_throw)
  :aot-c (driver xicgen_throw))

(lower xi.try
  :match ()
  :required-targets (vm-bytecode aot-c-stmt)
  :vm-bytecode (driver xi_emit_try)
  :aot-c-stmt (driver xicgen_stmt_try))

(lower xi.catch
  :match ()
  :required-targets (vm-bytecode aot-c-stmt)
  :vm-bytecode (driver xi_emit_catch)
  :aot-c-stmt (driver xicgen_stmt_catch))

(lower xi.end.try
  :match ()
  :required-targets (vm-bytecode aot-c-stmt)
  :vm-bytecode (driver xi_emit_end_try)
  :aot-c-stmt (driver xicgen_stmt_end_try))

(lower xi.err.set
  :match ()
  :required-targets (vm-bytecode aot-c-stmt)
  :vm-bytecode (driver xi_emit_err_set)
  :aot-c-stmt (driver xicgen_stmt_err_set))

(lower xi.err.return
  :match ()
  :required-targets (vm-bytecode aot-c-stmt)
  :vm-bytecode (driver xi_emit_err_return)
  :aot-c-stmt (driver xicgen_stmt_err_return))

(lower xi.err.check
  :match ()
  :required-targets (vm-bytecode aot-c-stmt)
  :vm-bytecode (driver xi_emit_err_check)
  :aot-c-stmt (driver xicgen_stmt_err_check))

(lower xi.err.catch
  :match ()
  :required-targets (vm-bytecode aot-c-stmt)
  :vm-bytecode (driver xi_emit_err_catch)
  :aot-c-stmt (driver xicgen_stmt_err_catch))

(lower xi.retain
  :match ()
  :required-targets (vm-bytecode aot-c)
  :vm-bytecode (driver xi_emit_retain)
  :aot-c (driver xicgen_retain))

(lower xi.release
  :match ()
  :required-targets (vm-bytecode aot-c)
  :vm-bytecode (driver xi_emit_release :raw-cell-args yes)
  :aot-c (driver xicgen_release))

(lower xi.stack.alloc
  :match ()
  :required-targets (aot-c)
  :aot-c (driver xicgen_stack_alloc))

(lower xi.shl
  :match (value-binary)
  :required-targets (vm-bytecode aot-c)
  :vm-bytecode (driver xi_emit_arith)
  :aot-c (driver xicgen_shl))

(lower xi.shr
  :match (value-binary)
  :required-targets (vm-bytecode aot-c)
  :vm-bytecode (driver xi_emit_arith)
  :aot-c (driver xicgen_shr))

(lower xi.eq
  :match (compare)
  :required-targets (vm-bytecode aot-c)
  :vm-bytecode (driver xi_emit_cmp)
  :aot-c (driver xicgen_eq))

(lower xi.ne
  :match (compare)
  :required-targets (vm-bytecode aot-c)
  :vm-bytecode (driver xi_emit_cmp)
  :aot-c (driver xicgen_ne))

(lower xi.eq.strict
  :match (compare)
  :required-targets (vm-bytecode aot-c)
  :vm-bytecode (driver xi_emit_cmp)
  :aot-c (driver xicgen_eq_strict))

(lower xi.ne.strict
  :match (compare)
  :required-targets (vm-bytecode aot-c)
  :vm-bytecode (driver xi_emit_cmp)
  :aot-c (driver xicgen_ne_strict))

(lower xi.lt
  :match (compare)
  :required-targets (vm-bytecode aot-c)
  :vm-bytecode (driver xi_emit_cmp)
  :aot-c (driver xicgen_lt))

(lower xi.le
  :match (compare)
  :required-targets (vm-bytecode aot-c)
  :vm-bytecode (driver xi_emit_cmp)
  :aot-c (driver xicgen_le))

(lower xi.gt
  :match (compare)
  :required-targets (vm-bytecode aot-c)
  :vm-bytecode (driver xi_emit_cmp)
  :aot-c (driver xicgen_gt))

(lower xi.ge
  :match (compare)
  :required-targets (vm-bytecode aot-c)
  :vm-bytecode (driver xi_emit_cmp)
  :aot-c (driver xicgen_ge))

(lower xi.narrow.i8
  :match (narrow)
  :required-targets (vm-bytecode aot-c)
  :vm-bytecode (driver xi_emit_narrow)
  :aot-c (driver xicgen_narrow_i8))

(lower xi.narrow.u8
  :match (narrow)
  :required-targets (vm-bytecode aot-c)
  :vm-bytecode (driver xi_emit_narrow)
  :aot-c (driver xicgen_narrow_u8))

(lower xi.narrow.i16
  :match (narrow)
  :required-targets (vm-bytecode aot-c)
  :vm-bytecode (driver xi_emit_narrow)
  :aot-c (driver xicgen_narrow_i16))

(lower xi.narrow.u16
  :match (narrow)
  :required-targets (vm-bytecode aot-c)
  :vm-bytecode (driver xi_emit_narrow)
  :aot-c (driver xicgen_narrow_u16))

(lower xi.narrow.i32
  :match (narrow)
  :required-targets (vm-bytecode aot-c)
  :vm-bytecode (driver xi_emit_narrow)
  :aot-c (driver xicgen_narrow_i32))

(lower xi.narrow.u32
  :match (narrow)
  :required-targets (vm-bytecode aot-c)
  :vm-bytecode (driver xi_emit_narrow)
  :aot-c (driver xicgen_narrow_u32))

(lower xi.narrow.f32
  :match (narrow)
  :required-targets (vm-bytecode aot-c)
  :vm-bytecode (driver xi_emit_narrow)
  :aot-c (driver xicgen_narrow_f32))

(lower xi.widen.i8
  :match (widen)
  :required-targets (vm-bytecode aot-c)
  :vm-bytecode (driver xi_emit_widen)
  :aot-c (driver xicgen_widen_i8))

(lower xi.widen.u8
  :match (widen)
  :required-targets (vm-bytecode aot-c)
  :vm-bytecode (driver xi_emit_widen)
  :aot-c (driver xicgen_widen_u8))

(lower xi.widen.i16
  :match (widen)
  :required-targets (vm-bytecode aot-c)
  :vm-bytecode (driver xi_emit_widen)
  :aot-c (driver xicgen_widen_i16))

(lower xi.widen.u16
  :match (widen)
  :required-targets (vm-bytecode aot-c)
  :vm-bytecode (driver xi_emit_widen)
  :aot-c (driver xicgen_widen_u16))

(lower xi.widen.i32
  :match (widen)
  :required-targets (vm-bytecode aot-c)
  :vm-bytecode (driver xi_emit_widen)
  :aot-c (driver xicgen_widen_i32))

(lower xi.widen.u32
  :match (widen)
  :required-targets (vm-bytecode aot-c)
  :vm-bytecode (driver xi_emit_widen)
  :aot-c (driver xicgen_widen_u32))

(lower xi.widen.f32
  :match (widen)
  :required-targets (vm-bytecode aot-c)
  :vm-bytecode (driver xi_emit_widen)
  :aot-c (driver xicgen_widen_f32))

(lower xi.isnull
  :match ()
  :required-targets (vm-bytecode aot-c)
  :vm-bytecode (driver xi_emit_isnull)
  :aot-c (driver xicgen_isnull))

(lower xi.is
  :match ()
  :required-targets (vm-bytecode aot-c)
  :vm-bytecode (driver xi_emit_is)
  :aot-c (driver xicgen_is))

(lower xi.convert
  :match ()
  :required-targets (vm-bytecode aot-c)
  :vm-bytecode (driver xi_emit_convert)
  :aot-c (driver xicgen_convert))

(lower xi.box
  :match ()
  :required-targets (vm-bytecode aot-c)
  :vm-bytecode (driver xi_emit_box)
  :aot-c (driver xicgen_box))

(lower xi.unbox
  :match ()
  :required-targets (vm-bytecode aot-c)
  :vm-bytecode (driver xi_emit_unbox)
  :aot-c (driver xicgen_unbox))

(lower xi.load.field
  :match ()
  :required-targets (vm-bytecode aot-c)
  :vm-bytecode (driver xi_emit_load_field)
  :aot-c (driver xicgen_load_field))

(lower xi.store.field
  :match ()
  :required-targets (vm-bytecode aot-c)
  :vm-bytecode (driver xi_emit_store_field)
  :aot-c (driver xicgen_store_field))

(lower xi.index.get
  :match ()
  :required-targets (vm-bytecode aot-c)
  :vm-bytecode (driver xi_emit_index_get)
  :aot-c (driver xicgen_index_get))

(lower xi.index.set
  :match ()
  :required-targets (vm-bytecode aot-c)
  :vm-bytecode (driver xi_emit_index_set)
  :aot-c (driver xicgen_index_set))

(lower xi.array.new
  :match ()
  :required-targets (vm-bytecode aot-c)
  :vm-bytecode (driver xi_emit_array_new)
  :aot-c (driver xicgen_array_new))

(lower xi.map.new
  :match ()
  :required-targets (vm-bytecode aot-c)
  :vm-bytecode (driver xi_emit_map_new)
  :aot-c (driver xicgen_map_new))

(lower xi.set.new
  :match ()
  :required-targets (vm-bytecode aot-c)
  :vm-bytecode (driver xi_emit_set_new)
  :aot-c (driver xicgen_set_new))

(lower xi.str.concat
  :match ()
  :required-targets (vm-bytecode aot-c)
  :vm-bytecode (driver xi_emit_str_concat)
  :aot-c (driver xicgen_str_concat))

(lower xi.as
  :match ()
  :required-targets (vm-bytecode aot-c)
  :vm-bytecode (driver xi_emit_as)
  :aot-c (driver xicgen_as))

(lower xi.slice
  :match ()
  :required-targets (vm-bytecode aot-c)
  :vm-bytecode (driver xi_emit_slice)
  :aot-c (driver xicgen_slice))

(lower xi.range
  :match ()
  :required-targets (vm-bytecode aot-c)
  :vm-bytecode (driver xi_emit_range)
  :aot-c (driver xicgen_range))

(lower xi.iter.new
  :match ()
  :required-targets (vm-bytecode)
  :vm-bytecode (driver xi_emit_iter))

(lower xi.iter.next
  :match ()
  :required-targets (vm-bytecode)
  :vm-bytecode (driver xi_emit_iter))

(lower xi.iter.valid
  :match ()
  :required-targets (vm-bytecode)
  :vm-bytecode (driver xi_emit_iter))

(lower xi.go
  :match ()
  :required-targets (vm-bytecode)
  :vm-bytecode (driver xi_emit_go :fresh-dst yes))

(lower xi.await
  :match ()
  :required-targets (vm-bytecode)
  :vm-bytecode (driver xi_emit_await))

(lower xi.chan.send
  :match ()
  :required-targets (vm-bytecode)
  :vm-bytecode (driver xi_emit_chan_send))

(lower xi.chan.recv
  :match ()
  :required-targets (vm-bytecode)
  :vm-bytecode (driver xi_emit_chan_recv :fresh-dst yes))

(lower xi.chan.recv.status
  :match ()
  :required-targets (vm-bytecode aot-c)
  :vm-bytecode (driver xi_emit_chan_recv_status)
  :aot-c (driver xicgen_chan_recv_status))

(lower xi.chan.try.send
  :match ()
  :required-targets (vm-bytecode)
  :vm-bytecode (driver xi_emit_chan_try_send))

(lower xi.chan.try.recv
  :match ()
  :required-targets (vm-bytecode)
  :vm-bytecode (driver xi_emit_chan_try_recv :fresh-dst yes))

(lower xi.chan.is.closed
  :match ()
  :required-targets (vm-bytecode)
  :vm-bytecode (driver xi_emit_chan_is_closed))

(lower xi.time.after
  :match ()
  :required-targets (vm-bytecode)
  :vm-bytecode (driver xi_emit_time_after))

(lower xi.chan.timer.dispose
  :match ()
  :required-targets (vm-bytecode)
  :vm-bytecode (driver xi_emit_chan_timer_dispose))

(lower xi.select.block
  :match ()
  :required-targets (vm-bytecode)
  :vm-bytecode (driver xi_emit_select_block :fresh-dst yes))

(lower xi.yield
  :match ()
  :required-targets (vm-bytecode)
  :vm-bytecode (driver xi_emit_yield))

(lower xi.coro.op
  :match ()
  :required-targets (vm-bytecode)
  :vm-bytecode (driver xi_emit_coro_op))

(lower xi.chan.new
  :match ()
  :required-targets (vm-bytecode)
  :vm-bytecode (driver xi_emit_chan_new))

(lower xi.defer
  :match ()
  :required-targets (vm-bytecode aot-c-stmt)
  :vm-bytecode (driver xi_emit_defer)
  :aot-c-stmt (driver xicgen_stmt_defer))

(lower xi.json.new
  :match ()
  :required-targets (vm-bytecode aot-c)
  :vm-bytecode (driver xi_emit_json_new)
  :aot-c (driver xicgen_json_new))

(lower xi.json.init.f
  :match ()
  :required-targets (vm-bytecode aot-c)
  :vm-bytecode (driver xi_emit_json_init_f)
  :aot-c (driver xicgen_json_init_f))

(lower xi.json.get.f
  :match ()
  :required-targets (vm-bytecode aot-c)
  :vm-bytecode (driver xi_emit_json_get_f)
  :aot-c (driver xicgen_json_get_f))

(lower xi.json.set.f
  :match ()
  :required-targets (vm-bytecode aot-c)
  :vm-bytecode (driver xi_emit_json_set_f)
  :aot-c (driver xicgen_json_set_f))

(lower xi.json.decode
  :match ()
  :required-targets (vm-bytecode aot-c)
  :vm-bytecode (driver xi_emit_json_decode)
  :aot-c (reject xicgen_reject_unsupported))

(lower xi.struct.new
  :match ()
  :required-targets (vm-bytecode aot-c)
  :vm-bytecode (driver xi_emit_struct_new)
  :aot-c (driver xicgen_struct_new))

(lower xi.struct.get
  :match ()
  :required-targets (vm-bytecode aot-c)
  :vm-bytecode (driver xi_emit_struct_get)
  :aot-c (driver xicgen_struct_get))

(lower xi.struct.set
  :match ()
  :required-targets (vm-bytecode aot-c)
  :vm-bytecode (driver xi_emit_struct_set)
  :aot-c (driver xicgen_struct_set))

(lower xi.scope.enter
  :match ()
  :required-targets (vm-bytecode)
  :vm-bytecode (driver xi_emit_scope_enter))

(lower xi.scope.exit
  :match ()
  :required-targets (vm-bytecode)
  :vm-bytecode (driver xi_emit_scope_exit))

(lower xi.regex.compile
  :match ()
  :required-targets (vm-bytecode)
  :vm-bytecode (driver xi_emit_regex_compile))

(lower xi.tuple.new
  :match ()
  :required-targets (vm-bytecode aot-c)
  :vm-bytecode (driver xi_emit_tuple_new)
  :aot-c (driver xicgen_tuple_new))

(lower xi.tuple.get
  :match ()
  :required-targets (vm-bytecode aot-c)
  :vm-bytecode (driver xi_emit_tuple_get)
  :aot-c (driver xicgen_tuple_get))

(lower xi.bytes.load.u32.le
  :match ()
  :required-targets (vm-bytecode aot-c)
  :vm-bytecode (driver xi_emit_bytes_load_u32_le)
  :aot-c (driver xicgen_bytes_load_u32_le))

(lower xi.bytes.load.u64.le
  :match ()
  :required-targets (vm-bytecode aot-c)
  :vm-bytecode (driver xi_emit_bytes_load_u64_le)
  :aot-c (driver xicgen_bytes_load_u64_le))

(lower xi.bytes.copy.within
  :match ()
  :required-targets (vm-bytecode aot-c)
  :vm-bytecode (driver xi_emit_bytes_copy_within)
  :aot-c (driver xicgen_bytes_copy_within))

(lower xi.bytes.copy.from
  :match ()
  :required-targets (vm-bytecode aot-c)
  :vm-bytecode (driver xi_emit_bytes_copy_from)
  :aot-c (driver xicgen_bytes_copy_from))

(lower xi.bytes.repeat.from
  :match ()
  :required-targets (vm-bytecode aot-c)
  :vm-bytecode (driver xi_emit_bytes_repeat_from)
  :aot-c (driver xicgen_bytes_repeat_from))

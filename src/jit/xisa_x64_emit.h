/* AUTO-GENERATED by xisagen — DO NOT EDIT */
/* Source: xisa/arch/x64.isa */

#ifndef XISA_X64_EMIT_H
#define XISA_X64_EMIT_H

#include <stdint.h>
#include <string.h>

/* Register class: x64.gpr64 (16 regs, 4-bit encoding) */
enum {
    XISA_X64_REG_RAX = 0,
    XISA_X64_REG_RCX = 1,
    XISA_X64_REG_RDX = 2,
    XISA_X64_REG_RBX = 3,
    XISA_X64_REG_RSP = 4,
    XISA_X64_REG_RBP = 5,
    XISA_X64_REG_RSI = 6,
    XISA_X64_REG_RDI = 7,
    XISA_X64_REG_R8 = 8,
    XISA_X64_REG_R9 = 9,
    XISA_X64_REG_R10 = 10,
    XISA_X64_REG_R11 = 11,
    XISA_X64_REG_R12 = 12,
    XISA_X64_REG_R13 = 13,
    XISA_X64_REG_R14 = 14,
    XISA_X64_REG_R15 = 15,
};

/* Register class: x64.gpr32 (16 regs, 4-bit encoding) */
enum {
    XISA_X64_REG_EAX = 0,
    XISA_X64_REG_ECX = 1,
    XISA_X64_REG_EDX = 2,
    XISA_X64_REG_EBX = 3,
    XISA_X64_REG_ESP = 4,
    XISA_X64_REG_EBP = 5,
    XISA_X64_REG_ESI = 6,
    XISA_X64_REG_EDI = 7,
    XISA_X64_REG_R8D = 8,
    XISA_X64_REG_R9D = 9,
    XISA_X64_REG_R10D = 10,
    XISA_X64_REG_R11D = 11,
    XISA_X64_REG_R12D = 12,
    XISA_X64_REG_R13D = 13,
    XISA_X64_REG_R14D = 14,
    XISA_X64_REG_R15D = 15,
};

/* x64.add.rr: 3-3 bytes */
static inline int xisa_emit_x64_add_rr(uint8_t *buf, int cap, uint8_t dst, uint8_t src) {
    if (cap < 3)
        return 0;
    uint8_t *p = buf;
    *p++ = (uint8_t) (0x48 | ((src >> 3) << 2) | ((dst >> 3) << 0));
    *p++ = 0x01;
    *p++ = (uint8_t) (0x03 << 6 | ((src & 7)) << 3 | ((dst & 7)));
    return (int) (p - buf);
}

/* x64.sub.rr: 3-3 bytes */
static inline int xisa_emit_x64_sub_rr(uint8_t *buf, int cap, uint8_t dst, uint8_t src) {
    if (cap < 3)
        return 0;
    uint8_t *p = buf;
    *p++ = (uint8_t) (0x48 | ((src >> 3) << 2) | ((dst >> 3) << 0));
    *p++ = 0x29;
    *p++ = (uint8_t) (0x03 << 6 | ((src & 7)) << 3 | ((dst & 7)));
    return (int) (p - buf);
}

/* x64.and.rr: 3-3 bytes */
static inline int xisa_emit_x64_and_rr(uint8_t *buf, int cap, uint8_t dst, uint8_t src) {
    if (cap < 3)
        return 0;
    uint8_t *p = buf;
    *p++ = (uint8_t) (0x48 | ((src >> 3) << 2) | ((dst >> 3) << 0));
    *p++ = 0x21;
    *p++ = (uint8_t) (0x03 << 6 | ((src & 7)) << 3 | ((dst & 7)));
    return (int) (p - buf);
}

/* x64.or.rr: 3-3 bytes */
static inline int xisa_emit_x64_or_rr(uint8_t *buf, int cap, uint8_t dst, uint8_t src) {
    if (cap < 3)
        return 0;
    uint8_t *p = buf;
    *p++ = (uint8_t) (0x48 | ((src >> 3) << 2) | ((dst >> 3) << 0));
    *p++ = 0x09;
    *p++ = (uint8_t) (0x03 << 6 | ((src & 7)) << 3 | ((dst & 7)));
    return (int) (p - buf);
}

/* x64.xor.rr: 3-3 bytes */
static inline int xisa_emit_x64_xor_rr(uint8_t *buf, int cap, uint8_t dst, uint8_t src) {
    if (cap < 3)
        return 0;
    uint8_t *p = buf;
    *p++ = (uint8_t) (0x48 | ((src >> 3) << 2) | ((dst >> 3) << 0));
    *p++ = 0x31;
    *p++ = (uint8_t) (0x03 << 6 | ((src & 7)) << 3 | ((dst & 7)));
    return (int) (p - buf);
}

/* x64.cmp.rr: 3-3 bytes */
static inline int xisa_emit_x64_cmp_rr(uint8_t *buf, int cap, uint8_t lhs, uint8_t rhs) {
    if (cap < 3)
        return 0;
    uint8_t *p = buf;
    *p++ = (uint8_t) (0x48 | ((rhs >> 3) << 2) | ((lhs >> 3) << 0));
    *p++ = 0x39;
    *p++ = (uint8_t) (0x03 << 6 | ((rhs & 7)) << 3 | ((lhs & 7)));
    return (int) (p - buf);
}

/* x64.test.rr: 3-3 bytes */
static inline int xisa_emit_x64_test_rr(uint8_t *buf, int cap, uint8_t lhs, uint8_t rhs) {
    if (cap < 3)
        return 0;
    uint8_t *p = buf;
    *p++ = (uint8_t) (0x48 | ((rhs >> 3) << 2) | ((lhs >> 3) << 0));
    *p++ = 0x85;
    *p++ = (uint8_t) (0x03 << 6 | ((rhs & 7)) << 3 | ((lhs & 7)));
    return (int) (p - buf);
}

/* x64.imul.rr: 4-4 bytes */
static inline int xisa_emit_x64_imul_rr(uint8_t *buf, int cap, uint8_t dst, uint8_t src) {
    if (cap < 4)
        return 0;
    uint8_t *p = buf;
    *p++ = (uint8_t) (0x48 | ((dst >> 3) << 2) | ((src >> 3) << 0));
    *p++ = 0x0f;
    *p++ = 0xaf;
    *p++ = (uint8_t) (0x03 << 6 | ((dst & 7)) << 3 | ((src & 7)));
    return (int) (p - buf);
}

/* x64.add.ri32: 7-7 bytes */
static inline int xisa_emit_x64_add_ri32(uint8_t *buf, int cap, uint8_t dst, int32_t imm) {
    if (cap < 7)
        return 0;
    uint8_t *p = buf;
    *p++ = (uint8_t) (0x48 | ((dst >> 3) << 0));
    *p++ = 0x81;
    *p++ = (uint8_t) (0x03 << 6 | (0) << 3 | ((dst & 7)));
    {
        int32_t v = imm;
        memcpy(p, &v, 4);
        p += 4;
    }
    return (int) (p - buf);
}

/* x64.add.ri8: 4-4 bytes */
static inline int xisa_emit_x64_add_ri8(uint8_t *buf, int cap, uint8_t dst, int32_t imm) {
    if (cap < 4)
        return 0;
    uint8_t *p = buf;
    *p++ = (uint8_t) (0x48 | ((dst >> 3) << 0));
    *p++ = 0x83;
    *p++ = (uint8_t) (0x03 << 6 | (0) << 3 | ((dst & 7)));
    *p++ = (uint8_t) imm;
    return (int) (p - buf);
}

/* x64.sub.ri8: 4-4 bytes */
static inline int xisa_emit_x64_sub_ri8(uint8_t *buf, int cap, uint8_t dst, int32_t imm) {
    if (cap < 4)
        return 0;
    uint8_t *p = buf;
    *p++ = (uint8_t) (0x48 | ((dst >> 3) << 0));
    *p++ = 0x83;
    *p++ = (uint8_t) (0x03 << 6 | (5) << 3 | ((dst & 7)));
    *p++ = (uint8_t) imm;
    return (int) (p - buf);
}

/* x64.cmp.ri8: 4-4 bytes */
static inline int xisa_emit_x64_cmp_ri8(uint8_t *buf, int cap, uint8_t lhs, int32_t imm) {
    if (cap < 4)
        return 0;
    uint8_t *p = buf;
    *p++ = (uint8_t) (0x48 | ((lhs >> 3) << 0));
    *p++ = 0x83;
    *p++ = (uint8_t) (0x03 << 6 | (7) << 3 | ((lhs & 7)));
    *p++ = (uint8_t) imm;
    return (int) (p - buf);
}

/* x64.mov.rr: 3-3 bytes */
static inline int xisa_emit_x64_mov_rr(uint8_t *buf, int cap, uint8_t dst, uint8_t src) {
    if (cap < 3)
        return 0;
    uint8_t *p = buf;
    *p++ = (uint8_t) (0x48 | ((src >> 3) << 2) | ((dst >> 3) << 0));
    *p++ = 0x89;
    *p++ = (uint8_t) (0x03 << 6 | ((src & 7)) << 3 | ((dst & 7)));
    return (int) (p - buf);
}

/* x64.mov.ri64: 10-10 bytes */
static inline int xisa_emit_x64_mov_ri64(uint8_t *buf, int cap, uint8_t dst, int32_t imm) {
    if (cap < 10)
        return 0;
    uint8_t *p = buf;
    *p++ = (uint8_t) (0x48 | ((dst >> 3) << 0));
    *p++ = (uint8_t) (0xb8 + (dst & 7));
    {
        int64_t v = imm;
        memcpy(p, &v, 8);
        p += 8;
    }
    return (int) (p - buf);
}

/* x64.shl.rcl: 3-3 bytes */
static inline int xisa_emit_x64_shl_rcl(uint8_t *buf, int cap, uint8_t dst) {
    if (cap < 3)
        return 0;
    uint8_t *p = buf;
    *p++ = (uint8_t) (0x48 | ((dst >> 3) << 0));
    *p++ = 0xd3;
    *p++ = (uint8_t) (0x03 << 6 | (4) << 3 | ((dst & 7)));
    return (int) (p - buf);
}

/* x64.shr.rcl: 3-3 bytes */
static inline int xisa_emit_x64_shr_rcl(uint8_t *buf, int cap, uint8_t dst) {
    if (cap < 3)
        return 0;
    uint8_t *p = buf;
    *p++ = (uint8_t) (0x48 | ((dst >> 3) << 0));
    *p++ = 0xd3;
    *p++ = (uint8_t) (0x03 << 6 | (5) << 3 | ((dst & 7)));
    return (int) (p - buf);
}

/* x64.sar.rcl: 3-3 bytes */
static inline int xisa_emit_x64_sar_rcl(uint8_t *buf, int cap, uint8_t dst) {
    if (cap < 3)
        return 0;
    uint8_t *p = buf;
    *p++ = (uint8_t) (0x48 | ((dst >> 3) << 0));
    *p++ = 0xd3;
    *p++ = (uint8_t) (0x03 << 6 | (7) << 3 | ((dst & 7)));
    return (int) (p - buf);
}

/* x64.load.bd32: 7-7 bytes */
static inline int xisa_emit_x64_load_bd32(uint8_t *buf, int cap, uint8_t dst, uint8_t base,
                                          int32_t disp) {
    if (cap < 7)
        return 0;
    uint8_t *p = buf;
    *p++ = (uint8_t) (0x48 | ((dst >> 3) << 2) | ((base >> 3) << 0));
    *p++ = 0x8b;
    *p++ = (uint8_t) (0x02 << 6 | ((dst & 7)) << 3 | ((base & 7)));
    {
        int32_t v = disp;
        memcpy(p, &v, 4);
        p += 4;
    }
    return (int) (p - buf);
}

/* x64.store.bd32: 7-7 bytes */
static inline int xisa_emit_x64_store_bd32(uint8_t *buf, int cap, uint8_t base, int32_t disp,
                                           uint8_t src) {
    if (cap < 7)
        return 0;
    uint8_t *p = buf;
    *p++ = (uint8_t) (0x48 | ((src >> 3) << 2) | ((base >> 3) << 0));
    *p++ = 0x89;
    *p++ = (uint8_t) (0x02 << 6 | ((src & 7)) << 3 | ((base & 7)));
    {
        int32_t v = disp;
        memcpy(p, &v, 4);
        p += 4;
    }
    return (int) (p - buf);
}

/* x64.call.rel32: 5-5 bytes */
static inline int xisa_emit_x64_call_rel32(uint8_t *buf, int cap, int32_t target) {
    if (cap < 5)
        return 0;
    uint8_t *p = buf;
    *p++ = 0xe8;
    {
        int32_t v = target;
        memcpy(p, &v, 4);
        p += 4;
    }
    return (int) (p - buf);
}

/* x64.call.r: 2-3 bytes */
static inline int xisa_emit_x64_call_r(uint8_t *buf, int cap, uint8_t target) {
    if (cap < 3)
        return 0;
    uint8_t *p = buf;
    *p++ = 0xff;
    *p++ = (uint8_t) (0x03 << 6 | (2) << 3 | ((target & 7)));
    return (int) (p - buf);
}

/* x64.ret: 1-1 bytes */
static inline int xisa_emit_x64_ret(uint8_t *buf, int cap) {
    if (cap < 1)
        return 0;
    uint8_t *p = buf;
    *p++ = 0xc3;
    return (int) (p - buf);
}

/* x64.jmp.rel32: 5-5 bytes */
static inline int xisa_emit_x64_jmp_rel32(uint8_t *buf, int cap, int32_t target) {
    if (cap < 5)
        return 0;
    uint8_t *p = buf;
    *p++ = 0xe9;
    {
        int32_t v = target;
        memcpy(p, &v, 4);
        p += 4;
    }
    return (int) (p - buf);
}

/* x64.jcc.rel32: 6-6 bytes */
static inline int xisa_emit_x64_jcc_rel32(uint8_t *buf, int cap, uint8_t cc, int32_t target) {
    if (cap < 6)
        return 0;
    uint8_t *p = buf;
    *p++ = 0x80;
    {
        int32_t v = target;
        memcpy(p, &v, 4);
        p += 4;
    }
    return (int) (p - buf);
}

/* x64.push.r: 1-2 bytes */
static inline int xisa_emit_x64_push_r(uint8_t *buf, int cap, uint8_t src) {
    if (cap < 2)
        return 0;
    uint8_t *p = buf;
    *p++ = (uint8_t) (0x50 + (src & 7));
    return (int) (p - buf);
}

/* x64.pop.r: 1-2 bytes */
static inline int xisa_emit_x64_pop_r(uint8_t *buf, int cap, uint8_t dst) {
    if (cap < 2)
        return 0;
    uint8_t *p = buf;
    *p++ = (uint8_t) (0x58 + (dst & 7));
    return (int) (p - buf);
}

/* x64.nop: 1-1 bytes */
static inline int xisa_emit_x64_nop(uint8_t *buf, int cap) {
    if (cap < 1)
        return 0;
    uint8_t *p = buf;
    *p++ = 0x90;
    return (int) (p - buf);
}

/* x64.int3: 1-1 bytes */
static inline int xisa_emit_x64_int3(uint8_t *buf, int cap) {
    if (cap < 1)
        return 0;
    uint8_t *p = buf;
    *p++ = 0xcc;
    return (int) (p - buf);
}

/* x64.neg.r: 3-3 bytes */
static inline int xisa_emit_x64_neg_r(uint8_t *buf, int cap, uint8_t dst) {
    if (cap < 3)
        return 0;
    uint8_t *p = buf;
    *p++ = (uint8_t) (0x48 | ((dst >> 3) << 0));
    *p++ = 0xf7;
    *p++ = (uint8_t) (0x03 << 6 | (3) << 3 | ((dst & 7)));
    return (int) (p - buf);
}

/* x64.not.r: 3-3 bytes */
static inline int xisa_emit_x64_not_r(uint8_t *buf, int cap, uint8_t dst) {
    if (cap < 3)
        return 0;
    uint8_t *p = buf;
    *p++ = (uint8_t) (0x48 | ((dst >> 3) << 0));
    *p++ = 0xf7;
    *p++ = (uint8_t) (0x03 << 6 | (2) << 3 | ((dst & 7)));
    return (int) (p - buf);
}

/* x64.lea.bd32: 7-7 bytes */
static inline int xisa_emit_x64_lea_bd32(uint8_t *buf, int cap, uint8_t dst, uint8_t base,
                                         int32_t disp) {
    if (cap < 7)
        return 0;
    uint8_t *p = buf;
    *p++ = (uint8_t) (0x48 | ((dst >> 3) << 2) | ((base >> 3) << 0));
    *p++ = 0x8d;
    *p++ = (uint8_t) (0x02 << 6 | ((dst & 7)) << 3 | ((base & 7)));
    {
        int32_t v = disp;
        memcpy(p, &v, 4);
        p += 4;
    }
    return (int) (p - buf);
}

/* ========== Instruction Metadata ========== */

#ifndef XISA_MCINSN_INFO_DEFINED
#define XISA_MCINSN_INFO_DEFINED
typedef struct {
    const char *name;
    uint8_t min_bytes;
    uint8_t max_bytes;
    uint8_t noperands;
    uint16_t flags; /* bitmask of XISA_F_* */
} XisaMcInsnInfo;
#endif

#ifndef XISA_F_BRANCH
#define XISA_F_BRANCH (1u << 0)
#endif
#ifndef XISA_F_CALL
#define XISA_F_CALL (1u << 1)
#endif
#ifndef XISA_F_MEMORY
#define XISA_F_MEMORY (1u << 2)
#endif
#ifndef XISA_F_PATCHABLE
#define XISA_F_PATCHABLE (1u << 3)
#endif
#ifndef XISA_F_STACK
#define XISA_F_STACK (1u << 4)
#endif
#ifndef XISA_F_TERMINATOR
#define XISA_F_TERMINATOR (1u << 5)
#endif
#ifndef XISA_F_TRAP
#define XISA_F_TRAP (1u << 6)
#endif

#ifdef XISA_X64_IMPL
static const XisaMcInsnInfo xisa_x64_mcinsn_info[] = {
    {"x64.add.rr", 3, 3, 2, 0},
    {"x64.sub.rr", 3, 3, 2, 0},
    {"x64.and.rr", 3, 3, 2, 0},
    {"x64.or.rr", 3, 3, 2, 0},
    {"x64.xor.rr", 3, 3, 2, 0},
    {"x64.cmp.rr", 3, 3, 2, 0},
    {"x64.test.rr", 3, 3, 2, 0},
    {"x64.imul.rr", 4, 4, 2, 0},
    {"x64.add.ri32", 7, 7, 2, 0},
    {"x64.add.ri8", 4, 4, 2, 0},
    {"x64.sub.ri8", 4, 4, 2, 0},
    {"x64.cmp.ri8", 4, 4, 2, 0},
    {"x64.mov.rr", 3, 3, 2, 0},
    {"x64.mov.ri64", 10, 10, 2, 0},
    {"x64.shl.rcl", 3, 3, 1, 0},
    {"x64.shr.rcl", 3, 3, 1, 0},
    {"x64.sar.rcl", 3, 3, 1, 0},
    {"x64.load.bd32", 7, 7, 3, XISA_F_MEMORY},
    {"x64.store.bd32", 7, 7, 3, XISA_F_MEMORY},
    {"x64.call.rel32", 5, 5, 1, XISA_F_CALL | XISA_F_PATCHABLE},
    {"x64.call.r", 2, 3, 1, XISA_F_CALL},
    {"x64.ret", 1, 1, 0, XISA_F_TERMINATOR},
    {"x64.jmp.rel32", 5, 5, 1, XISA_F_BRANCH | XISA_F_TERMINATOR | XISA_F_PATCHABLE},
    {"x64.jcc.rel32", 6, 6, 2, XISA_F_BRANCH | XISA_F_PATCHABLE},
    {"x64.push.r", 1, 2, 1, XISA_F_STACK},
    {"x64.pop.r", 1, 2, 1, XISA_F_STACK},
    {"x64.nop", 1, 1, 0, 0},
    {"x64.int3", 1, 1, 0, XISA_F_TRAP},
    {"x64.neg.r", 3, 3, 1, 0},
    {"x64.not.r", 3, 3, 1, 0},
    {"x64.lea.bd32", 7, 7, 3, 0},
};
#endif

#endif  // XISA_X64_EMIT_H

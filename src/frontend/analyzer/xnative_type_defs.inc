/* Auto-generated by scripts/embed_native_types.py — DO NOT EDIT */

static const char xr_native_def_array[] = "// Built-in Array<T> type — implementation in src/runtime/object/xarray_methods.c\n\n@native\nclass Array<T> {\n    length: int\n    push(value: T): void\n    pop(): T?\n    shift(): T?\n    unshift(value: T): void\n    slice(start?: int, end?: int): Array<T>\n    splice(start: int, deleteCount?: int, ...items: T): Array<T>\n    concat(...arrays: Array<T>): Array<T>\n    indexOf(value: T): int\n    includes(value: T): bool\n    join(separator?: string): string\n    reverse(): Array<T>\n    sort(compareFn?: fn(a: T, b: T): int): Array<T>\n    map(fn: fn(item: T, index: int): U): Array<U>\n    filter(fn: fn(item: T, index: int): bool): Array<T>\n    reduce(fn: fn(acc: U, item: T): U, initial: U): U\n    forEach(fn: fn(item: T, index: int): void): void\n    find(fn: fn(item: T): bool): T?\n    findIndex(fn: fn(item: T): bool): int\n    every(fn: fn(item: T): bool): bool\n    some(fn: fn(item: T): bool): bool\n    flat(depth?: int): Array<T>\n    fill(value: T, start?: int, end?: int): Array<T>\n    copyWithin(target: int, start: int, end?: int): Array<T>\n}\n";

static const char xr_native_def_bigint[] = "// Built-in BigInt type — implementation in src/runtime/object/xbigint_methods.c\n\n@native\nclass BigInt {\n    abs(): BigInt\n    toString(): string\n    sign(): int\n    isZero(): bool\n    isNegative(): bool\n    isPositive(): bool\n    toInt(): int?\n    toFloat(): float\n}\n";

static const char xr_native_def_bool[] = "// Built-in bool type — implementation in src/runtime/value/xbool_methods.c\n\n@native\nclass bool {\n    toString(): string\n}\n";

static const char xr_native_def_channel[] = "// Built-in Channel<T> type — implementation in src/runtime/coro/xchannel_methods.c\n\n@native\nclass Channel<T> {\n    send(value: T): void\n    recv(): T\n    trySend(value: T): bool\n    tryRecv(): (T, bool)\n    close(): void\n    closed: bool\n}\n";

static const char xr_native_def_coroutine[] = "// Built-in Task type (coroutine handle) — implementation in src/runtime/coro/\n\n@native\nclass Task {\n    done: bool\n    cancelled: bool\n    result: Json\n    error: string?\n    cancel(): void\n}\n";

static const char xr_native_def_enum[] = "// Built-in enum value/type runtime info\n\n@native\nclass EnumValue {\n    name: string\n    value: Json\n    ordinal: int\n    toString(): string\n}\n\n@native\nclass EnumType {\n    memberCount: int\n    getMember(index: int): EnumValue\n}\n";

static const char xr_native_def_exception[] = "// Built-in Exception type — implementation in src/runtime/object/xexception.c\n\n@native\nclass Exception {\n    message: string\n    stack: string\n    toString(): string\n}\n";

static const char xr_native_def_float[] = "// Built-in float type — implementation in src/runtime/value/xfloat_methods.c\n\n@native\nclass float {\n    abs(): float\n    toString(): string\n    toFixed(decimals?: int): string\n    toInt(): int\n    floor(): int\n    ceil(): int\n    round(): int\n    sqrt(): float\n    pow(exp: float): float\n}\n";

static const char xr_native_def_int[] = "// Built-in int type — implementation in src/runtime/value/xint_methods.c\n\n@native\nclass int {\n    abs(): int\n    toString(): string\n    toBigInt(): BigInt\n    toFloat(): float\n    toHex(): string\n    max(other: int): int\n    min(other: int): int\n    floor(): int\n    ceil(): int\n    round(): int\n    sqrt(): float\n    pow(exp: float): float\n}\n";

static const char xr_native_def_json[] = "// Built-in Json type — implementation in src/runtime/object/xjson_methods.c\n\n@native\nclass Json {\n    keys(): Array<string>\n    values(): Array<Json>\n    entries(): Array<[string, Json]>\n    has(key: string): bool\n    get(key: string): Json\n    isEmpty(): bool\n    delete(key: string): void\n    clear(): void\n    toString(): string\n}\n";

static const char xr_native_def_map[] = "// Built-in Map<K, V> type — implementation in src/runtime/object/xmap_methods.c\n\n@native\nclass Map<K, V> {\n    length: int\n    get(key: K): V?\n    set(key: K, value: V): void\n    has(key: K): bool\n    delete(key: K): bool\n    clear(): void\n    keys(): Array<K>\n    values(): Array<V>\n    entries(): Array<[K, V]>\n    forEach(fn: fn(value: V, key: K): void): void\n}\n";

static const char xr_native_def_regex[] = "// Built-in Regex type — implementation in stdlib/regex/regex_methods.c\n\n@native\nclass Regex {\n    test(text: string): bool\n    find(text: string): string?\n    findAll(text: string): Array<string>\n    replace(text: string, replacement: string): string\n    split(text: string): Array<string>\n}\n";

static const char xr_native_def_set[] = "// Built-in Set<T> type — implementation in src/runtime/object/xset_methods.c\n\n@native\nclass Set<T> {\n    length: int\n    add(value: T): void\n    has(value: T): bool\n    delete(value: T): bool\n    clear(): void\n    values(): Array<T>\n    forEach(fn: fn(value: T): void): void\n}\n";

static const char xr_native_def_string[] = "// Built-in string type — implementation in src/runtime/object/xstring_methods.c\n\n@native\nclass string {\n    length: int\n    charAt(index: int): string\n    charCodeAt(index: int): int\n    concat(...strings: string): string\n    includes(search: string): bool\n    indexOf(search: string, start?: int): int\n    lastIndexOf(search: string): int\n    slice(start: int, end?: int): string\n    substring(start: int, end?: int): string\n    substr(start: int, length?: int): string\n    toLowerCase(): string\n    toUpperCase(): string\n    trim(): string\n    trimStart(): string\n    trimEnd(): string\n    split(separator: string, limit?: int): Array<string>\n    replace(search: string, replacement: string): string\n    replaceAll(search: string, replacement: string): string\n    repeat(count: int): string\n    startsWith(search: string): bool\n    endsWith(search: string): bool\n    padStart(length: int, pad?: string): string\n    padEnd(length: int, pad?: string): string\n    match(pattern: string): Array<string>?\n}\n";

static const char xr_native_def_stringbuilder[] = "// Built-in StringBuilder type — implementation in src/runtime/object/xstringbuilder_builtins.c\n\n@native\nclass StringBuilder {\n    length: int\n    append(value: Json): StringBuilder\n    toString(): string\n    clear(): StringBuilder\n}\n";

#define XR_NATIVE_TYPE_DEFS(X) \
    X("array", xr_native_def_array) \
    X("bigint", xr_native_def_bigint) \
    X("bool", xr_native_def_bool) \
    X("channel", xr_native_def_channel) \
    X("coroutine", xr_native_def_coroutine) \
    X("enum", xr_native_def_enum) \
    X("exception", xr_native_def_exception) \
    X("float", xr_native_def_float) \
    X("int", xr_native_def_int) \
    X("json", xr_native_def_json) \
    X("map", xr_native_def_map) \
    X("regex", xr_native_def_regex) \
    X("set", xr_native_def_set) \
    X("string", xr_native_def_string) \
    X("stringbuilder", xr_native_def_stringbuilder)


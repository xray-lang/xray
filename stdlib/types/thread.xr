// sys.Thread is compiler-defined, not a native constructor surface.
//
// Task 147 selected the language/IR construct form:
//     sys.Thread.spawn(fn() -> T { ... }) -> sys.Thread<T>
//
// Do not declare `@native class Thread<T>` here. A native method accepting a
// closure would bypass the same capture/move rules that protect `go`.

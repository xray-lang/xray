// Built-in Semaphore type; implemented by the runtime.
class Semaphore {
    available: i64
    isClosed: bool
    // @receiver_write
    release(count?: i64) -> i64
    // @receiver_write
    tryAcquire() -> bool
    // @receiver_write
    acquire() -> bool
    // @receiver_write
    close()
}

// Built-in CountdownLatch type; implemented by the runtime.
class CountdownLatch {
    remaining: i64
    isClosed: bool
    reset(count: i64) -> bool
    done(count?: i64) -> i64
    tryWait() -> bool
    wait() -> bool
    close()
}

{
  "id": "coroutine",
  "title": "Coroutines & Concurrency",
  "spec_anchor": "#10-并发与协程-concurrency",
  "aliases": [
    "go",
    "goroutine",
    "goroutines",
    "await",
    "task",
    "async",
    "concurrency",
    "scope",
    "select",
    "defer"
  ],
  "sections": [
    {
      "heading": "go — spawn a coroutine",
      "fences": [
        "coro-go-forms"
      ]
    },
    {
      "heading": "await",
      "fences": [
        "coro-await-forms"
      ]
    },
    {
      "heading": "Common constraints",
      "markdown": "`go { ... }` is a zero-argument block form and cannot capture ordinary mutable locals; pass local data with `go fn(x: T) -> R { ... }(arg)` or `go worker(arg)`. `await all` is homogeneous: mixed `Task<int>` / `Task<string>` collections must be awaited individually or converted to a shared result type."
    },
    {
      "heading": "Task handle",
      "fences": [
        "coro-task-handle"
      ]
    },
    {
      "heading": "scope — structured concurrency",
      "fences": [
        "coro-scope",
        "coro-linked-supervisor-scope"
      ]
    },
    {
      "heading": "select — multiplex channels",
      "fences": [
        "coro-select"
      ]
    },
    {
      "heading": "yield — cooperative safepoint",
      "fences": [
        "coro-yield-loop",
        "expr-yield"
      ]
    },
    {
      "heading": "defer — LIFO cleanup",
      "fences": [
        "stmt-defer"
      ]
    }
  ]
}

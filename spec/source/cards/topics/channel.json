{
  "id": "channel",
  "title": "Channel",
  "spec_anchor": "#105-channel",
  "aliases": ["channels", "chan", "Channel", "send", "recv", "buffered"],
  "lead": "Channels are the primary inter-coroutine communication mechanism.",
  "sections": [
    {
      "heading": "Declaration",
      "fences": ["channel-decl-variants"],
      "after_text": "Name a channel with `const`. `Channel<T>` is an audited synchronization handle: its binding is stable while `send`/`recv` mutate protected runtime state."
    },
    {
      "heading": "Operations",
      "fences": ["channel-basic-ops"]
    },
    {
      "heading": "Function parameter type",
      "fences": ["channel-param-type"]
    },
    {
      "heading": "Concurrency rules",
      "bullets": [
        "Channels can be captured by `go` closures through their verified synchronization capability",
        "Execution-local graphs require an explicit `move` or `copy` at send sites",
        "Channel payload transfer never performs an implicit deep copy"
      ]
    }
  ]
}

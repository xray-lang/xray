{
  "id": "concurrency_rules",
  "title": "Concurrency Safety Rules",
  "spec_anchor": "#1011-并发安全模型",
  "aliases": [
    "shared_rules",
    "sharing",
    "move",
    "safety",
    "concurrent",
    "atomic"
  ],
  "sections": [
    {
      "heading": "Sharing model",
      "markdown": "- `shared` creates a stable, non-rebindable identity on the global heap\n- `shared` values may be captured by `go` closures and passed across coroutine boundaries directly\n- `move` is for explicit ownership transfer of rebindable local `var` values, not for `shared` bindings\n- ordinary local variables cannot be captured by `go` closures; pass data explicitly with parameters, `copy(...)`, `move`, or `shared`\n- `go { ... }` is a zero-argument block form; pass local data with `go fn(x: T) -> R { ... }(arg)` or `go worker(arg)`"
    },
    {
      "heading": "await all",
      "markdown": "`await all` requires a homogeneous collection of tasks: every element must have the same static `Task<T>` type, and the result is `Array<T>`. Await heterogeneous tasks individually, or convert each task to a shared enum / union / Json result type."
    },
    {
      "heading": "Move into coroutine",
      "fences": [
        "coro-move-argument"
      ]
    },
    {
      "heading": "Move ownership",
      "fences": [
        "coro-move-transfer"
      ]
    },
    {
      "heading": "Channels",
      "fences": [
        "channel-basic-ops"
      ]
    },
    {
      "heading": "Atomic",
      "markdown": "`Atomic<T>` wraps `int`/`float`/`bool` with lock-free atomic operations. Must be `shared`; `move` prohibited. Methods: `load`, `store`, `add`, `sub`, `fetchAdd`, `fetchSub`, `swap`, `compareExchange`, `toggle`. Optional `Ordering` enum parameter (default `SeqCst`)."
    }
  ]
}

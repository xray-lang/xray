{
  "module": "simd",
  "summary": "Portable fixed-width integer vectors",
  "lead": "Portable 128-bit, 256-bit, and 512-bit integer-vector operations. The same API has scalar semantics in the VM and on targets without matching vector instructions; native AOT lowers supported operations according to the selected target.",
  "sections": [
    {
      "heading": "Vector types",
      "bullets": [
        "`U8x16`, `U32x4`, and `U64x2` are the portable 128-bit baseline.",
        "`U8x32`, `U32x8`, and `U64x4` provide 256-bit operations for algorithms that select a wide path.",
        "`U8x64`, `U32x16`, and `U64x8` provide the AVX-512F-width operations used by 64-byte stripe algorithms.",
        "`Capabilities.nativeBytes()` returns the native vector width chosen for the compile target; it is at least 16 bytes."
      ]
    },
    {
      "heading": "Loading and storing",
      "before_text": "Use `load` and `store` with a typed `Slice<T>`. The optional offset is measured in elements. `fromLanes` constructs a vector from a fixed array, while `splat` fills every lane with one value where that operation is available."
    },
    {
      "heading": "Target lowering",
      "before_text": "Native lowering is available for AArch64 NEON, x86 SSE2/AVX2/AVX-512F, and PowerPC64 VSX. Use `--simd avx512` for an explicit AVX-512F target or `--simd dispatch` for x86 CPU/OS selection across 16, 32, and 64 bytes. VSX is supported on both `powerpc64-linux-musl` and `powerpc64le-linux-musl`; use `--simd vsx` for an explicit Power8-baseline target plan."
    },
    {
      "heading": "Operations",
      "before_text": "The vector types provide lane extraction and replacement, integer arithmetic, bitwise operations, shifts, shuffles, widening multiplication, reductions, and bit-preserving reinterpretation where supported by the type. Shuffle lane arguments must be compile-time integers within the vector's lane range. Reinterpretation is endian-neutral: byte zero is the least-significant byte of numeric lane zero, so VM, scalar AOT, and native SIMD produce identical lane values on little- and big-endian targets."
    },
    {
      "heading": "Example",
      "code": [
        "import { Capabilities, U32x4 } from simd\n\nfn main() {\n    var words = Array<u32>([1, 2, 3, 4])\n    var view: Slice<u32> = words[:]\n    var result = U32x4.load(view).add(U32x4.splat(10))\n\n    print(result.extract(2))\n    print(Capabilities.nativeBytes() >= 16)\n}\n\nmain()"
      ]
    }
  ]
}

{
  "module": "mem",
  "summary": "Raw memory, Buffer allocation, page allocation, volatile access, and memory primitives",
  "lead": "Raw-memory capabilities for explicit low-level work. `mem.alloc*` returns managed `Buffer`; GC and heap introspection lives in the `runtime` module."
}

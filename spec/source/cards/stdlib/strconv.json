{
  "module": "strconv",
  "summary": "Explicit string and scalar conversions",
  "lead": "Explicit string and scalar conversion helpers.",
  "sections": [
    {
      "heading": "Functions",
      "bullets": [
        "`strconv.parseInt(value)` returns `int?`",
        "`strconv.parseFloat(value)` returns `float?`"
      ]
    }
  ]
}

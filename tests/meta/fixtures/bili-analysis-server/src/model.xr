export struct VideoSummary {
    aid: i64
    title: string
    views: i64
}

export fn engagementScore(video: VideoSummary) -> f64 {
    if (video.views <= 0) {
        return 0.0
    }
    return (len(video.title) as f64) / (video.views as f64)
}

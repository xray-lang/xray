# Known-deviating spec snippets: sha:<sha12> of the fence body per row.
# Rows are removed as the spec or the compiler converges;
# regenerate with --update-baseline only alongside a written reason.
#
# All four rows below were checked by hand and are benign: the fence is an
# excerpt, not a program. One has its failing line commented out, one is an
# `// OK` demonstration the extractor mislabels, and two declare a shape
# without the call that would trip the diagnostic. Rewritten as complete
# programs, the compiler does reject them (E0363 and E0356 respectively).
sha:8c8c2e3808ce  # near line 1116, error-demo: <no output>
sha:0ea5789a3757  # near line 3484, error-demo: <no output>
sha:1e282353faf7  # near line 4265, error-demo: <no output>
sha:230d115261b4  # near line 5087, error-demo: <no output>

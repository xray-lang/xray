// xray: Fibonacci benchmark
fn fib(n: int): int {
    if (n <= 1) {
        return n
    }
    return fib(n - 1) + fib(n - 2)
}

// Warmup: trigger JIT compilation (threshold=100)
for (let i = 0; i < 120; i++) {
    fib(5)
}

print(fib(40))

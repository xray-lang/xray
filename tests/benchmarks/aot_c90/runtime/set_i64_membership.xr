fn run(n: int, rounds: int) -> int {
    var s: Set<int> = #[]

    var i = 0
    while (i < n) {
        s.add(i * 2)
        i = i + 1
    }

    var hits = 0
    var r = 0
    while (r < rounds) {
        i = 0
        while (i < n * 2) {
            if (s.has(i)) {
                hits = hits + 1
            }
            i = i + 1
        }
        r = r + 1
    }

    return hits + s.size
}

print(run(150000, 4))

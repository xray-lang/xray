fn run(n: int, queries: int) -> int {
    var haystack: Array<string> = []
    var i = 0
    while (i < n) {
        haystack.push("entry-" + i.toString())
        i = i + 1
    }

    var found = 0
    i = 0
    while (i < queries) {
        var needle = "entry-" + ((i * 13) % (n * 2)).toString()
        var idx = haystack.indexOf(needle)
        if (idx >= 0) {
            found = found + idx
        }
        i = i + 1
    }

    return found
}

print(run(2000, 3000))

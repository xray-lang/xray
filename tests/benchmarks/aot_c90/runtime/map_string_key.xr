fn run(n: i64) -> i64 {
    var m: Map<string, i64> = #{}

    var i = 0
    while (i < n) {
        var key = "user:" + ((i * 7) % n).toString()
        m.set(key, i)
        i = i + 1
    }

    var sum = 0
    i = 0
    while (i < n) {
        var key = "user:" + i.toString()
        if (m.containsKey(key)) {
            sum = sum + m.get(key)!
        }
        i = i + 1
    }

    return sum + len(m)
}

print(run(60000))

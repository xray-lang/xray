fn run(n: int, rounds: int) -> int {
    var bytes = Bytes(n)

    var i = 0
    while (i < bytes.length) {
        bytes[i] = i * 3 + 1
        i = i + 1
    }

    var r = 0
    var sum = 0
    while (r < rounds) {
        var j = 0
        while (j < bytes.length) {
            sum = sum + bytes[j]
            j = j + 1
        }
        r = r + 1
    }
    return sum
}

print(run(256, 20000))

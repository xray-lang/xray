fn run(n: int, rounds: int) -> int {
    var bytes = Array<byte>(n)
    var limit = len(bytes)

    var i = 0
    while (i < limit) {
        bytes[i] = i * 3 + 1
        i = i + 1
    }

    var r = 0
    var sum = 0
    while (r < rounds) {
        var j = 0
        while (j < limit) {
            sum = sum + bytes[j]
            j = j + 1
        }
        r = r + 1
    }
    return sum
}

print(run(256, 200000))

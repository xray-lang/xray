fn run(n: int) -> int {
    var bytes: Array<uint8> = []
    var i = 0
    while (i < n) {
        bytes.push(i)
        i = i + 1
    }

    var sum = 0
    i = 0
    while (i < bytes.length) {
        sum = sum + bytes[i]
        i = i + 1
    }
    return sum
}

print(run(200000))

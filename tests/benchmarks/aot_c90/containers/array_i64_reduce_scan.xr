fn run(n: int) -> int {
    var values: Array<int> = []
    var i = 0
    while (i < n) {
        values.push((i * 17) % 251)
        i = i + 1
    }

    return values.reduce(fn(acc: int, x: int) -> int {
        return acc + x * 3
    }, 5)
}

print(run(200000))

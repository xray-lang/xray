fn run(n: i64) -> i64 {
    var values: Array<i64> = []
    var i = 0
    while (i < n) {
        values.push((i * 17) % 251)
        i = i + 1
    }

    return values.reduce(fn(acc: i64, x: i64) -> i64 {
        return acc + x * 3
    }, 5)
}

print(run(200000))

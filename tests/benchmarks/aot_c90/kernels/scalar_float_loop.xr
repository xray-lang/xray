fn run(n: i64) -> f64 {
    var acc = 0.0
    var x = 1.0
    var i = 0
    while (i < n) {
        acc = acc + x * 1.000001
        x = x + 0.000001
        if (x > 2.0) {
            x = 1.0
        }
        i = i + 1
    }
    return acc
}

print(run(300000))

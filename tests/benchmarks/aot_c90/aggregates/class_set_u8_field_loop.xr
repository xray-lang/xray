class ByteSetBag {
    values: Set<u8>

    constructor() {
        var values: Set<u8> = #[]
        this.values = values
    }

    fill(n: i64) -> i64 {
        var i = 0
        while (i < n) {
            this.values.add(i as u8)
            i = i + 1
        }
        return len(this.values)
    }

    scan(n: i64, rounds: i64) -> i64 {
        var r = 0
        var hits = 0
        while (r < rounds) {
            var i = 0
            while (i < n) {
                if (this.values.contains(i as u8)) {
                    hits = hits + i
                }
                i = i + 1
            }
            r = r + 1
        }
        return hits
    }

    prune(n: i64) -> i64 {
        var i = 0
        var removed = 0
        while (i < n) {
            if (this.values.delete(i as u8)) {
                removed = removed + 1
            }
            i = i + 2
        }
        return removed + len(this.values)
    }
}

fn class_set_u8_field_loop_run(n: i64, rounds: i64) -> i64 {
    var bag = ByteSetBag()
    var count = bag.fill(n)
    var sum = bag.scan(n, rounds)
    return sum + count + bag.prune(n)
}

print(class_set_u8_field_loop_run(64, 5000))

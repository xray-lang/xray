class IntFloatMapBag {
    values: Map<i64, f32>

    constructor() {
        var values: Map<i64, f32> = #{}
        this.values = values
    }

    ref fill(n: i64) -> i64 {
        var i = 0
        while (i < n) {
            this.values.set(i, (i as f32) * 0.25 + 1.0)
            i = i + 1
        }
        return len(this.values)
    }

    scan(n: i64, rounds: i64) -> f64 {
        var r = 0
        var sum = 0.0
        while (r < rounds) {
            var i = 0
            while (i < n) {
                if (this.values.containsKey(i)) {
                    sum = sum + this.values.get(i)!
                }
                i = i + 1
            }
            r = r + 1
        }
        return sum
    }
}

fn class_map_i64_f32_field_loop_run(n: i64, rounds: i64) -> f64 {
    var bag = IntFloatMapBag()
    var count = bag.fill(n)
    return bag.scan(n, rounds) + (count as f64)
}

print(class_map_i64_f32_field_loop_run(64, 5000))

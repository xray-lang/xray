class IntFloatMapBag {
    values: Map<int, float32>

    constructor() {
        let values: Map<int, float32> = #{}
        this.values = values
    }

    fill(n: int) -> int {
        let i = 0
        while (i < n) {
            this.values.set(i, i * 0.25 + 1.0)
            i = i + 1
        }
        return this.values.size
    }

    scan(n: int, rounds: int) -> float {
        let r = 0
        let sum = 0.0
        while (r < rounds) {
            let i = 0
            while (i < n) {
                if (this.values.has(i)) {
                    sum = sum + this.values.get(i)
                }
                i = i + 1
            }
            r = r + 1
        }
        return sum
    }
}

fn class_map_i64_f32_field_loop_run(n: int, rounds: int) -> float {
    let bag = IntFloatMapBag()
    let count = bag.fill(n)
    return bag.scan(n, rounds) + count
}

print(class_map_i64_f32_field_loop_run(64, 5000))

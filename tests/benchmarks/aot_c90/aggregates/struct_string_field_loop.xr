struct Counter {
    total: i64
    step: i64
    name: string
}

var counter = Counter{total: 1, step: 3, name: "counter"}

fn run(n: i64) -> i64 {
    var p = counter
    var i = 0
    var sum = 0
    while (i < n) {
        p.total = p.total + p.step + i
        sum = sum + p.total
        i = i + 1
    }
    return sum + p.total
}

print(run(2000000))

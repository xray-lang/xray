class IntBoolMapBag {
    values: Map<i64, bool>

    constructor() {
        var values: Map<i64, bool> = #{}
        this.values = values
    }

    ref fill(n: i64) -> i64 {
        var i = 0
        while (i < n) {
            this.values.set(i, i % 3 == 0)
            i = i + 1
        }
        return len(this.values)
    }

    scan(n: i64, rounds: i64) -> i64 {
        var r = 0
        var total = 0
        while (r < rounds) {
            var i = 0
            while (i < n) {
                if (this.values.containsKey(i)) {
                    if (this.values.get(i) == true) {
                        total = total + 1
                    }
                }
                i = i + 1
            }
            r = r + 1
        }
        return total
    }
}

fn class_map_i64_bool_field_loop_run(n: i64, rounds: i64) -> i64 {
    var bag = IntBoolMapBag()
    var count = bag.fill(n)
    return bag.scan(n, rounds) + count
}

print(class_map_i64_bool_field_loop_run(64, 5000))

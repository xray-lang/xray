/*
** mandelbrot_aot.xr - Mandelbrot benchmark (AOT typed version)
*/

fn mandelbrot(size: int): int {
    var w = size
    var h = size
    var count = 0
    var inv_w = 2.0 / float(w)
    var inv_h = 2.0 / float(h)
    for (var y = 0; y < h; y++) {
        var ci = inv_h * float(y) - 1.0
        for (var x = 0; x < w; x++) {
            var zr = 0.0
            var zi = 0.0
            var tr = 0.0
            var ti = 0.0
            var cr = inv_w * float(x) - 1.5
            var i = 0
            while (i < 50 && (tr + ti) <= 4.0) {
                zi = 2.0 * zr * zi + ci
                zr = tr - ti + cr
                tr = zr * zr
                ti = zi * zi
                i = i + 1
            }
            if ((tr + ti) <= 4.0) {
                count = count + 1
            }
        }
    }
    return count
}

fn run(size: int) {
    var count = mandelbrot(size)
    print("Points in set: " + count)
}

var size: int = len(process.args) > 0 ? int(process.args[0]) ?? 200 : 200
run(size)

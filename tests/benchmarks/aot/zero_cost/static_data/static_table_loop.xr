struct Pair {
    left: int
    right: int
}

fn run(n: int) -> int {
    const pair = comptime Pair{left: 3, right: 4}
    const bias = comptime 7
    var i = 0
    var sum = 0
    while (i < n) {
        sum = sum + pair.left + pair.right + bias + (i % 3)
        i = i + 1
    }
    return sum
}

print(run(10000))

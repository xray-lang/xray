fn run(n: int) -> int {
    const inc = fn(x: int) -> int {
        return x + 1
    }
    var i = 0
    var sum = 0
    while (i < n) {
        sum = sum + inc(i % 11)
        i = i + 1
    }
    return sum
}

print(run(50000))

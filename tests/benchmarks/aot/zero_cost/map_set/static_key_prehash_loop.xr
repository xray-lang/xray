const SCORES: Map<string, int> = #{"ada": 1}

fn run(n: int) -> int {
    var i = 0
    var sum = 0
    while (i < n) {
        if (SCORES.containsKey("ada")) {
            sum = sum + SCORES.get("ada")!
        }
        i = i + 1
    }
    return sum
}

print(run(20000))

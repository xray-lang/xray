class Box<T> {
    value: T

    constructor(value: T) {
        this.value = value
    }

    get() -> T {
        return this.value
    }
}

fn id<T>(x: T) -> T {
    return x
}

fn run(n: i64) -> i64 {
    var i = 0
    var sum = 0
    while (i < n) {
        var box = Box<i64>((i % 17) + 1)
        sum = sum + id<i64>(box.get())
        i = i + 1
    }
    return sum
}

print(run(30000))

import http
import { HttpRequest, HttpResponse } from http

http.routeHandler("GET", "/", fn(req: HttpRequest) -> HttpResponse {
    return http.textResponse("Hello, World!")
})

http.routeHandler("GET", "/json", fn(req: HttpRequest) -> HttpResponse {
    return http.jsonResponse({message: "Hello, World!"})
})

http.listen(8080)

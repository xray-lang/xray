schema = 1
module = "os"
contract_revision = 2
legacy_commit = "971205b8e773e1d3eeaeafb9432355cf8be038e6"
legacy_oracle = "executable"
legacy_cases_manifest = "tests/stdlib/contracts/os/legacy_expected.jsonl"
legacy_public_surface = [
  "exec",
  "getenv",
  "setenv",
  "platform",
  "arch",
  "cpuCount",
  "hostname",
]
intentional_semantic_changes = [
  "os is a permanent L2 syscall shell; the .def signatures are the typed contract",
  "exec captures bytes with independent bounds instead of assuming subprocess output is UTF-8 text",
]
known_legacy_bugs = [
  "none recorded for the native baseline",
]
equivalence = ["value", "error", "effect", "complexity"]
diff_cases_manifest = "tests/stdlib/contracts/os/diff_cases.txt"
benchmark_suite = "stdlib/os"

[[legacy_behavior]]
id = "query-parity"
classification = "required"
new_behavior = "platform/arch/cpuCount/env queries return consistent typed values on VM and AOT"

[[legacy_behavior]]
id = "exec-result"
classification = "required"
new_behavior = "exec composes sys.Process and Pipe in Xray, captures stdout/stderr as bounded byte arrays, and reports independent truncation"

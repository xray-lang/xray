schema = 1
module = "text"
contract_revision = 1
legacy_commit = "971205b8e773e1d3eeaeafb9432355cf8be038e6"
legacy_oracle = "executable"
legacy_cases_manifest = "tests/stdlib/contracts/text/legacy_expected.jsonl"
legacy_public_surface = [
  "lower",
  "upper",
  "trim",
  "trimStart",
  "trimEnd",
  "padStart",
  "padEnd",
  "reverseRunes",
]
intentional_semantic_changes = [
  "text transformation semantics are owned by stdlib/text/text.xr as the sole source",
  "case/trim/pad/reverse operate on always-valid UTF-8 strings with rune-aware behavior",
]
known_legacy_bugs = [
  "earlier string case/trim helpers were split across surfaces without a single text source",
]
equivalence = ["value", "error", "effect", "complexity"]
diff_cases_manifest = "tests/stdlib/contracts/text/diff_cases.txt"
benchmark_suite = "stdlib/text"

[[legacy_behavior]]
id = "case-trim-pad"
classification = "required"
new_behavior = "lower/upper/trim/pad produce identical strings on VM and AOT"

[[legacy_behavior]]
id = "rune-aware"
classification = "required"
new_behavior = "reverseRunes reverses by Unicode scalar identically on VM and AOT"

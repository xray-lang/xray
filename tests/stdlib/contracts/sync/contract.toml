schema = 1
module = "sync"
contract_revision = 1
legacy_commit = "971205b8e773e1d3eeaeafb9432355cf8be038e6"
legacy_oracle = "executable"
legacy_cases_manifest = "tests/stdlib/contracts/sync/legacy_expected.jsonl"
legacy_public_surface = [
  "Semaphore",
  "CountdownLatch",
  "EventCount",
  "WorkQueue",
  "ResultGroup",
]
intentional_semantic_changes = [
  "synchronization primitive classes are composed in stdlib/sync/sync.xr over scheduler-backed native bodies",
  "primitives are explicitly imported from sync rather than exposed as prelude bare names",
]
known_legacy_bugs = [
  "earlier prelude exposed these primitives as bare global names",
]
equivalence = ["value", "error", "effect", "complexity"]
diff_cases_manifest = "tests/stdlib/contracts/sync/diff_cases.txt"
benchmark_suite = "stdlib/sync"

[[legacy_behavior]]
id = "primitive-classes"
classification = "required"
new_behavior = "Semaphore/CountdownLatch/EventCount/WorkQueue/ResultGroup behave identically on VM and AOT"

[[legacy_behavior]]
id = "explicit-import"
classification = "required"
new_behavior = "the primitives are only reachable via explicit import from sync"

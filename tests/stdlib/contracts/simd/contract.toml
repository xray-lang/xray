schema = 1
module = "simd"
contract_revision = 1
legacy_commit = "971205b8e773e1d3eeaeafb9432355cf8be038e6"
legacy_oracle = "executable"
legacy_cases_manifest = "tests/stdlib/contracts/simd/legacy_expected.jsonl"
legacy_public_surface = [
  "add",
  "sub",
  "mul",
  "min",
  "max",
  "load",
  "store",
]
intentional_semantic_changes = [
  "SIMD vector semantics are owned by stdlib/simd/simd.xr with portable integer/float lanes",
  "AOT lowers lane ops to native vectors; VM interprets the same portable semantics",
]
known_legacy_bugs = [
  "earlier builds exposed only a handful of float vec ops without portable integer lanes",
]
equivalence = ["value", "error", "effect", "complexity"]
diff_cases_manifest = "tests/stdlib/contracts/simd/diff_cases.txt"
benchmark_suite = "stdlib/simd"

[[legacy_behavior]]
id = "portable-lane-ops"
classification = "required"
new_behavior = "integer and float lane arithmetic yields identical results on VM and AOT"

[[legacy_behavior]]
id = "intrinsic-identity"
classification = "required"
new_behavior = "reexported simd intrinsics keep a single canonical identity across modules"

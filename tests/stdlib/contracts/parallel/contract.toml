schema = 1
module = "parallel"
contract_revision = 1
legacy_commit = "971205b8e773e1d3eeaeafb9432355cf8be038e6"
legacy_oracle = "executable"
legacy_cases_manifest = "tests/stdlib/contracts/parallel/legacy_expected.jsonl"
legacy_public_surface = [
  "forEach",
  "map",
  "reduce",
]
intentional_semantic_changes = [
  "parallel for/map/reduce semantics are owned by stdlib/parallel/parallel.xr over the runtime scheduler",
  "AOT parallel loops reach native scheduler throughput; VM runs the same batched semantics",
]
known_legacy_bugs = [
  "earlier intrinsic surface predated the unified parallel .xr composition",
]
equivalence = ["value", "error", "effect", "complexity"]
diff_cases_manifest = "tests/stdlib/contracts/parallel/diff_cases.txt"
benchmark_suite = "stdlib/parallel"

[[legacy_behavior]]
id = "parallel-combinators"
classification = "required"
new_behavior = "forEach/map/reduce produce order-correct results identical on VM and AOT"

[[legacy_behavior]]
id = "batch-equivalence"
classification = "required"
new_behavior = "VM batched execution matches AOT native execution for the covered workloads"

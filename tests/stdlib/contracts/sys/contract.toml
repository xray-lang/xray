schema = 1
module = "sys"
contract_revision = 1
legacy_commit = "971205b8e773e1d3eeaeafb9432355cf8be038e6"
legacy_oracle = "executable"
legacy_cases_manifest = "tests/stdlib/contracts/sys/legacy_expected.jsonl"
legacy_public_surface = [
  "OsMutex",
  "OsCondvar",
  "OsBarrier",
  "OsRwLock",
  "OsOnce",
  "cpuCount",
  "threadYield",
]
intentional_semantic_changes = [
  "sys is a permanent L2 OS thread/lock shell; the .def signatures are the typed contract",
  "this contract is a behavior baseline lock, not a migration",
]
known_legacy_bugs = [
  "none recorded for the native baseline",
]
equivalence = ["value", "error", "effect", "complexity"]
diff_cases_manifest = "tests/stdlib/contracts/sys/diff_cases.txt"
benchmark_suite = "stdlib/sys"

[[legacy_behavior]]
id = "lock-primitives"
classification = "required"
new_behavior = "OsMutex/OsCondvar/OsBarrier/OsRwLock/OsOnce enforce mutual exclusion identically on VM and AOT"

[[legacy_behavior]]
id = "os-lock-naming"
classification = "required"
new_behavior = "the OS locks use the sys.Os* names with no prelude bare aliases"

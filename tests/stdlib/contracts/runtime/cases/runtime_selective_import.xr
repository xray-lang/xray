import {
    info,
    liveBytes,
    liveObjects,
} from runtime

print(liveBytes() >= 0)
print(liveObjects() >= 0)
var snapshot = info()
print(snapshot.liveBytes >= 0)
print(snapshot.liveObjects >= 0)

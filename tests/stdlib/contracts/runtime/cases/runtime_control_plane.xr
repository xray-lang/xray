// VM/AOT control-plane contract: each backend observes its current execution-local domain.
//
// The cycle-collector toggles this case used to exercise are gone with the
// collector (task 247). What remains is the part that still has meaning on
// both backends: the heap counters.
import runtime

print(runtime.liveBytes() >= 0)
print(runtime.liveObjects() >= 0)
print(runtime.finalizerCount() >= 0)
var info = runtime.info()
print(info.liveBytes >= 0)
print(info.liveObjects >= 0)
print(info.finalizerCount >= 0)

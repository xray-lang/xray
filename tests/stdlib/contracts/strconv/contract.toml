schema = 1
module = "strconv"
contract_revision = 1
legacy_commit = "971205b8e773e1d3eeaeafb9432355cf8be038e6"
legacy_oracle = "executable"
legacy_cases_manifest = "tests/stdlib/contracts/strconv/legacy_expected.jsonl"
legacy_public_surface = [
  "parseInt",
  "parseFloat",
]
intentional_semantic_changes = [
  "string-to-number conversion semantics are owned by stdlib/strconv/strconv.xr as the sole source",
  "parseInt/parseFloat return typed optionals (int?/float?), signaling failure as null rather than a sentinel number",
]
known_legacy_bugs = [
  "earlier callers relied on adjacent modules for numeric parsing without a single typed source",
]
equivalence = ["value", "error", "effect", "complexity"]
diff_cases_manifest = "tests/stdlib/contracts/strconv/diff_cases.txt"
benchmark_suite = "stdlib/strconv"

[[legacy_behavior]]
id = "optional-parse"
classification = "required"
new_behavior = "parseInt/parseFloat return the parsed value or null identically on VM and AOT"

[[legacy_behavior]]
id = "reject-malformed"
classification = "required"
new_behavior = "malformed input yields null (not a coerced value) on VM and AOT"

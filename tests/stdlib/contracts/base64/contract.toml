schema = 1
module = "base64"
contract_revision = 1
legacy_commit = "e50953853746185e1f2e8e2d6371898166c6a42a"
legacy_public_surface = ["encode", "decode", "encodeUrl", "decodeUrl", "isValid"]
intentional_semantic_changes = ["byte-oriented exports encodeBytes and decodeToBytes are canonical"]
known_legacy_bugs = []
equivalence = ["value", "error", "effect", "complexity"]
diff_cases_manifest = "tests/stdlib/contracts/base64/diff_cases.txt"
benchmark_suite = "stdlib/base64"

[[legacy_behavior]]
id = "rfc4648-roundtrip"
classification = "required"
new_behavior = "standard and URL-safe alphabets round-trip on VM and AOT"

[[legacy_behavior]]
id = "invalid-input-null"
classification = "required"
new_behavior = "invalid encodings return null without partial output"

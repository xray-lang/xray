schema = 1
module = "base64"
contract_revision = 1
legacy_commit = "e50953853746185e1f2e8e2d6371898166c6a42a"
legacy_oracle = "classification_only"
legacy_public_surface = ["encode", "decode", "encodeUrl", "decodeUrl", "isValid"]
intentional_semantic_changes = [
  "public encode accepts Slice<byte>",
  "decode returns Array<byte> and throws Base64Error on invalid input",
  "URL-safe and padding policy are explicit options",
  "encodeBytes/decodeToBytes/encodeUrl/decodeUrl aliases are removed"
]
known_legacy_bugs = []
equivalence = ["value", "error", "effect", "complexity"]
diff_cases_manifest = "tests/stdlib/contracts/base64/diff_cases.txt"
benchmark_suite = "stdlib/base64"

[[legacy_behavior]]
id = "rfc4648-roundtrip"
classification = "required"
new_behavior = "standard and URL-safe alphabets round-trip through Slice<byte>/Array<byte> on VM and AOT"

[[legacy_behavior]]
id = "typed-error-invalid-input"
classification = "required"
new_behavior = "invalid encodings throw Base64Error and do not produce partial output"

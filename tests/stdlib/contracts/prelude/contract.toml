schema = 1
module = "prelude"
contract_revision = 1
legacy_commit = "971205b8e773e1d3eeaeafb9432355cf8be038e6"
legacy_oracle = "classification_only"
legacy_public_surface = [
  "Array",
  "Map",
  "string",
]
intentional_semantic_changes = [
  "prelude provides the permanent L1 core value types (Array/Map/string) with opcode/intrinsic bindings",
  "this contract is a behavior baseline lock for the implicit core container/string semantics, not a migration",
]
known_legacy_bugs = [
  "none recorded for the native baseline",
]
equivalence = ["value", "error", "effect", "complexity"]
diff_cases_manifest = "tests/stdlib/contracts/prelude/diff_cases.txt"
benchmark_suite = "stdlib/prelude"

[[legacy_behavior]]
id = "core-container-semantics"
classification = "required"
new_behavior = "Array sort/index/len and Map insert/index/len behave identically on VM and AOT"

[[legacy_behavior]]
id = "string-core-semantics"
classification = "required"
new_behavior = "string len/concat/slice behave identically on VM and AOT"

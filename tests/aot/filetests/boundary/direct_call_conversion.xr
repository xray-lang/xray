fn add_with_capture(delta: i64) -> i64 {
    fn add(x: i64) -> i64 {
        var acc = x
        var i = 0
        while (i < 4) {
            acc = acc + delta + i
            i = i + 1
        }
        return acc
    }
    return add(5)
}

print(add_with_capture(7))

import mem
import { allocZeroed } from mem

fn xray_alloc_selective_score(n: int) -> int {
    var b = allocZeroed(n)
    var view: Slice<byte> = b.asMutBytes()
    if (n > 0) {
        view[0] = 5
    }
    return len(b)
}

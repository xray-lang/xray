import mem
import { allocZeroed } from "mem"

@c_export("xray_alloc_selective_score")
fn xray_alloc_selective_score(n: int) -> int {
    var b = allocZeroed(n)
    var view: Slice<byte> = b.asSpan()
    if (n > 0) {
        view[0] = 5
    }
    return len(b)
}

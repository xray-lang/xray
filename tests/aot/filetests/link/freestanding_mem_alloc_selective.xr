import mem
import { allocZeroed } from mem

fn xray_alloc_selective_score(n: i64) -> i64 {
    var b = allocZeroed(n)
    var view: Slice<u8> = b.asMutBytes()
    if (n > 0) {
        view[0] = 5
    }
    return len(b)
}

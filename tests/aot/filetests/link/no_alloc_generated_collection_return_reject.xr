import os

fn xray_no_alloc_os_loadavg_reject() -> int {
    return len(os.loadavg())
}

fn xray_no_alloc_os_environ_reject() -> int {
    return len(os.environ())
}

print(xray_no_alloc_os_loadavg_reject())
print(xray_no_alloc_os_environ_reject())

enum FsStatus {
    Ready,
    Done
}

fn xrayEnumToStringStatic(flag: int) -> int {
    var s = FsStatus.Ready
    if (flag != 0) {
        s = FsStatus.Done
    }
    print(s.toString())
    return s.ordinal
}

fn xrayPreludeEnumToStringStatic(flag: int) -> int {
    var e = Endian.LE
    if (flag != 0) {
        e = Endian.BE
    }
    print(e.toString())
    return e.ordinal
}

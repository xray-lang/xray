import mem

fn xray_no_alloc_buffer_resize_reject(b: ref Buffer, n: i64) -> bool {
    return b.resize(n)
}

fn xray_no_alloc_buffer_resize_entry(n: i64) -> bool {
    var b = mem.alloc(n)
    return xray_no_alloc_buffer_resize_reject(ref b, n + 1)
}

fn xray_map_reject() -> i64 {
    var m = #{"a": 1}
    return 0
}

fn xray_set_reject() -> i64 {
    var s = #[1, 2]
    return 0
}

fn xray_object_reject() -> i64 {
    var obj = { answer: 42 }
    return 0
}

fn xray_regex_reject() -> i64 {
    var re = /abc/
    return 0
}

fn xray_bigint_reject() -> i64 {
    var b = 123n
    return 0
}

fn xray_closure_reject() -> i64 {
    var f = fn() -> i64 { return 1 }
    return f()
}

fn xray_string_concat_reject() -> i64 {
    var s = "a" + "b"
    print(s)
    return 0
}

class HeapBacked {
    value: i64

    constructor(value: i64) {
        this.value = value
    }
}

fn xray_class_reject() -> i64 {
    var c = HeapBacked(1)
    return c.value
}

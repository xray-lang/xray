import regex

var xray_no_alloc_digits = regex.compile("\\d+")

fn xray_no_alloc_regex_find_text_reject(text: string) -> string? {
    return xray_no_alloc_digits.findText(text)
}

print(xray_no_alloc_regex_find_text_reject("abc123"))

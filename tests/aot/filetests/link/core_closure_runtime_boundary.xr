fn direct_score(delta: int) -> int {
    fn add(x: int) -> int {
        let acc = x
        let i = 0
        while (i < 4) {
            acc = acc + delta + i
            i = i + 1
        }
        return acc
    }

    return add(5)
}

fn cell_score() -> int {
    let n = 0

    fn inc(step: int) -> int {
        n = n + step
        return n
    }

    let a = inc(2)
    let b = inc(3)
    return a * 10 + b
}

print(direct_score(7))
print(cell_score())

fn xray_scalar_compare(a: f64, b: f64, x: i64, y: i64) -> i64 {
    var n = 0
    if (a < b) {
        n = n + 1
    }
    if (a <= b) {
        n = n + 2
    }
    if (x > y) {
        n = n + 4
    }
    if (x >= y) {
        n = n + 8
    }
    return n
}

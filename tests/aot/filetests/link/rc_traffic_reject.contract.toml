version = 1

[[function]]
symbol = "rcTrafficReject"
scope = "backend"
backend = "aot"
target = "native"
profile = "hosted"
requires = ["no_runtime_heap"]

[function.shape]
forbid = ["rc_traffic"]
allow = []

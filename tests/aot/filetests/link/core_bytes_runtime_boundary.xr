fn run_bytes_boundary() {
    var bytes = Array<byte>(8, 1)
    bytes[0] = 1
    bytes[1] = 2
    bytes[2] = 3
    bytes[3] = 4
    bytes[4] = 5
    bytes[5] = 6
    bytes[6] = 7
    bytes[7] = 8
    print(len(bytes))
    var view: Slice<byte> = bytes[:]
    var u16: uint16 = view.load<uint16>(0, Endian.LE)
    var u32: uint32 = view.load<uint32>(0, Endian.LE)
    var u64: uint64 = view.load<uint64>(0, Endian.LE)
    view.store<uint16>(6, 0x0a09, Endian.LE)
    print(int(u16))
    print(int(u32))
    print(int(u64))
    print(bytes[6])

    var dst = Array<byte>.withCapacity(6)
    print(len(dst))
    print(dst.capacity)
    dst.appendFrom(view[1:4])
    print(len(dst))
    print(dst[0])
    print(dst[2])

    var repeated = Array<byte>.withCapacity(9)
    var seed = Array<byte>(3)
    seed[0] = 65
    seed[1] = 66
    seed[2] = 67
    var seedView: Slice<byte> = seed[:]
    repeated.appendFrom(seedView)
    repeated.repeatFrom(3, 6)
    print(repeated[3])
    print(repeated[8])

    var filled = Array<byte>(5, 0)
    filled.fill(7, 1, 4)
    print(filled[0])
    print(filled[1])
    print(filled[3])
    print(filled[4])

    var filledView: Slice<byte> = filled[1:4]
    print(len(filledView))
    print(filledView[0])
}

run_bytes_boundary()

import mem

const TABLE: [int; 4] = comptime [2, 4, 6, 8]
const BYTES: [byte; 3] = comptime [11, 22, 33]

fn xray_fixed_array_ptr_addr() -> int {
    return mem.addr(unsafe { TABLE.ptr() }) + mem.addr(unsafe { BYTES.ptr() })
}

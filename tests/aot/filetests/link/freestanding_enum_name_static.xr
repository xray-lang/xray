enum FsState {
    Ready,
    Done
}

fn xrayEnumNameStatic(flag: int) -> int {
    var s = FsState.Ready
    if (flag != 0) {
        s = FsState.Done
    }
    print(s.name)
    return s.ordinal
}

fn xrayPreludeEnumNameStatic(flag: int) -> int {
    var e = Endian.LE
    if (flag != 0) {
        e = Endian.BE
    }
    print(e.name)
    return e.ordinal
}

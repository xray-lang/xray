fn xray_no_alloc_map_set_reject(scores: ref Map<i64, i64>, key: i64, value: i64) -> i64 {
    scores.set(key, value)
    return len(scores)
}

var scores: Map<i64, i64> = #{}
print(xray_no_alloc_map_set_reject(ref scores, 1, 10))

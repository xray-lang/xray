enum GateErr {
    Boom
}

fn typed_error(flag: bool) -> i64 {
    if (flag) {
        throw GateErr.Boom
    }
    return 1
}

fn panic_boundary(n: i64) -> i64 {
    try {
        return 10 / n
    } catch panic (p) {
        return -1
    }
}

print(typed_error(false) + panic_boundary(1))

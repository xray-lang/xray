struct Pair {
    left: i64
    right: i64
}

fn runtimeOnly() -> i64 {
    return 1
}

const PAIR = Pair{left: runtimeOnly(), right: 2}

fn xray_shared_aggregate_reject() -> i64 {
    return PAIR.left + PAIR.right
}

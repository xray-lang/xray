struct Pair {
    left: int
    right: int
}

fn runtimeOnly() -> int {
    return 1
}

const PAIR = Pair{left: runtimeOnly(), right: 2}

@c_export("xray_shared_aggregate_reject")
fn xray_shared_aggregate_reject() -> int {
    return PAIR.left + PAIR.right
}

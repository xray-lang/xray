var COUNTER: int = 1
var ENABLED: bool = true
var SCALE: float = 0.5
var LETTER: char = 'A'
var NOTHING = null
var LABEL: string = "boot"

@c_export("xray_top_var_static")
fn xray_top_var_static() -> int {
    COUNTER = COUNTER + 2
    if (ENABLED) {
        return COUNTER
    }
    return 0
}

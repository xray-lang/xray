fn consume(ch: Channel<Array<int>>, data: move Array<int>) {
    ch.send(move data)
}

fn main_value() -> int {
    const ch: Channel<Array<int>> = Channel(1)
    var data = Array<int>(2, 0)
    data[0] = 41
    data[1] = 1
    consume(ch, move data)
    return 42
}

print(main_value())

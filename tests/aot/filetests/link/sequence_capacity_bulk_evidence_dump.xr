fn touch(xs: ref Array<i64>, b: ref Array<u8>, s: string, span: Slice<u8>,
         sb: ref StringBuilder) -> i64 {
    xs.push(1)
    var first = xs[0]
    var part: Slice<i64> = xs[0:1]
    var n = len(xs)
    b.appendFrom(span)
    b.fill(0)
    var text = b.toString()
    var decoded = string.fromUtf8(b[:])
    var bytes = s.copyBytes()
    sb.append(text)
    sb.append(decoded)
    var built = sb.toString()
    return n + first + len(built)
}

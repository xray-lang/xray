import { Semaphore, CountdownLatch, EventCount, WorkQueue, ResultGroup } from sync
shared latch = CountdownLatch(1)
shared counter = Atomic(0)

go fn() {
    counter.add(7)
    latch.done()
}()

print(latch.wait())
print(counter.load())
print(latch.remaining)

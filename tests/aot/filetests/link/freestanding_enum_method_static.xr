enum FsMode {
    Boot,
    Run,
    Halt

    fn code(offset: i64) -> i64 {
        return this.ordinal + offset
    }

    static fn base() -> i64 {
        return 40
    }
}

fn xray_enum_method_static(flag: i64) -> i64 {
    var mode = FsMode.Boot
    if (flag == 1) {
        mode = FsMode.Run
    }
    if (flag == 2) {
        mode = FsMode.Halt
    }
    return mode.code(10) + FsMode.base()
}

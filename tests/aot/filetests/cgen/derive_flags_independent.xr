@derive(Inspect)
class InspectOnly {
    value: int

    constructor(value: int) {
        this.value = value
    }
}

@derive(JSON)
class JsonOnly {
    value: int

    constructor(value: int) {
        this.value = value
    }
}

var inspect_only = InspectOnly(7)
print(inspect_only)

var json_only = JsonOnly(9)
var encoded = JSON.value(json_only)
print(JSON.require<int>(encoded, ["value"]))
print(json_only)

type User = { name: string, age: i64 }

class Holder {
    user: User

    constructor(user: User) {
        this.user = user
    }

    objectFieldReceiverPlan() -> i64 {
        return this.user.age
    }
}

fn mainValue() -> i64 {
    var user: User = { name: "ada", age: 7 }
    return Holder(user).objectFieldReceiverPlan()
}

print(mainValue())

type User = { name: string, age: int }

fn makeRecordIndirectReturnPlan() -> User {
    var user: User = { name: "ada", age: 7 }
    return user
}

fn recordIndirectReturnReceiverPlan() -> int {
    return makeRecordIndirectReturnPlan().age
}

print(recordIndirectReturnReceiverPlan())

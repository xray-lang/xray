type JsonBag = { items: Array<JSON.Value> }

fn main() -> int {
    var data = JSON.parseValue("{\"items\":[1,true,null]}")
    var bag = JSON.decode<JsonBag>(data)
    if (bag == null) {
        return -1
    }
    return len(bag!.items)
}

print(main())

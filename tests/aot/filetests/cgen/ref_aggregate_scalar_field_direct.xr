fn bumpPair(left: ref i64, right: ref i64) {
    left += 1
    right += 2
}

struct Counter {
    left: i64
    right: i64

    advance() {
        bumpPair(ref this.left, ref this.right)
    }
}

var counter = Counter{left: 3, right: 5}
counter.advance()
print(counter.left + counter.right)

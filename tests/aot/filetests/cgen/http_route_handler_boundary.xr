import http
import { HttpRequest, HttpResponse } from http

fn handle(req: HttpRequest) -> HttpResponse {
    return http.textResponse(req.path)
}

http.routeHandler("GET", "/hello", handle)
print("registered")

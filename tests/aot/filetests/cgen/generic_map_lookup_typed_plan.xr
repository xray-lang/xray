fn lookup<K: Hashable, V>(items: Map<K, V>, key: K, fallback: V) -> V {
    return items.get(key) ?? fallback
}

fn run(n: int) -> int {
    var values: Map<int, int> = #{}
    var seed = 0
    while (seed < 64) {
        values.set(seed, (seed % 17) + 1)
        seed = seed + 1
    }

    var i = 0
    var sum = 0
    while (i < n) {
        sum = sum + lookup<int, int>(values, i % 64, 0)
        i = i + 1
    }
    return sum
}

print(run(30000))

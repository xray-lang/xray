type User = { name: string, age: int }

fn objectParamReceiverPlan(user: User) -> int {
    return user.age
}

fn mainValue() -> int {
    var user: User = { name: "ada", age: 7 }
    return objectParamReceiverPlan(user)
}

print(mainValue())

type Visitor = CFn<(int, int) -> int>

var visits = 0

fn visit(left: int, right: int) -> int {
    visits += 1
    return left + right
}

fn forEach(left: int, visitor: Visitor, right: int) -> int {
    var result = visitor(left, right)
    print(result)
    return result
}

print(forEach(20, visit, 22))
print(visits)

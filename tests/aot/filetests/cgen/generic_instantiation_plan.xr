fn id<T>(x: T, keep: bool) -> T {
    if (keep) {
        return x
    }
    return x
}

fn wrapper(x: int) -> int {
    if (x < 0) {
        return x
    }
    return id<int>(x, true)
}

print(wrapper(41))

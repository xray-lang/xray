interface Shape {
    area() -> i64
}

class Circle implements Shape {
    r: i64

    constructor(r: i64) {
        this.r = r
    }

    area() -> i64 {
        return this.r * this.r
    }
}

fn score(shape: Shape) -> i64 {
    return shape.area()
}

print(score(Circle(7)))

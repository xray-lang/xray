import { U64x2 } from simd

fn sumPair(values: Slice<u64>) -> u64 {
    var loaded = U64x2.load(values)
    var biased = loaded.add(U64x2.splat(7))
    return biased.reduceAdd()
}

var values: [u64; 2] = [11, 13]
print(sumPair(values[:]))

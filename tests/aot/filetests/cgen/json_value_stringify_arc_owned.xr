fn run(n: i64) -> i64 {
    var user = { name: "Ada", age: 37, active: true }
    var i = 0
    var total = 0
    while (i < n) {
        var text = JSON.stringify(JSON.value(user))
        total = total + len(text)
        i = i + 1
    }
    return total
}

print(run(10))

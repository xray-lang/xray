import { Plan, Options } from parallel

var plan = Plan<int>(Options(2), (lane) -> lane + 10)

var mapped = plan.map(0..4, (state, i) -> state + i)

var out: Array<int> = []
var same = plan.mapInto(0..4, out, (state, i) -> state * 10 + i)

print(mapped[0])
print(same[3])

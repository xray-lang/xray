enum PairResult {
    Pair(i64, i64),
    Empty
}

fn make_pair() -> PairResult {
    return PairResult.Pair(10, 32)
}

fn score() -> i64 {
    var result = make_pair()
    match (result) {
        PairResult.Pair(left, right) -> {
            return left + right
        }
        PairResult.Empty -> {
            return 0
        }
    }
}

print(score())

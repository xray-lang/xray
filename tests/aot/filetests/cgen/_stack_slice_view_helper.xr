export fn sumWindow(view: ByteSpan) -> int {
    var total = 0
    var i = 0
    while (i < view.length) {
        total = total + int(view[i])
        i = i + 1
    }
    return total + view.length
}

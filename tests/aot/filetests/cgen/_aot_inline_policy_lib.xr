fn small(x: int) -> int {
    return x + 1
}

fn looping(n: int) -> int {
    var acc = 0
    var i = 0
    while (i < n) {
        acc = acc + small(i)
        i = i + 1
    }
    return acc
}

export fn runPolicy(n: int) -> int {
    return looping(n) + small(n)
}

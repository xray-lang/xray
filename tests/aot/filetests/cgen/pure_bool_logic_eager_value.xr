fn hotGuard(a: int, b: int, c: int, d: int, e: int, f: int) -> int {
    if (a < b && c + 16 <= d && e + 32 <= f) {
        return 1
    }
    return 0
}

print(hotGuard(1, 2, 8, 24, 16, 64))
print(hotGuard(3, 2, 8, 24, 16, 64))

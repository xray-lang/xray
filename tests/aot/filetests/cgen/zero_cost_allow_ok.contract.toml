version = 1

[[function]]
symbol = "zeroCostAllowBounds"
scope = "backend"
backend = "aot"
target = "native"
profile = "hosted"
requires = ["no_runtime_heap"]

[function.shape]
forbid = ["runtime_dispatch", "runtime_heap", "pending_error", "box", "lane_spill"]
allow = ["bounds_in_loop"]

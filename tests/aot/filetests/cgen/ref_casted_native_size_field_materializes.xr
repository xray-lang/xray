fn bump(value: ref i64) {
    value += 3
}

struct Counter {
    stripes: usize
    total: u64

    advanced() -> i64 {
        var stripes = this.stripes as i64
        bump(ref stripes)
        return stripes
    }
}

var counter = Counter{stripes: 4, total: 241}
print(counter.advanced())

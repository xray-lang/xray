type User = { name: string, age: int }

fn recordParamReceiverPlan(user: User) -> int {
    return user.age
}

fn mainValue() -> int {
    var user: User = { name: "ada", age: 7 }
    return recordParamReceiverPlan(user)
}

print(mainValue())

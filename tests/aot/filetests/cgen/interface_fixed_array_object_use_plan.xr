interface Drawable {
    draw() -> i64
}

class Sprite implements Drawable {
    draw() -> i64 { return 1 }
}

class Icon implements Drawable {
    draw() -> i64 { return 2 }
}

fn keep(items: [Drawable; 2]) -> Drawable {
    return items[0]
}

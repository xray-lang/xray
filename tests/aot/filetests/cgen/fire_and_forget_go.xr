import { Semaphore, CountdownLatch, EventCount, WorkQueue, ResultGroup } from sync
const results: ResultGroup = ResultGroup(1)

fn worker() {
    results.add(42)
    results.close()
}

go worker()
var value = results.recv()
print(value ?? -1)

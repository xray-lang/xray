type User = { name: string, age: i64 }

fn objectContainerReceiverPlan(users: Array<User>) -> i64 {
    return users[0].age
}

fn mainValue() -> i64 {
    var users: Array<User> = []
    users.push({ name: "ada", age: 7 })
    return objectContainerReceiverPlan(users)
}

print(mainValue())

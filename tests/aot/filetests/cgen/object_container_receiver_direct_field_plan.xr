type User = { name: string, age: int }

fn objectContainerReceiverPlan(users: Array<User>) -> int {
    return users[0].age
}

fn mainValue() -> int {
    var users: Array<User> = []
    users.push({ name: "ada", age: 7 })
    return objectContainerReceiverPlan(users)
}

print(mainValue())

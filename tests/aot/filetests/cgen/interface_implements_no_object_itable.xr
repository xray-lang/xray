interface Shape {
    area() -> int
}

class Circle implements Shape {
    r: int

    constructor(r: int) {
        this.r = r
    }

    area() -> int {
        return this.r * this.r
    }
}

fn run() -> int {
    var circle = Circle(7)
    return circle.area()
}

print(run())

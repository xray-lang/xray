fn patchFirst(outputs: ref Array<Array<byte>>) {
    unsafe {
        var block = outputs.get(0)
        block.set(0, 0x2A)
    }
}

var block: Array<byte> = Array.withCapacity(1)
unsafe {
    block.push(0)
}
var outputs: Array<Array<byte>> = []
outputs.push(block)
patchFirst(ref outputs)
print(outputs[0][0])

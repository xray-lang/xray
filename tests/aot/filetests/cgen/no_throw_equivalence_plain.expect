c_contains=int64_t xray_add_one(int64_t p0)
# Pin the wrapping-add shape, not the operand's value number: a folded
# constant operand is emitted inline rather than as its own SSA value.
c_regex=\(uint64_t\)\(v[0-9]+\) \+ \(uint64_t\)\((v[0-9]+|INT64_C\([0-9]+\))\)
c_count=1:xrt_has_pending_error()

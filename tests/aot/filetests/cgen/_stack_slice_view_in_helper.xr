export fn sumWindowIn(view: in Slice<byte>) -> int {
    var total = 0
    var i = 0
    while (i < len(view)) {
        total = total + int(view[i])
        i = i + 1
    }
    return total + len(view)
}

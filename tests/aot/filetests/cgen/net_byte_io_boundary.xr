import net

fn exchange(conn: NetConn) -> i64 {
    var buffer = Array<u8>(32)
    var read = net.readInto(conn, ref buffer, 16)
    if (read <= 0) {
        return read
    }
    return net.writeBytes(conn, buffer)
}

print(typeName(exchange))

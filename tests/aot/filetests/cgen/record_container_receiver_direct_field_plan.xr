type User = { name: string, age: int }

fn recordContainerReceiverPlan(users: Array<User>) -> int {
    return users[0].age
}

fn mainValue() -> int {
    var users: Array<User> = []
    users.push({ name: "ada", age: 7 })
    return recordContainerReceiverPlan(users)
}

print(mainValue())

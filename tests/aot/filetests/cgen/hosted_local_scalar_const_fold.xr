const CAPACITY: i64 = 256

fn remaining(used: i64) -> i64 {
    // Keep a separate codegen boundary so this tests GET_SHARED emission, not
    // caller-side constant propagation.
    var scratch: [u8; 128] = [0; 128]
    return CAPACITY - used + (scratch[0] as i64)
}

print(remaining(1))

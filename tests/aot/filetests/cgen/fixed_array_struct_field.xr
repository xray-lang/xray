struct Buf {
    data: [byte; 4]
    bias: int
}

var buf = Buf{data: [1, 2, 3, 4], bias: 5}

fn run(n: int) -> int {
    var i = 0
    while (i < n) {
        buf.data[0] = i as byte
        buf.data[1] = buf.data[0]
        i = i + 1
    }
    return buf.data[1] as int
}

print(run(10))

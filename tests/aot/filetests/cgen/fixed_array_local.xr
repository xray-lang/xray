fn first(key: [byte; 4]) -> int {
    return key[0] as int
}

fn at(key: [byte; 4], i: int) -> int {
    return key[i] as int
}

fn run() -> int {
    var key: [byte; 4] = [1, 2, 3, 4]
    key[1] = 9
    return len(key) + first(key) + (key[1] as int) + at(key, 2)
}

print(run())

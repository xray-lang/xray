fn first(key: [uint8; 4]) -> int {
    return key[0]
}

fn at(key: [uint8; 4], i: int) -> int {
    return key[i]
}

fn run() -> int {
    var key: [uint8; 4] = [1, 2, 3, 4]
    key[1] = 9
    return key.length + first(key) + key[1] + at(key, 2)
}

print(run())

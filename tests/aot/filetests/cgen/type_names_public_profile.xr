export class PublicBox {
    value: i64

    constructor(value: i64) {
        this.value = value
    }
}

class PrivateBox {
    value: i64

    constructor(value: i64) {
        this.value = value
    }
}

var a = PublicBox(1)
var b = PrivateBox(2)
print(a.value + b.value)

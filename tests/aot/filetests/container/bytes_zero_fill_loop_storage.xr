fn makeBytes(n: int) -> Bytes {
    let out = Bytes(0)
    let i = 0
    while (i < n) {
        out.push(i & 0xFF)
        i = i + 1
    }
    return out
}

let data = makeBytes(16)
print(data.length)
print(data[7])

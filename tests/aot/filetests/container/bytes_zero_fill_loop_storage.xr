fn makeBytes(n: int) -> Bytes {
    var out = Bytes(0)
    var i = 0
    while (i < n) {
        out.push(i & 0xFF)
        i = i + 1
    }
    return out
}

var data = makeBytes(16)
print(data.length)
print(data[7])

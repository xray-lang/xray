fn pair(src: ByteSpan) -> int {
    unsafe {
        return int(src.get(1)) + int(src.get(2))
    }
}

var bytes = Bytes.withCapacity(4)
unsafe {
    bytes.push(3)
    bytes.push(7)
    bytes.push(11)
}
var view: ByteSpan = bytes[:]
print(pair(view))

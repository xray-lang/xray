fn common(src: ByteSpan, left: int, right: int, maxCount: int) -> int {
    var a: ByteSpan = src[left:left + maxCount]
    var b: ByteSpan = src[right:right + maxCount]
    return a.commonPrefix(b)
}

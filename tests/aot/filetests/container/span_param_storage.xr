fn sum_bytes(src: Slice<u8>) -> i64 {
    var total = 0
    var i = 0
    while (i < len(src)) {
        total = total + (src[i] as i64)
        i = i + 1
    }
    return total
}

var bytes = Array<u8>(4)
bytes[0] = 1
bytes[1] = 2
bytes[2] = 3
bytes[3] = 4

print(sum_bytes(bytes[1:4]))

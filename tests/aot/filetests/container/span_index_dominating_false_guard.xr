fn guardedRead(bytes: Slice<byte>, index: int) -> int {
    if (index < 0) {
        return 0
    }
    if (index >= len(bytes)) {
        return 0
    }
    return bytes[index] as int
}

var bytes: [byte; 3] = [7, 11, 13]
print(guardedRead(bytes[:], 1))

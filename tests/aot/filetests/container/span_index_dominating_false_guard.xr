fn guardedRead(bytes: Slice<u8>, index: i64) -> i64 {
    if (index < 0) {
        return 0
    }
    if (index >= len(bytes)) {
        return 0
    }
    return bytes[index] as i64
}

var bytes: [u8; 3] = [7, 11, 13]
print(guardedRead(bytes[:], 1))

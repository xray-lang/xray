fn makeByteArray(n: i64) -> Array<u8> {
    var out = Array<u8>(0)
    var i = 0
    while (i < n) {
        out.push((i & 0xFF) as u8)
        i = i + 1
    }
    return out
}

var data = makeByteArray(16)
print(len(data))
print(data[7])

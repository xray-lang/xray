fn makeByteArray(n: int) -> Array<byte> {
    var out = Array<byte>(0)
    var i = 0
    while (i < n) {
        out.push(i & 0xFF)
        i = i + 1
    }
    return out
}

var data = makeByteArray(16)
print(len(data))
print(data[7])

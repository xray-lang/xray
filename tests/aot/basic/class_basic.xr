// AOT basic test: class with fields and methods
class Point {
    x: i64
    y: i64

    constructor(x: i64, y: i64) {
        this.x = x
        this.y = y
    }

    sum() -> i64 {
        return this.x + this.y
    }
}

var p = Point(3, 4)
print(p.sum())  // 7
print(p.x)      // 3

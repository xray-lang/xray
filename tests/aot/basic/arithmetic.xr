// AOT basic test: typed arithmetic
fn add(a: int, b: int): int {
    return a + b
}

fn mul(a: float, b: float): float {
    return a * b
}

fn fib(n: int): int {
    if (n <= 1) {
        return n
    }
    return fib(n - 1) + fib(n - 2)
}

print(add(3, 4))
print(mul(2.5, 4.0))
print(fib(10))

// AOT basic test: shared variables (GETSHARED/SETSHARED)
var counter = 0

fn increment() -> i64 {
    counter = counter + 1
    return counter
}

fn get_counter() -> i64 {
    return counter
}

increment()
increment()
increment()
print(get_counter())

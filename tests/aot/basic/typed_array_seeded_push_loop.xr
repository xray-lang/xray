fn copyRepeated(matchLen: int) -> Array<byte> {
    var out: Array<byte> = []
    out.push(65)
    var matchPos = 0
    var i = 0
    while (i < matchLen) {
        out.push(out[matchPos + i])
        i = i + 1
    }
    return out
}

var copied = copyRepeated(63)
print(len(copied))
print(copied[0])
print(copied[63])

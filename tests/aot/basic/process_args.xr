// AOT test: standalone process.args is available in synchronous programs.

print(len(process.args))
if (len(process.args) > 0) {
    print(process.args[0])
} else {
    print("none")
}
if (len(process.args) > 1) {
    print(process.args[1])
} else {
    print("none")
}

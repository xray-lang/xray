fn makeBytes(n: int) -> Bytes {
    var out = Bytes(0)
    var i = 0
    while (i < n) {
        out.push(i & 0xFF)
        i = i + 1
    }
    return out
}

fn appendToSized(n: int) -> Bytes {
    var out = Bytes(3)
    out[0] = 100
    out[1] = 101
    out[2] = 102
    var i = 0
    while (i < n) {
        out.push(i & 0xFF)
        i = i + 1
    }
    return out
}

var a = makeBytes(16)
print(a.length)
print(a[7])

var b = appendToSized(5)
print(b.length)
print(b[0])
print(b[3])
print(b[7])

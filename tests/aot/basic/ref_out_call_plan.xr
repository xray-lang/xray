fn bump(value: ref i64) -> i64 {
    value = value + 1
    return value
}

fn fill(value: ref i64) -> i64 {
    value = 9
    return value
}

fn invokeBump(action: fn(ref i64) -> i64, value: ref i64) -> i64 {
    return action(ref value)
}

fn invokeFill(action: fn(ref i64) -> i64, value: ref i64) -> i64 {
    return action(ref value)
}

var value = 1
print(bump(ref value))
print(value)

var slot = 0
print(fill(ref slot))
print(slot)

var indirect = 4
print(invokeBump(bump, ref indirect))
print(indirect)

var indirectSlot = 0
print(invokeFill(fill, ref indirectSlot))
print(indirectSlot)

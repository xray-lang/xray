fn bump(value: ref int) -> int {
    value = value + 1
    return value
}

fn fill(value: ref int) -> int {
    value = 9
    return value
}

fn invokeBump(action: fn(ref int) -> int, value: ref int) -> int {
    return action(ref value)
}

fn invokeFill(action: fn(ref int) -> int, value: ref int) -> int {
    return action(ref value)
}

var value = 1
print(bump(ref value))
print(value)

var slot = 0
print(fill(ref slot))
print(slot)

var indirect = 4
print(invokeBump(bump, ref indirect))
print(indirect)

var indirectSlot = 0
print(invokeFill(fill, ref indirectSlot))
print(indirectSlot)

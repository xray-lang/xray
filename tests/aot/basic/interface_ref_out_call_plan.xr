interface Mutator {
    bump(value: ref int) -> int
    fill(value: ref int) -> int
}

class Stepper implements Mutator {
    step: int

    constructor(step: int) {
        this.step = step
    }

    bump(value: ref int) -> int {
        value = value + this.step
        return value
    }

    fill(value: ref int) -> int {
        value = this.step * 10
        return value
    }
}

class Doubler implements Mutator {
    constructor() {}

    bump(value: ref int) -> int {
        value = value * 2
        return value
    }

    fill(value: ref int) -> int {
        value = 40
        return value
    }
}

fn applyBump(mutator: ref Mutator, value: ref int) -> int {
    return mutator.bump(ref value)
}

fn applyFill(mutator: ref Mutator, value: ref int) -> int {
    return mutator.fill(ref value)
}

var selected: Mutator = Stepper(3)
var value = 4
print(applyBump(ref selected, ref value))
print(value)

selected = Doubler()
print(applyBump(ref selected, ref value))
print(value)

var output: int
print(applyFill(ref selected, ref output))
print(output)

fn pair(src: ByteSpan) -> int {
    unsafe {
        return int(src.getUnchecked(1)) + int(src.getUnchecked(2))
    }
}

var bytes = Bytes.withCapacity(4)
unsafe {
    bytes.push(3)
    bytes.push(7)
    bytes.push(11)
}
var view: ByteSpan = bytes[:]
print(pair(view))

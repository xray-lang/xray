[project]
name = "freestanding-coro-provider"
main = "main.xr"

[target.native]
profile = "freestanding"
runtime_provider = "test-kernel-executor-v1"
runtime_capabilities = ["coroutine", "objects", "task"]
runtime_hooks = ["task_alloc", "submit", "park_wake", "executor_pump"]

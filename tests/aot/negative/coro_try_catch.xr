enum AppErr {
    Bad(code: int)
}

fn worker() -> int {
    var result = 0
    try {
        Coro.yield()
        throw AppErr.Bad(7)
    } catch (e) {
        result = 42
    }
    return result
}

var task = go worker()
print(await task)

fn captured_string(s: string) -> string {
    fn child(value: move string) -> string {
        return value + "!"
    }

    return (await go child(copy(s))) ?? "failed"
}

print(await go captured_string("capture"))

fn wait_recv(ch: Channel<int>) -> int {
    var result = 0
    select {
        value from ch -> {
            result = value + 1
        }
    }
    return result
}

shared ch = Channel<int>(1)
ch.send(41)
var task = go wait_recv(ch)

var result = await task
print(result)
ch.close()

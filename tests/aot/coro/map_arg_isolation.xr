fn worker(m: Map<string, Array<int>>) -> int {
    let xs = m.get("items")
    xs.push(99)
    yield
    return xs.length
}

let m: Map<string, Array<int>> = #{"items": [1, 2]}
let task = go worker(copy(m))
let child_len = await task
print(child_len)
print(m.get("items").length)

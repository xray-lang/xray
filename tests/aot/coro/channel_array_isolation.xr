fn worker(ch: Channel<Array<int>>) -> int {
    let xs = ch.recv()
    xs.push(99)
    yield
    return xs.length
}

let ch = new Channel<Array<int>>(1)
let xs = [1, 2]
ch.send(xs)
let task = go worker(ch)
let child_len = await task
print(child_len)
print(xs.length)
ch.close()

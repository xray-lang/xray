fn worker(n: i64) -> i64 {
    return n + 1
}

fn run_many(iter: i64) -> i64 {
    var sum = 0
    for (var i = 0; i < iter; i++) {
        var task = go worker(i)
        sum = sum + await task
    }
    return sum
}

print(run_many(1000))
print(run_many(1000))

fn worker(n: int) -> int {
    return n + 1
}

fn run_many(iter: int) -> int {
    var sum = 0
    for (var i = 0; i < iter; i++) {
        var task = go worker(i)
        sum = sum + await task
    }
    return sum
}

print(run_many(1000))
print(run_many(1000))

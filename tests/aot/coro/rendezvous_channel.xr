fn sender(ch: Channel<int>) -> int {
    ch.send(41)
    return 1
}

const ch = Channel<int>(0)
const task = go sender(ch)
var v = ch.recv()
var done = await task
print(match (v) {
    Recv.Value(value) -> value + done
    _ -> -1
})
ch.close()

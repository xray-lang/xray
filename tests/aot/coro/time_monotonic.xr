import time

fn monotonic_ok() -> bool {
    var before = time.monotonic()
    Coro.yield()
    var after = time.monotonic()
    return after >= before
}

var task = go monotonic_ok()
print(await task)

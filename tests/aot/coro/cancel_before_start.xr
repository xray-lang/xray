import time

fn sleeper() -> int {
    time.sleep(1000)
    return 7
}

var task = go sleeper()
task.cancel()
var value = task.awaitResult()
print(match (value) {
    TaskResult.Cancelled -> true
    _ -> false
})
print(task.done)
print(task.status)

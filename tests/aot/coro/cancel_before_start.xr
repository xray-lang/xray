import time

fn sleeper() -> int {
    time.sleep(1000)
    return 7
}

let task = go sleeper()
task.cancel()
let value = task.awaitResult()
print(match (value) {
    TaskResult.Cancelled -> true
    _ -> false
})
print(task.done)
print(task.status)

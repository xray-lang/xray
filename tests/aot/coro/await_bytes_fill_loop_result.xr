fn makeSample(n: int) -> Bytes {
    var out = Bytes(0)
    var i = 0
    while (i < n) {
        out.push((i * 3) & 0xFF)
        i = i + 1
    }
    return out
}

fn worker(n: int) -> Bytes {
    return makeSample(n)
}

var t = go worker(1024)
var data = await t
print(data.length)
print(data[7])

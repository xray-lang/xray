export struct InnerValue {
    value: u64
    other: u64
}

export struct OuterValue {
    inner: InnerValue
    marker: u64
}

export fn makeOuter(value: u64) -> OuterValue {
    return OuterValue{
        inner: InnerValue{value: value, other: value + 1},
        marker: value + 2,
    }
}

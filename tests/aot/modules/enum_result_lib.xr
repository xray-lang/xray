// AOT module test library: exported generic enum aggregate result.

export enum FastResult<T> {
    Ok { value: T },
    Err { code: i64 }
}

export fn pick(ok: bool) -> FastResult<i64> {
    if (ok) {
        return FastResult.Ok { value: 41 }
    }
    return FastResult.Err { code: 9 }
}

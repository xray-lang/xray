export fn suspend_plus(x: i64) -> i64 {
    Coro.yield()
    return x + 1
}

export fn recv_once(ch: Channel<i64>) -> i64 {
    return match (ch.recv()) {
        Recv.Value { value: v } -> v
        _ -> -1
    }
}

export fn sync_plus(x: i64) -> i64 {
    return x + 2
}

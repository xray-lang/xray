export fn suspend_plus(x: int) -> int {
    yield
    return x + 1
}

export fn recv_once(ch: Channel<int>) -> int {
    return match (ch.recv()) {
        Recv.Value(v) -> v
        _ -> -1
    }
}

export fn sync_plus(x: int) -> int {
    return x + 2
}

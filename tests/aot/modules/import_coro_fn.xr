import { suspend_plus, recv_once, sync_plus } from "./coro_lib"

let ch = new Channel<int>(1)
ch.send(20)

print(suspend_plus(40))
print(recv_once(ch) + 1)

let suspended = go suspend_plus(50)
print(await suspended)

let synced = go sync_plus(60)
print(await synced)

ch.close()

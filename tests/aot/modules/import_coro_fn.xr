import { suspend_plus, recv_once, sync_plus } from "./coro_lib"

const ch = Channel<int>(1)
ch.send(20)

print(suspend_plus(40))
print(recv_once(ch) + 1)

var suspended = go suspend_plus(50)
print(await suspended)

var synced = go sync_plus(60)
print(await synced)

ch.close()

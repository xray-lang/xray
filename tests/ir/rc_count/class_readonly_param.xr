class Box {
    value: int

    constructor(value: int) {
        this.value = value
    }

    get() -> int {
        return this.value
    }
}

fn readBox(box: Box) -> int {
    return box.get()
}

let box = new Box(7)
print(readBox(box))

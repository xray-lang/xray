class Box<T> {
    value: T

    constructor(value: T) {
        this.value = value
    }

    touch(value: ref T) {
        assertEqual(value, this.value)
    }
}

@test
fn test_generic_method_keeps_ref_contract() {
    var box = Box<i64>(7)
    var value = 7
    box.touch(ref value)
}

fn bump(value: ref int) -> int {
    return value + 1
}

fn fill(value: ref int) -> int {
    value = 7
    return value
}

fn observe(values: ref Array<int>) -> int {
    return len(values)
}

@test
fn test_writable_places_accept_ref() {
    var value = 1
    assert_eq(bump(ref value), 2)

    var slot = 0
    assert_eq(fill(ref slot), 7)
    assert_eq(slot, 7)

    var values = Array<int>(0)
    assert_eq(observe(ref values), 0)
}

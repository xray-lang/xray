fn plus_one(x: i64) -> i64 {
    return x + 1
}

@test
fn test_out_identifier_call_argument_not_marker() {
    var out = 41
    assertEqual(plus_one(out), 42)
    assertEqual(plus_one(out + 1), 43)
}

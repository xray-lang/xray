class Holder {
    items: Array<i64>

    constructor() {
        this.items = []
    }
}

fn stash(data: Array<i64>, holder: ref Holder) {
    var alias = data
    holder.items = alias
}

var values: Array<i64> = [1, 2]
var holder = Holder()
stash(values, ref holder)

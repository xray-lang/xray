class Holder {
    items: Array<i64>

    constructor() {
        this.items = [1, 2]
    }
}

fn leak(holder: Holder) -> Array<i64> {
    return holder.items
}

var holder = Holder()
var leaked = leak(holder)
print(len(leaked))

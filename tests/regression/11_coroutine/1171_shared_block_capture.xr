@test
fn test_shared_block_capture() {
    const payload = Atomic<int>(3)
    var t = go fn() -> int {
        payload.fetchAdd(1)
        return payload.load()
    }()

    assert_eq(await t, 4)
    assert_eq(payload.load(), 4)
}

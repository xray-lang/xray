import text

// 0120_string_basic.xr
// 作者：xingleixu@gmail.com
// 测试：字符串字面量基础

// ===== 正常路径 =====

@test
fn test_simple_string() {
    var s = "hello"
    assertEqual(s, "hello")
}

@test
fn test_empty_string() {
    var s = ""
    assertEqual(s, "")
    assertEqual(len(s), 0)
}

@test
fn test_single_char() {
    var s = "x"
    assertEqual(s, "x")
    assertEqual(len(s), 1)
}

@test
fn test_spaces() {
    var s = "hello world"
    assertEqual(s, "hello world")
    assertEqual(len(s), 11)
}

// ===== 转义序列 =====

@test
fn test_escape_newline() {
    var s = "line1\nline2"
    assertEqual(s.contains("\n"), true)
}

@test
fn test_escape_tab() {
    var s = "col1\tcol2"
    assertEqual(s.contains("\t"), true)
}

@test
fn test_escape_quote() {
    var s = "say \"hello\""
    assertEqual(s.contains("\""), true)
}

@test
fn test_escape_backslash() {
    var s = "path\\to\\file"
    assertEqual(s.contains("\\"), true)
}

// ===== Unicode =====

@test
fn test_unicode_chinese() {
    var s = "你好世界"
    assertEqual(s, "你好世界")
    assertEqual(len(s), 4)
}

@test
fn test_unicode_emoji() {
    var s = "hello 🌍"
    assertEqual(s.contains("🌍"), true)
}

@test
fn test_unicode_mixed() {
    var s = "Hello 世界 🎉"
    assertEqual(len(s) > 5, true)
}

// ===== 模板字符串 =====

@test
fn test_template_basic() {
    var name = "Alice"
    var s = "Hello, ${name}!"
    assertEqual(s, "Hello, Alice!")
}

@test
fn test_template_expression() {
    var a = 10
    var b = 20
    var s = "Sum: ${a + b}"
    assertEqual(s, "Sum: 30")
}

@test
fn test_template_nested() {
    var x = 5
    var s = "Value: ${x * 2} (doubled)"
    assertEqual(s.contains("10"), true)
}

// ===== 边界条件 =====

@test
fn test_long_string() {
    var s = "a".repeat(1000)
    assertEqual(len(s), 1000)
}

@test
fn test_whitespace_only() {
    var s = "   "
    assertEqual(len(s), 3)
    assertEqual(text.trim(s), "")
}


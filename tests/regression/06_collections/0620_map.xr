// 0620_map.xr
// 作者：xingleixu@gmail.com
// 测试：Map 集合
// ===== 创建 =====
@test
fn test_map_literal() {
    var m = #{"a": 1, "b": 2, "c": 3}
    assert_eq(len(m), 3)
}

@test
fn test_empty_map() {
    var m: Map<string, i64> = #{}
    assert_eq(len(m), 0)
}

@test
fn test_single_entry() {
    var m = #{"key": "value"}
    assert_eq(len(m), 1)
}

// ===== 获取 =====
@test
fn test_get_basic() {
    var m = #{"name": "Alice", "age": 30}
    assert_eq(m.get("name"), "Alice")
    assert_eq(m.get("age"), 30)
}

@test
fn test_get_missing() {
    var m = #{"a": 1}
    assert_eq(m.get("b"), null)
}

@test
fn test_bracket_access() {
    var m = #{"x": 10, "y": 20}
    assert_eq(m["x"], 10)
    assert_eq(m["y"], 20)
}

// ===== 设置 =====
@test
fn test_set_new() {
    var m: Map<string, string> = #{}
    m.set("key", "value")
    assert_eq(m.get("key"), "value")
    assert_eq(len(m), 1)
}

@test
fn test_set_overwrite() {
    var m = #{"key": "old"}
    m.set("key", "new")
    assert_eq(m.get("key"), "new")
    assert_eq(len(m), 1)
}

@test
fn test_bracket_assign() {
    var m: Map<string, i64> = #{}
    m["x"] = 100
    assert_eq(m["x"], 100)
}

// ===== 检查 =====
@test
fn test_has_key() {
    var m = #{"a": 1, "b": 2}
    assert_eq(m.containsKey("a"), true)
    assert_eq(m.containsKey("c"), false)
}

// ===== 删除 =====
@test
fn test_delete() {
    var m = #{"a": 1, "b": 2, "c": 3}
    m.delete("b")
    assert_eq(len(m), 2)
    assert_eq(m.containsKey("b"), false)
    assert_eq(m.containsKey("a"), true)
    assert_eq(m.containsKey("c"), true)
}

@test
fn test_delete_missing() {
    var m = #{"a": 1}
    m.delete("b")
    // 不存在的键
    assert_eq(len(m), 1)
}

// ===== 遍历 =====
@test
fn test_iterate_values() {
    var m = #{"a": 1, "b": 2, "c": 3}
    var sum = 0
    for (k, v in m) {
        sum += v
    }
    assert_eq(sum, 6)
}

@test
fn test_collect_keys() {
    var m = #{"x": 10, "y": 20, "z": 30}
    var keys: Array<string> = []
    for (k, v in m) {
        keys.push(k)
    }
    assert_eq(len(keys), 3)
}

// ===== keys/values =====
@test
fn test_keys() {
    var m = #{"a": 1, "b": 2}
    var keys = m.keys()
    assert_eq(len(keys), 2)
}

@test
fn test_values() {
    var m = #{"a": 1, "b": 2}
    var values = m.values()
    assert_eq(len(values), 2)
}

// ===== 清空 =====
@test
fn test_clear() {
    var m = #{"a": 1, "b": 2, "c": 3}
    m.clear()
    assert_eq(len(m), 0)
}

// ===== 不同类型的键值 =====
@test
fn test_string_keys() {
    var m = #{"hello": 1, "world": 2}
    assert_eq(m.get("hello"), 1)
}

@test
fn test_mixed_value_types() {
    var m = #{"int": 42, "str": "hello", "bool": true, "null": null}
    assert_eq(m.get("int"), 42)
    assert_eq(m.get("str"), "hello")
    assert_eq(m.get("bool"), true)
    assert_eq(m.get("null"), null)
}

// ===== 边界条件 =====
@test
fn test_many_entries() {
    var m: Map<string, i64> = #{}
    for (var i = 0; i < 100; i++) {
        m.set("key" + i.toString(), i)
    }
    assert_eq(len(m), 100)
    assert_eq(m.get("key50"), 50)
}

@test
fn test_overwrite_many_times() {
    var m = #{"x": 0}
    for (var i = 0; i < 10; i++) {
        m.set("x", i)
    }
    assert_eq(m.get("x"), 9)
    assert_eq(len(m), 1)
}

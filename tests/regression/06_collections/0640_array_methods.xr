// 0640_array_methods.xr
// 作者：xingleixu@gmail.com
// 测试：数组方法扩展
// ===== slice expression =====
@test
fn test_slice_basic() {
    let arr = [1, 2, 3, 4, 5]
    let s = arr[1:4]
    assert_eq(s.length, 3)
    assert_eq(s[0], 2)
    assert_eq(s[2], 4)
}

@test
fn test_slice_from_start() {
    let arr = [1, 2, 3, 4, 5]
    let s = arr[:2]
    assert_eq(s.length, 2)
    assert_eq(s[0], 1)
}

@test
fn test_slice_to_end() {
    let arr = [1, 2, 3, 4, 5]
    let s = arr[3:]
    assert_eq(s.length, 2)
    assert_eq(s[1], 5)
}

@test
fn test_slice_negative_bounds() {
    let arr = [1, 2, 3, 4, 5]
    let s = arr[-4:-1]
    assert_eq(s.length, 3)
    assert_eq(s[0], 2)
    assert_eq(s[2], 4)
    let empty = arr[4:2]
    assert_eq(empty.length, 0)
}

@test
fn test_slice_is_view() {
    let arr = [10, 20, 30, 40]
    let s = arr[1:3]
    arr[1] = 21
    assert_eq(s[0], 21)
    s[1] = 31
    assert_eq(arr[2], 31)
}

@test
fn test_slice_copy_is_independent() {
    let arr = [10, 20, 30, 40]
    let s = copy(arr[1:3])
    arr[1] = 21
    assert_eq(s[0], 20)
    s[1] = 31
    assert_eq(arr[2], 30)
}

// ===== concat =====
@test
fn test_concat_arrays() {
    let a = [1, 2]
    let b = [3, 4]
    let c = a.concat(b)
    assert_eq(c.length, 4)
    assert_eq(c[0], 1)
    assert_eq(c[3], 4)
}

// ===== find =====
@test
fn test_find_element() {
    let arr = [10, 20, 30, 40]
    let found = arr.find(fn(x) {
        return x > 25
    })
    assert_eq(found, 30)
}

@test
fn test_find_not_found() {
    let arr = [1, 2, 3]
    let found = arr.find(fn(x) {
        return x > 100
    })
    assert_eq(found, null)
}

// ===== findIndex =====
@test
fn test_find_index() {
    let arr = ["a", "b", "c", "d"]
    let idx = arr.findIndex(fn(x) {
        return x == "c"
    })
    assert_eq(idx, 2)
}

@test
fn test_find_index_not_found() {
    let arr = [1, 2, 3]
    let idx = arr.findIndex(fn(x) {
        return x > 100
    })
    assert_eq(idx, -1)
}

@test
fn test_index_of_and_includes() {
    let arr = [10, 20, 30]
    assert_eq(arr.indexOf(20), 1)
    assert_eq(arr.indexOf(99), -1)
    assert_eq(arr.includes(30), true)
    assert_eq(arr.includes(99), false)
}

// ===== every =====
@test
fn test_every_true() {
    let arr = [2, 4, 6, 8]
    let result = arr.every(fn(x) {
        return x % 2 == 0
    })
    assert_eq(result, true)
}

@test
fn test_every_false() {
    let arr = [2, 3, 4, 6]
    let result = arr.every(fn(x) {
        return x % 2 == 0
    })
    assert_eq(result, false)
}

// ===== some =====
@test
fn test_some_true() {
    let arr = [1, 3, 5, 6]
    let result = arr.some(fn(x) {
        return x % 2 == 0
    })
    assert_eq(result, true)
}

@test
fn test_some_false() {
    let arr = [1, 3, 5, 7]
    let result = arr.some(fn(x) {
        return x % 2 == 0
    })
    assert_eq(result, false)
}

// ===== fill =====
@test
fn test_fill() {
    let arr = [1, 2, 3, 4, 5]
    arr.fill(0)
    assert_eq(arr[0], 0)
    assert_eq(arr[4], 0)
}

@test
fn test_fill_range_and_negative_bounds() {
    let arr = [1, 2, 3, 4, 5, 6]
    arr.fill(9, 2, 5)
    assert_eq(arr[0], 1)
    assert_eq(arr[2], 9)
    assert_eq(arr[4], 9)
    assert_eq(arr[5], 6)

    arr.fill(7, -3, -1)
    assert_eq(arr[2], 9)
    assert_eq(arr[3], 7)
    assert_eq(arr[4], 7)
    assert_eq(arr[5], 6)

    arr.fill(8, 5, 2)
    assert_eq(arr[2], 9)
    assert_eq(arr[5], 6)

    arr.fill(4, 4)
    assert_eq(arr[3], 7)
    assert_eq(arr[4], 4)
    assert_eq(arr[5], 4)
}

// ===== sort =====
@test
fn test_sort_numbers() {
    let arr = [3, 1, 4, 1, 5, 9, 2, 6]
    arr.sort()
    assert_eq(arr[0], 1)
    assert_eq(arr[1], 1)
    assert_eq(arr[7], 9)
}

@test
fn test_sort_strings() {
    let arr = ["banana", "apple", "cherry"]
    arr.sort()
    assert_eq(arr[0], "apple")
    assert_eq(arr[2], "cherry")
}

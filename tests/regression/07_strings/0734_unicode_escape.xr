// 0734_unicode_escape.xr
// 作者：xingleixu@gmail.com
// 测试：字符串字面量 unicode 转义（\uXXXX 与 \u{...}）
// 回归背景：R2-1 —— string 转义解码器曾缺失 case 'u'，\u{...} 被静默原样输出。
// 断言全部使用绝对期望值（不依赖 VM/AOT 对拍），防止"两后端一致错"再次潜伏。

// ===== \u{...} 花括号形式（1-6 位十六进制）=====

@test
fn test_braced_ascii() {
    assert_eq("\u{48}\u{49}", "HI")
    assert_eq("\u{7A}", "z")
    assert_eq(len("\u{48}"), 1)
}

@test
fn test_braced_cjk() {
    assert_eq("\u{4E16}\u{754C}", "世界")
    assert_eq("\u{4F60}\u{597D}", "你好")
    assert_eq(len("\u{4E16}"), 1)
    assert_eq(len("\u{4E16}".bytes()), 3)
}

@test
fn test_braced_emoji() {
    assert_eq("\u{1F600}", "😀")
    assert_eq(len("\u{1F600}"), 1)
    assert_eq(len("\u{1F600}".bytes()), 4)
    assert_eq("\u{1F600}".runes().nth(0).toUInt32(), 128512)
}

@test
fn test_braced_max_codepoint() {
    // U+10FFFF 是最大合法码点；6 位十六进制上限
    assert_eq(len("\u{10FFFF}"), 1)
    assert_eq("\u{10FFFF}".runes().nth(0).toUInt32(), 1114111)
}

@test
fn test_braced_leading_zeros() {
    assert_eq("\u{000041}", "A")
    assert_eq("\u{0041}", "A")
}

// ===== \uXXXX 四位形式（规范 EscapeSeq: 'u' HexDigit{4}）=====

@test
fn test_four_digit_form() {
    assert_eq("\u4F60\u597D", "你好")
    assert_eq("\u0041\u0042\u0043", "ABC")
    assert_eq("\u20AC", "€")
    assert_eq(len("\u4E16"), 1)
}

// ===== string 与 rune 两条解析路径必须一致 =====

@test
fn test_string_rune_consistency() {
    assert_eq("\u{4E16}".runes().nth(0), '\u{4E16}')
    assert_eq("\u{1F600}".runes().nth(0), '\u{1F600}')
    assert_eq("\u{41}".runes().nth(0), 'A')
}

// ===== 模板字符串（插值路径与普通字符串共用解码器）=====

@test
fn test_template_string_unicode() {
    var n = 42
    assert_eq("n=${n} \u{2764}", "n=42 ❤")
    assert_eq("\u{48}${n}\u{49}", "H42I")
    assert_eq("前${n}\u4E16后", "前42世后")
}

@test
fn test_template_string_hex_escape() {
    // \xHH 在模板字符串中必须与普通字符串一致（曾经只在普通字符串生效）
    var n = 1
    assert_eq("a${n}\x41end", "a1Aend")
    assert_eq("\x41plain", "Aplain")
}

// ===== 与相邻文本混排 =====

@test
fn test_mixed_text() {
    assert_eq("abc\u{4E16}def", "abc世def")
    assert_eq(len("abc\u{4E16}def"), 7)
    assert_eq("\u{48}\u{49} there", "HI there")
}

// ===== raw string 不解码 =====

@test
fn test_raw_string_untouched() {
    var raw = r"\u{48}"
    assert_eq(len(raw), 6)  // \ u { 4 8 } 原样保留，共 6 个字符
    assert_eq(raw.runes().nth(0), '\\')
}

print("=== Unicode escape tests completed ===")

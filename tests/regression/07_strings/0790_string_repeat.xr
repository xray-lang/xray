/*
** 字符串重复测试
** 作者：xingleixu@gmail.com
**
** 测试 s.repeat(n)。`"str" * n` 不是合法写法：`*` 只作用于数值。
*/

@test
fn test_string_repeat() {
    var line = "=".repeat(50)
    assertEqual(len(line), 50)

    var stars = "*".repeat(10)
    assertEqual(stars, "**********")

    // 边界情况
    var empty = "x".repeat(0)
    assertEqual(empty, "")

    var single = "abc".repeat(1)
    assertEqual(single, "abc")

    // 多字符重复
    var pattern = "ab".repeat(3)
    assertEqual(pattern, "ababab")

    // 中文字符重复
    var chinese = "好".repeat(3)
    assertEqual(chinese, "好好好")

    // 变量方式
    var s: string = "-"
    var n: i64 = 5
    var result = s.repeat(n)
    assertEqual(result, "-----")

    // 表达式中使用
    var header = "+" + "-".repeat(20) + "+"
    assertEqual(len(header), 22)
}

// 0720_stringbuilder.xr
// 作者：xingleixu@gmail.com
// 测试：StringBuilder 内置类

// ===== 基本操作 =====

@test
fn test_basic_append() {
    var sb = StringBuilder()
    sb.append("Hello")
    sb.append(" ")
    sb.append("World")
    assertEqual(sb.toString(), "Hello World")
}

@test
fn test_chain_append() {
    var sb = StringBuilder()
    sb.append("a").append("b").append("c")
    assertEqual(sb.toString(), "abc")
}

// ===== 不同类型 =====

@test
fn test_append_int() {
    var sb = StringBuilder()
    sb.append("num: ")
    sb.append(42)
    assertEqual(sb.toString(), "num: 42")
}

@test
fn test_append_float() {
    var sb = StringBuilder()
    sb.append("pi: ")
    sb.append(3.14)
    var result = sb.toString()
    assertEqual(result.startsWith("pi: 3.14"), true)
}

@test
fn test_mixed_types() {
    var sb = StringBuilder()
    sb.append("count=")
    sb.append(100)
    sb.append(", ok=")
    sb.append(true)
    var result = sb.toString()
    assertEqual(result.contains("100"), true)
}

@test
fn test_append_char() {
    var sb = StringBuilder()
    sb.append("hex=")
    sb.append("0123456789abcdef".runes().nth(10))
    sb.append(", unicode=")
    sb.append("你好".runes().nth(1))
    assertEqual(sb.toString(), "hex=a, unicode=好")
}

// ===== 循环中使用 =====

@test
fn test_loop_append() {
    var sb = StringBuilder()
    for (i in 0..5) {
        sb.append("x")
    }
    assertEqual(sb.toString(), "xxxxx")
}

@test
fn test_loop_numbers() {
    var sb = StringBuilder()
    for (i in 1..4) {
        if (i > 1) {
            sb.append(",")
        }
        sb.append(i)
    }
    assertEqual(sb.toString(), "1,2,3")
}

// ===== clear 方法 =====

@test
fn test_clear() {
    var sb = StringBuilder()
    sb.append("test")
    sb.clear()
    sb.append("new")
    assertEqual(sb.toString(), "new")
}

@test
fn test_clear_then_reuse() {
    var sb = StringBuilder()
    sb.append("first")
    assertEqual(sb.toString(), "first")
    sb.clear()
    sb.append("second")
    assertEqual(sb.toString(), "second")
}

// ===== 空 StringBuilder =====

@test
fn test_empty() {
    var sb = StringBuilder()
    assertEqual(sb.toString(), "")
}

// ===== 大量数据 =====

@test
fn test_large_append() {
    var sb = StringBuilder()
    for (i in 0..100) {
        sb.append("a")
    }
    assertEqual(len(sb.toString()), 100)
}

// 0720_stringbuilder.xr
// 作者：xingleixu@gmail.com
// 测试：StringBuilder 内置类

// ===== 基本操作 =====

@test
fn test_basic_append() {
    let sb = StringBuilder()
    sb.append("Hello")
    sb.append(" ")
    sb.append("World")
    assert_eq(sb.toString(), "Hello World")
}

@test
fn test_chain_append() {
    let sb = StringBuilder()
    sb.append("a").append("b").append("c")
    assert_eq(sb.toString(), "abc")
}

// ===== 不同类型 =====

@test
fn test_append_int() {
    let sb = StringBuilder()
    sb.append("num: ")
    sb.append(42)
    assert_eq(sb.toString(), "num: 42")
}

@test
fn test_append_float() {
    let sb = StringBuilder()
    sb.append("pi: ")
    sb.append(3.14)
    let result = sb.toString()
    assert_eq(result.startsWith("pi: 3.14"), true)
}

@test
fn test_mixed_types() {
    let sb = StringBuilder()
    sb.append("count=")
    sb.append(100)
    sb.append(", ok=")
    sb.append(true)
    let result = sb.toString()
    assert_eq(result.includes("100"), true)
}

@test
fn test_append_char() {
    let sb = StringBuilder()
    sb.append("hex=")
    sb.append("0123456789abcdef"[10])
    sb.append(", unicode=")
    sb.append("你好"[1])
    assert_eq(sb.toString(), "hex=a, unicode=好")
}

// ===== 循环中使用 =====

@test
fn test_loop_append() {
    let sb = StringBuilder()
    for (i in 0..5) {
        sb.append("x")
    }
    assert_eq(sb.toString(), "xxxxx")
}

@test
fn test_loop_numbers() {
    let sb = StringBuilder()
    for (i in 1..4) {
        if (i > 1) {
            sb.append(",")
        }
        sb.append(i)
    }
    assert_eq(sb.toString(), "1,2,3")
}

// ===== clear 方法 =====

@test
fn test_clear() {
    let sb = StringBuilder()
    sb.append("test")
    sb.clear()
    sb.append("new")
    assert_eq(sb.toString(), "new")
}

@test
fn test_clear_then_reuse() {
    let sb = StringBuilder()
    sb.append("first")
    assert_eq(sb.toString(), "first")
    sb.clear()
    sb.append("second")
    assert_eq(sb.toString(), "second")
}

// ===== 空 StringBuilder =====

@test
fn test_empty() {
    let sb = StringBuilder()
    assert_eq(sb.toString(), "")
}

// ===== 大量数据 =====

@test
fn test_large_append() {
    let sb = StringBuilder()
    for (i in 0..100) {
        sb.append("a")
    }
    assert_eq(sb.toString().length, 100)
}

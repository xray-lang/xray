// Test: just define and use a class

class Box {
    value: int
    constructor(v: int) {
        this.value = v
    }
}

@test
fn test_crash_min() {

    for (let i = 0; i < 200; i++) {
        let b = new Box(i)
    }
}


// 1200_log_basic.xr - Log module tests

import log
import { LogFormat, LogLevel } from log

// ===== Typed levels =====

@test
fn test_log_level_ordering() {
    assert_eq(log.isEnabled(LogLevel.Fatal), true)
    assert_eq(LogLevel.Debug == LogLevel.Debug, true)
    assert_eq(LogLevel.Debug == LogLevel.Info, false)
}

// ===== isEnabled =====

@test
fn test_is_enabled_default() {
    // Default level is Info
    assert_eq(log.isEnabled(LogLevel.Info), true)
    assert_eq(log.isEnabled(LogLevel.Error), true)
    assert_eq(log.isEnabled(LogLevel.Debug), false)
}

@test
fn test_set_level() {
    log.setLevel(LogLevel.Debug)
    assert_eq(log.isEnabled(LogLevel.Debug), true)
    // Restore default
    log.setLevel(LogLevel.Info)
    assert_eq(log.isEnabled(LogLevel.Debug), false)
}

// ===== Log output (should not crash) =====

@test
fn test_log_functions() {
    log.setLevel(LogLevel.Debug)
    log.debug("debug message")
    log.info("info message")
    log.warn("warning message")
    log.error("error message")
    log.setLevel(LogLevel.Info)
}

@test
fn test_log_with_attrs() {
    log.info("server started", "port", 8080, "host", "localhost")
}

// ===== Format =====

@test
fn test_log_json_format() {
    log.setFormat(LogFormat.Json)
    log.info("json format test", "key", "value")
    log.setFormat(LogFormat.Text)
}

import regex

fn regex_match_ok(pattern: string, text: string) -> bool {
    var re = regex.compile(pattern)
    return regex.find(re, text) != null
}

@test
fn test_inlined_regex_find_uses_current_args() {
    for (var i = 0; i < 260; i++) {
        assertEqual(regex_match_ok("", "abc"), true)
    }
    assertEqual(regex_match_ok("a", ""), false)
}

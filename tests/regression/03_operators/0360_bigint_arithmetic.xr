// 0360_bigint_arithmetic.xr
// 作者：xingleixu@gmail.com
// 测试：BigInt 算术运算符

// ===== 加法 =====

@test
fn test_add_small() {
    var a = 100n
    var b = 200n
    assertEqual((a + b).toString(), "300")
}

@test
fn test_add_large() {
    var a = 999999999999999999999999999999n
    var b = 1n
    assertEqual((a + b).toString(), "1000000000000000000000000000000")
}

// ===== 减法 =====

@test
fn test_sub_basic() {
    var a = 200n
    var b = 100n
    assertEqual((a - b).toString(), "100")
}

@test
fn test_sub_negative_result() {
    var a = 100n
    var b = 200n
    assertEqual((a - b).toString(), "-100")
}

// ===== 乘法 =====

@test
fn test_mul_basic() {
    var a = 100n
    var b = 200n
    assertEqual((a * b).toString(), "20000")
}

@test
fn test_mul_large() {
    var a = 12345678901234567890n
    var b = 2n
    assertEqual((a * b).toString(), "24691357802469135780")
}

// ===== 除法 =====

@test
fn test_div_basic() {
    var a = 100n
    var b = 3n
    assertEqual((a / b).toString(), "33")
}

@test
fn test_div_exact() {
    var a = 100n
    var b = 10n
    assertEqual((a / b).toString(), "10")
}

// ===== 取模 =====

@test
fn test_mod_basic() {
    var a = 100n
    var b = 3n
    assertEqual((a % b).toString(), "1")
}

@test
fn test_mod_no_remainder() {
    var a = 100n
    var b = 10n
    assertEqual((a % b).toString(), "0")
}

// ===== 取负 =====

@test
fn test_negate_positive() {
    var a = 999n
    assertEqual((-a).toString(), "-999")
}

@test
fn test_negate_negative() {
    var a = -999n
    assertEqual((-a).toString(), "999")
}

// 0360_precedence.xr
// 作者：xingleixu@gmail.com
// 测试：运算符优先级

// ===== 算术优先级 =====

@test
fn test_mul_before_add() {
    assertEqual(2 + 3 * 4, 14)
    assertEqual(10 - 2 * 3, 4)
}

@test
fn test_div_before_sub() {
    assertEqual(10 - 6 / 2, 7)
    assertEqual(20 + 8 / 4, 22)
}

@test
fn test_parentheses_override() {
    assertEqual((2 + 3) * 4, 20)
    assertEqual(2 * (3 + 4), 14)
}

// ===== 比较与逻辑优先级 =====

@test
fn test_comparison_before_and() {
    assertEqual(1 < 2 && 3 < 4, true)
    assertEqual(1 > 2 && 3 < 4, false)
}

@test
fn test_and_before_or() {
    assertEqual(true || false && false, true)
    assertEqual(false || true && true, true)
    assertEqual(false || false && true, false)
}

// ===== 算术与比较混合 =====

@test
fn test_arithmetic_then_compare() {
    assertEqual(2 + 3 < 10, true)
    assertEqual(2 * 5 == 10, true)
    assertEqual(10 - 3 >= 7, true)
}

// ===== 位运算优先级 =====

@test
fn test_shift_before_comparison() {
    var x = 1
    var shift = 3
    var result = x << shift
    assertEqual(result < 16, true)
}

@test
fn test_bitwise_and_before_or() {
    // a | b & c == a | (b & c)
    assertEqual(0b1100 | 0b1010 & 0b0110, 0b1110)
}

// ===== 一元运算符 =====

@test
fn test_unary_minus() {
    assertEqual(-5 + 10, 5)
    assertEqual(10 + -5, 5)
    assertEqual(-(-5), 5)
}

@test
fn test_logical_not() {
    assertEqual(!true, false)
    assertEqual(!false, true)
    assertEqual(!!true, true)
}

// ===== 复杂表达式 =====

@test
fn test_complex_expression() {
    var a = 2
    var b = 3
    var c = 4
    // a + b * c - a = 2 + 12 - 2 = 12
    assertEqual(a + b * c - a, 12)
}

@test
fn test_nested_parentheses() {
    assertEqual(((1 + 2) * 3) + 4, 13)
    assertEqual((1 + (2 * (3 + 4))), 15)
}

// ===== 三元运算符优先级 =====

@test
fn test_ternary_with_arithmetic() {
    var x = 5
    var result = x > 3 ? x * 2 : x + 1
    assertEqual(result, 10)
}

@test
fn test_ternary_false_branch() {
    var x = 1
    var result = x > 3 ? x * 2 : x + 1
    assertEqual(result, 2)
}

@test
fn test_nested_ternary() {
    var x = 5
    var result = x < 0 ? "negative" : x == 0 ? "zero" : "positive"
    assertEqual(result, "positive")
}

@test
fn test_ternary_zero() {
    var x = 0
    var result = x < 0 ? "negative" : x == 0 ? "zero" : "positive"
    assertEqual(result, "zero")
}

// ===== 赋值优先级（最低）=====

@test
fn test_assignment_lowest() {
    var x = 0
    x = 2 + 3  // 赋值的是 5，不是 (x = 2) + 3
    assertEqual(x, 5)
}


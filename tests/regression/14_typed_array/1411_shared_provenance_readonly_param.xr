fn readFirst(view: Slice<byte>) -> int {
    return int(view[0]) + len(view)
}

@test
fn test_shared_provenance_readonly_param() {
    const bytes = Array<byte>(2, 4)
    const view: Slice<byte> = bytes[:]
    assert_eq(readFirst(view), 6)
}

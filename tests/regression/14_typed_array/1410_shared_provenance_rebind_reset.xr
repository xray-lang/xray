class Box {
    items: Array<int> = Array<int>(1, 0)
}

@test
fn test_shared_provenance_rebind_reset() {
    const box = Box()
    var items = box.items
    items = Array<int>(1, 0)
    items[0] = 7
    assert_eq(items[0], 7)
}

class SharedCaptureBox {
    items: Array<int> = Array<int>(2, 5)
}

@test
fn test_shared_provenance_readonly_closure_capture() {
    shared box = SharedCaptureBox()
    var items = box.items
    const read = fn() -> int {
        return items[0] + len(items)
    }
    assert_eq(read(), 7)
}

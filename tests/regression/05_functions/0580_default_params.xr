// Test: default parameter values
// Expected output:
// Hello, World
// Hello, Alice
// 111
// 103
// 6
// 0 0
// 5 0
// 5 10
// John Unknown
// John Doe

// Basic default parameter

fn greet(name: string = "World") {
    print("Hello, " + name)
}
fn add(a: i64, b: i64 = 10, c: i64 = 100) -> i64 {
    return a + b + c
}
type Point = { x: i64, y: i64 }
fn createPoint(x: i64 = 0, y: i64 = 0) -> Point {
    return { x: x, y: y }
}
fn formatName(first: string, last: string = "Unknown") -> string {
    return first + " " + last
}

@test
fn test_default_params() {

    greet()
    greet("Alice")

    // Multiple parameters with defaults

    print(add(1))           // 1 + 10 + 100 = 111
    print(add(1, 2))        // 1 + 2 + 100 = 103
    print(add(1, 2, 3))     // 1 + 2 + 3 = 6

    // Default with all optional params

    var p1 = createPoint()
    var p2 = createPoint(5)
    var p3 = createPoint(5, 10)
    print(p1.x, p1.y)       // 0 0
    print(p2.x, p2.y)       // 5 0
    print(p3.x, p3.y)       // 5 10

    // Required param followed by optional

    print(formatName("John"))           // John Unknown
    print(formatName("John", "Doe"))    // John Doe
}

// Range patterns cover integer subjects only; floating-point endpoints have
// no exact-step semantics and NaN breaks the ordering.
fn f(x: float) -> string {
    return match (x) {
        0.0..1.0 -> "low"
        _ -> "high"
    }
}

fn main() { print(f(0.5)) }
main()

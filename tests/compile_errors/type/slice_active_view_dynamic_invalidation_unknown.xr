type Grow = fn(ref Array<byte>)

fn grow(data: ref Array<byte>) {
    data.push(99)
}

fn invoke(handler: Grow) {
    var owner: Array<byte> = [1, 2]
    var view: Slice<byte> = owner[:]
    handler(ref owner)
    print(view[0])
}

invoke(grow)

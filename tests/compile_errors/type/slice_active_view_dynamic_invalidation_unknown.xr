type Grow = fn(ref Array<u8>)

fn grow(data: ref Array<u8>) {
    data.push(99)
}

fn invoke(handler: Grow) {
    var owner: Array<u8> = [1, 2]
    var view: Slice<u8> = owner[:]
    handler(ref owner)
    print(view[0])
}

invoke(grow)

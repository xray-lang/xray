class Box {
    bytes: Array<byte> = Array<byte>(0)
}

fn main() {
    var box = Box()
    box.bytes.push(1)
    var view: Slice<byte> = box.bytes[:]
    box.bytes.push(2)
    print(len(view))
}

main()

fn erase(x: unknown) -> unknown {
    return x
}

fn bad() {
    var bytes = Array<byte>(4)
    var view: Slice<byte> = bytes[:]
    var any = erase(view)
    var values = Array<unknown>()
    values.push(any)
}

bad()

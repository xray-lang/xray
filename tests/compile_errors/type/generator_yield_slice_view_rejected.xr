fn chunks(s: string) -> Iterator<Slice<u8>> {
    var view: Slice<u8> = s.bytes()
    yield view
}

fn main() {
    var text = "hello"
    for (chunk in chunks(text)) {
        print(len(chunk))
    }
}

main()

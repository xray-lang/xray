// @no_suspend on a function whose body reaches a suspend point (await) must be
// rejected at the definition (task 217, fail-closed assertion).
fn worker() -> int {
    return 1
}

@no_suspend
fn bad() -> int {
    var t = go worker()
    return await t
}

fn main() {
    print(bad())
}

fn chunks(text: string) -> Iterator<const Slice<u8>> {
    var view: const Slice<u8> = text.bytes()
    yield view
}

fn main() {
    for (chunk in chunks("hello")) {
        print(len(chunk))
    }
}

main()

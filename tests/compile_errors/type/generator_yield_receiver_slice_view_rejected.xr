fn chunks(text: string) -> Iterator<Slice<byte>> {
    var view: Slice<byte> = text.bytes()
    yield view
}

fn main() {
    for (chunk in chunks("hello")) {
        print(len(chunk))
    }
}

main()

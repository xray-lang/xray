fn worker(view: Slice<byte>) -> int {
    return len(view)
}

fn bad() {
    var bytes = Array<byte>(4)
    var view: Slice<byte> = bytes[0:2]
    var task = go worker(move view)
    print(await task)
}

bad()

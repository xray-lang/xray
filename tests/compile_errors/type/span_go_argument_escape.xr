fn worker(view: ByteSpan) -> int {
    return view.length
}

fn bad() {
    var bytes = Bytes(4)
    var view: ByteSpan = bytes[0:2]
    var task = go worker(move view)
    print(await task)
}

bad()

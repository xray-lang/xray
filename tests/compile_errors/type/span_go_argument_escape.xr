fn worker(view: Slice<u8>) -> i64 {
    return len(view)
}

fn bad() {
    var bytes = Array<u8>(4)
    var view: Slice<u8> = bytes[0:2]
    var task = go worker(move view)
    print(await task)
}

bad()

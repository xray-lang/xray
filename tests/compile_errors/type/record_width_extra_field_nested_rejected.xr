// Record width rule reaches into nested Record fields: the extra field lives on
// the inner shape, and the mismatch is still rejected.
type Point = { x: int, y: int }
type Outer = { p: Point }

fn main() {
    var o: Outer = { p: { x: 1, y: 2, z: 3 } }
    print(o.p.x)
}

main()

fn graphemes(text: string) -> Iterator<Slice<u8>> {
    var view: Slice<u8> = text.bytes()
    if (len(view) > 0) {
        print(len(view))
    }
}

fn main() {
    for (cluster in graphemes("hello")) {
        print(len(cluster))
    }
}

main()

// Record width rule: a source with MORE fields is not assignable to a sealed
// Record target with fewer fields. Xray has no width subtyping (§2.10.1).
type User = { name: string }

fn main() {
    var full = { name: "Ann", age: 18 }
    var u: User = full
    print(u.name)
}

main()

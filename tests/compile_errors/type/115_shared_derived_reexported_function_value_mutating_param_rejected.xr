import { overwrite } from "../../fixtures/shared_mutation_reexport"

fn main() {
    const bytes = Array<byte>(4, 0)
    var view: const Slice<byte> = bytes[:]
    const f = overwrite
    f(ref view)
}

main()

fn overwrite(view: Slice<u8>) {
    view.fill(1)
}

fn wrapper(view: Slice<u8>) {
    overwrite(view)
}

fn main() {
    const bytes = Array<u8>(4, 0)
    var view: Slice<u8> = bytes[:]
    wrapper(view)
}

main()

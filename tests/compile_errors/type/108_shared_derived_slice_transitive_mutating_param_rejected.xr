fn overwrite(view: Slice<byte>) {
    view.fill(1)
}

fn wrapper(view: Slice<byte>) {
    overwrite(view)
}

fn main() {
    const bytes = Array<byte>(4, 0)
    var view: Slice<byte> = bytes[:]
    wrapper(view)
}

main()

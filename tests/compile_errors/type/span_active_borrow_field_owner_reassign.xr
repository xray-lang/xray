class Pair {
    left: Bytes = Bytes(0)
    right: Bytes = Bytes(0)
}

fn main() {
    var box = Pair()
    box.left.push(1)
    var view: ByteSpan = box.left[:]
    box.left = Bytes(0)
    print(view.length)
}

main()

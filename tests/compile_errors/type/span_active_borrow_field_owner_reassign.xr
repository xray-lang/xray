class Pair {
    left: Array<u8> = Array<u8>(0)
    right: Array<u8> = Array<u8>(0)
}

fn main() {
    var box = Pair()
    box.left.push(1)
    var view: Slice<u8> = box.left[:]
    box.left = Array<u8>(0)
    print(len(view))
}

main()

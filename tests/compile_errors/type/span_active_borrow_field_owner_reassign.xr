class Pair {
    left: Array<byte> = Array<byte>(0)
    right: Array<byte> = Array<byte>(0)
}

fn main() {
    var box = Pair()
    box.left.push(1)
    var view: Slice<byte> = box.left[:]
    box.left = Array<byte>(0)
    print(len(view))
}

main()

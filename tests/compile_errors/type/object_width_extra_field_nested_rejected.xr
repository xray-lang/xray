// Object width rule reaches into nested Object fields: the extra field lives on
// the inner shape, and the mismatch is still rejected.
type Point = { x: i64, y: i64 }
type Outer = { p: Point }

fn main() {
    var o: Outer = { p: { x: 1, y: 2, z: 3 } }
    print(o.p.x)
}

main()

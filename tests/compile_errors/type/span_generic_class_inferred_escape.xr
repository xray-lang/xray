class Box<T> {
    value: T

    constructor(value: T) {
        this.value = value
    }
}

fn bad() {
    var bytes = Array<byte>(4)
    var view: Slice<byte> = bytes[:]
    var box = Box(view)
    print(len(box.value))
}

bad()

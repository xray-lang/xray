class Box<T> {
    value: T

    constructor(value: T) {
        this.value = value
    }
}

fn bad() {
    var bytes = Array<u8>(4)
    var view: Slice<u8> = bytes[:]
    var box = Box(view)
    print(len(box.value))
}

bad()

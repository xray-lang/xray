import "../../fixtures/shared_mutation_reexport_all" as mut

fn main() {
    const bytes = Array<u8>(4, 0)
    var view: const Slice<u8> = bytes[:]
    const f = mut.overwrite
    f(ref view)
}

main()

fn overwrite(view: Slice<byte>) {
    view[0] = 1
}

fn callIt(f: (Slice<byte>) -> (), view: Slice<byte>) {
    f(view)
}

fn main() {
    shared bytes = Array<byte>(4, 0)
    var view: Slice<byte> = bytes[:]
    callIt(overwrite, view)
}

main()

fn overwrite(view: ref Slice<u8>) {
    view[0] = 1
}

fn callIt(f: fn(ref Slice<u8>), view: ref Slice<u8>) {
    f(ref view)
}

fn main() {
    const bytes = Array<u8>(4, 0)
    var view: const Slice<u8> = bytes[:]
    callIt(overwrite, ref view)
}

main()

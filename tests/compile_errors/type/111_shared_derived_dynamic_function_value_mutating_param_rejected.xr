fn overwrite(view: ref Slice<byte>) {
    view[0] = 1
}

fn callIt(f: (ref Slice<byte>) -> (), view: ref Slice<byte>) {
    f(ref view)
}

fn main() {
    const bytes = Array<byte>(4, 0)
    var view: const Slice<byte> = bytes[:]
    callIt(overwrite, ref view)
}

main()

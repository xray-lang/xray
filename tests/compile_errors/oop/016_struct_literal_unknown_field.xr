// Aggregate literals name their fields, so an undeclared name is decidable
// during analysis instead of surfacing as a runtime field-not-declared panic.
struct Point {
    x: int
    y: int
}

var p = Point{x: 1, y: 2, z: 3}
print(p.x)

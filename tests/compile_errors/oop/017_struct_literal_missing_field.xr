// A field without a declaration default and without a nullable type must be
// set by the literal; use Point() when a whole zero value is intended.
struct Point {
    x: i64
    y: i64
}

var p = Point{x: 1}
print(p.y)

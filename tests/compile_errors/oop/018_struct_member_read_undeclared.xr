// A declared struct has a closed member set, so reading an undeclared name is
// a compile error rather than a runtime panic.
struct Point {
    x: int
    y: int
}

var p = Point{x: 1, y: 2}
print(p.zzz)

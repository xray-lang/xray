// A 'where' constraint must be enforced exactly like an inline `<T: C>` one.
class Opaque { x: i64 }

fn maxOf<T>(a: T, b: T) -> T where T: Comparable {
    if (a > b) { return a }
    return b
}

var p = Opaque{ x: 1 }
var q = Opaque{ x: 2 }
print(maxOf<Opaque>(p, q))

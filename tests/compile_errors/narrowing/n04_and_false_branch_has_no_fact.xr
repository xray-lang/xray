// Spec §2.13 N-4: the false branch of `&&` produces no fact.
fn main() {
    var a: string? = "abc"
    var flag = false
    if (a != null && flag) {
        print(len(a))
    } else {
        print(len(a))
    }
}

main()

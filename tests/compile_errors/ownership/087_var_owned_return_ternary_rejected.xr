fn makeA() -> Array<u8> {
    var a = Array<u8>(1, 0)
    return move a
}

fn makeB() -> Array<u8> {
    var b = Array<u8>(2, 0)
    return move b
}

fn choose(flag: bool) -> Array<u8> {
    return flag ? makeA() : makeB()
}

fn main() {
    var local = choose(true)
    print(len(local))
}

main()

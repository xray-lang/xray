fn buildBuffer() -> Array<u8> {
    var result = Array<u8>(1, 0)
    const frozen = move result
    return frozen
}

fn main() {
    const f = buildBuffer
    var local = f()
    print(len(local))
}

main()

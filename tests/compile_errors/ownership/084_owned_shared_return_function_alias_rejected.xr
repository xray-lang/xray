fn buildBuffer() -> Array<byte> {
    var result = Array<byte>(1, 0)
    const frozen = move result
    return frozen
}

fn main() {
    const f = buildBuffer
    var local = f()
    print(len(local))
}

main()

class CounterBase {
    value: i64

    constructor(v) {
        this.value = v
    }

    ref bump() {
        this.value = this.value + 1
    }
}

class CounterChild extends CounterBase {
    constructor(v) {
        super(v)
    }

    ref callBaseBump() {
        this.bump()
    }
}

fn view(counter: CounterChild) {
    counter.callBaseBump()
}

var counter = CounterChild(1)
view(counter)

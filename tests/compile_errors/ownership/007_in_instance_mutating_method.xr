class Counter {
    value: int

    constructor(v) {
        this.value = v
    }

    bump() {
        this.value = this.value + 1
    }
}

fn view(counter: in Counter) {
    counter.bump()
}

let counter = new Counter(1)
view(counter)

class Counter {
    value: i64

    constructor(v) {
        this.value = v
    }

    ref bump() {
        this.value = this.value + 1
    }
}

fn view(counter: Counter) {
    counter.bump()
}

var counter = Counter(1)
view(counter)

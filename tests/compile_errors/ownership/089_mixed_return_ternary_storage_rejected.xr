fn makeOwned() -> Array<u8> {
    var a = Array<u8>(1, 0)
    return move a
}

fn makeShared() -> Array<u8> {
    var b = Array<u8>(2, 0)
    const frozen = move b
    return frozen
}

fn choose(flag: bool) -> Array<u8> {
    return flag ? makeOwned() : makeShared()
}

fn main() {
    var local = choose(true)
    print(len(local))
}

main()

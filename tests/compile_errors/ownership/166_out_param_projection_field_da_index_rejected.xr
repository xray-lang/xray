struct ProjectionDaBox {
    slots: [int; 2]
}

fn fill(value: out int) {
    value = 1
}

fn bad(box: out ProjectionDaBox) {
    fill(out box.slots[0])
    var observed = box.slots[1]
    box = ProjectionDaBox{slots: [box.slots[0], observed]}
}

fn forward_array(ch: Channel<Array<int>>, data: Array<int>) {
    ch.send(move data)
}

fn main() {
    shared ch: Channel<Array<int>> = Channel(1)
    owned xs = Array<int>(3, 0)
    forward_array(ch, xs)
    ch.close()
}

main()

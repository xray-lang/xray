class Box<T> {
    value: T

    constructor(value: T) {
        this.value = value
    }

    touch(value: ref T) {
    }
}

fn main() {
    var box = Box<int>(1)
    var value = 2
    box.touch(value)
}

main()

import { forwardImportedArray } from "../../fixtures/owned_transfer_lib"

fn main() {
    const ch: Channel<Array<int>> = Channel(1)
    var xs = [1, 2, 3]
    forwardImportedArray(ch, move xs)
    ch.close()
}

main()

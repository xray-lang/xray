fn buildBuffer() -> Array<u8> {
    var result = Array<u8>(1, 0)
    return move result
}

fn wrap(f: fn() -> Array<u8>) -> Array<u8> {
    return f()
}

fn main() {
    var local = wrap(buildBuffer)
    print(len(local))
}

main()

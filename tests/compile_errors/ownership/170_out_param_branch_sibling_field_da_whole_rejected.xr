struct BranchSiblingPair {
    left: int
    right: int
}

fn bad(flag: bool, pair: out BranchSiblingPair) {
    if (flag) {
        pair.left = 1
    } else {
        pair.right = 2
    }

    var whole = pair
    pair = BranchSiblingPair{left: whole.left, right: whole.right}
}

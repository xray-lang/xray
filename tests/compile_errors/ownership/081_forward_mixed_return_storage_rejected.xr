fn main() {
    var value = choose(true)
    print(len(value))
}

fn choose(flag: bool) -> Array<byte> {
    var ownedBuf = Array<byte>(1, 1)
    const sharedBuf = copy(ownedBuf)
    if (flag) {
        return move ownedBuf
    }
    return sharedBuf
}

main()

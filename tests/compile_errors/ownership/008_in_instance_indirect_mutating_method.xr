class Counter {
    value: i64

    constructor(v) {
        this.value = v
    }

    ref bump() {
        this.value = this.value + 1
    }

    ref callBump() {
        this.bump()
    }
}

fn view(counter: Counter) {
    counter.callBump()
}

var counter = Counter(1)
view(counter)

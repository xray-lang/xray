import parallel

fn main_logic() {
    var seen = 0
    var total = parallel.reduce(
        0..4,
        0,
        (i) -> i,
        (a, b) -> {
            seen = seen + b
            return a + b
        },
        parallel.Options(2)
    )
    print(total)
}

main_logic()

import parallel

fn worker() -> i64 {
    return 1
}

fn suspends() -> i64 {
    var value = await go worker()
    return value
}

parallel.forEach(0..4, (i) -> {
    print(suspends())
}, parallel.Options(2))

import parallel

fn worker() -> i64 {
    return 1
}

fn main_logic() {
    const task = go worker()
    parallel.forEach(0..4, (i) -> {
        var value = await task
        print(value)
    }, parallel.Options(2))
}

main_logic()

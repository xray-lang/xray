import parallel

fn worker() -> i64 {
    return 1
}

parallel.forEach(0..4, (i) -> {
    const f = fn(x: i64) -> i64 {
        var value = await go worker()
        return value + x
    }
    print(f(i))
}, parallel.Options(2))

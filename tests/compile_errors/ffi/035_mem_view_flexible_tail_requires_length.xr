import mem

extern "C" {
    struct Packet {
        length: uint32
        data: flex uint8
    }
}

fn main() {
    var p = mem.mutPtr<Packet>(0)
    var owner = Array<uint8>(8)
    var view = unsafe { mem.slice<Packet>(p, 1, owner) }
    print(view[0].data)
}

main()

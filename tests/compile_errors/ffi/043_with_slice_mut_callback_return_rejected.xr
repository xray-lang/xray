import mem

var guard = 0
var p = mem.mutPtr<byte>(0)
unsafe {
    var escaped = mem.withSliceMut<byte>(p, 0, ref guard,
        fn(view: ref Slice<byte>) -> Slice<byte> { return view })
    print(len(escaped))
}

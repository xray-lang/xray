import mem

fn value() -> i64 { return 1 }

var guard = 0
var p = mem.mutPtr<u8>(0)
unsafe {
    mem.withSliceMut<u8>(p, 0, ref guard, fn(view: ref Slice<u8>) {
        var task = go value()
        print(await task)
    })
}

import mem

extern "C" {
    struct Header {
        value: uint32
    }
}

fn run() {
    var buffer = mem.alloc(mem.sizeOf<Header>())
    var view = unsafe { mem.slice<Header>(buffer.borrowPtr(), 1, buffer) }
    buffer.resize(32)
    print(len(view))
}

run()

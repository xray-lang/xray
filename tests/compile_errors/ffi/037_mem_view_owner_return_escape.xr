import mem

extern "C" {
    struct Header {
        value: uint32
    }
}

fn leak() -> Slice<Header> {
    var buffer = mem.alloc(mem.sizeOf<Header>())
    return unsafe { mem.slice<Header>(buffer.borrowPtr(), 1, buffer) }
}

print(len(leak()))

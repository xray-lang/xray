import mem

extern "C" {
    struct Header {
        value: uint32
    }
}

fn main() {
    var p = mem.ptr<Header>(0)
    var owner = Array<uint8>(4)
    var view = unsafe { mem.slice<Header>(p, 1, owner) }
    view[0].value = 1
}

main()

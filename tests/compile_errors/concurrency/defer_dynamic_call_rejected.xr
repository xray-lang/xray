fn child() -> i64 {
    print("child must not start")
    return 1
}

fn starts_task() {
    var task = go child()
    print(task)
}

fn owner(action: fn()) {
    defer {
        action()
    }
}

owner(starts_task)

// Scope exit joins linked children and is therefore a real suspension point.
fn work() -> int { return 7 }

fn gen() -> Iterator<int> {
    scope {
        go work()
    }
    yield 1
}

for (x in gen()) { print(x) }

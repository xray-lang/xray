// `Coro.yield()` inside a generator used to compile and inject a spurious null
// element into the value stream, in both the VM and AOT backends.
fn gen() -> Iterator<i64> {
    yield 1
    Coro.yield()
    yield 2
}

for (x in gen()) { print(x) }

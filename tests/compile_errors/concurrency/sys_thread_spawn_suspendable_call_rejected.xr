import sys

fn worker() -> int {
    return 1
}

fn suspends() -> int {
    var y = await go worker()
    return y
}

fn main() {
    var t = sys.Thread.spawn(fn() -> int {
        return suspends()
    })
    print(t)
}

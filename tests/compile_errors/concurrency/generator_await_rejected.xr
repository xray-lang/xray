// A generator frame is resumed by its driver, not by the scheduler. Awaiting
// inside one used to compile and silently drop every remaining yield.
fn work() -> i64 { return 7 }

fn gen() -> Iterator<i64> {
    var t = go work()
    yield await t
    yield 2
}

for (x in gen()) { print(x) }

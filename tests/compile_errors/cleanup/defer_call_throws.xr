// Spec 8.3.1 rule D1: a cleanup block cannot let a callee's error escape.
enum CleanupErr { Failed }

fn closeIt() {
    throw CleanupErr.Failed
}

fn f() {
    defer { closeIt() }
    print("body")
}

f()

// anchor: array-capacity-panic-shared-core
var dyn: Json = {
    "bad": true
}

try {
    var xs = Array<int>(dyn.bad, 1)
    print(xs.length)
} catch panic (p) {
    print(p.message)
    print(p.code)
}

try {
    var ys = Array.withCapacity(dyn.bad)
    print(ys.length)
} catch panic (p) {
    print(p.message)
    print(p.code)
}

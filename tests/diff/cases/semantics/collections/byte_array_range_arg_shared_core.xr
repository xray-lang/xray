// anchor: bytes-range-arg-shared-core
fn main() {
    var bytes = Array<u8>(8, 1)
    var view: Slice<u8> = bytes[:]
    var offset = 4294967296
    print("before-range")
    var value: u32 = view.load<u32>(offset, Endian.LE)
    print((value as i64))
    print("unreachable")
}

main()

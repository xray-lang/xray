// anchor: span-copyfrom-pod-shared-core
fn main() {
    var dst = Array<uint32>(4, 0)
    var src = Array<uint32>(2, 0)
    src[0] = 17
    src[1] = 42
    var dstSlice: Slice<uint32> = dst[:]
    var srcSlice: Slice<uint32> = src[:]
    var dstWindow: Slice<uint32> = dstSlice[1:3]
    var copied: Slice<uint32> = dstWindow.copyFrom(srcSlice)
    print(int(dst[0]))
    print(int(dst[1]))
    print(int(dst[2]))
    print(int(dst[3]))
    print(len(copied))
    var overlap = Array<uint16>(4, 0)
    overlap[0] = 1
    overlap[1] = 2
    overlap[2] = 3
    overlap[3] = 4
    var overlapSlice: Slice<uint16> = overlap[:]
    var overlapDst: Slice<uint16> = overlapSlice[1:4]
    var overlapSrc: Slice<uint16> = overlapSlice[0:3]
    overlapDst.copyFrom(overlapSrc)
    print(int(overlap[0]))
    print(int(overlap[1]))
    print(int(overlap[2]))
    print(int(overlap[3]))
    try {
        var tooSmall: Slice<uint32> = dstSlice[0:1]
        tooSmall.copyFrom(srcSlice)
        print(999)
    } catch panic (p) {
        print(p.message)
        print(p.code)
    }
}

main()

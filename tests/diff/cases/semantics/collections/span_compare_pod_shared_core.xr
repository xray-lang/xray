// anchor: span-compare-pod-shared-core
fn main() {
    var left = Array<u32>(2, 0)
    left[0] = 1
    left[1] = 2
    var same = Array<u32>(2, 0)
    same[0] = 1
    same[1] = 2
    var higher = Array<u32>(2, 0)
    higher[0] = 1
    higher[1] = 3
    var prefix = Array<u32>(1, 0)
    prefix[0] = 1
    var leftSlice: Slice<u32> = left[:]
    var sameSlice: Slice<u32> = same[:]
    var higherSlice: Slice<u32> = higher[:]
    var prefixSlice: Slice<u32> = prefix[:]
    print(leftSlice.compare(sameSlice))
    print(leftSlice.compare(higherSlice))
    print(higherSlice.compare(leftSlice))
    print(leftSlice.compare(prefixSlice))
    print(prefixSlice.compare(leftSlice))
    var windowed = Array<u16>(4, 0)
    windowed[0] = 9
    windowed[1] = 7
    windowed[2] = 8
    windowed[3] = 6
    var winSlice: Slice<u16> = windowed[:]
    var first: Slice<u16> = winSlice[1:3]
    var second: Slice<u16> = winSlice[2:4]
    print(first.compare(second))
}

main()

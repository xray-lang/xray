// anchor: bytes-int-arg-shared-core
var dyn: Json = {
    "badOffset": true,
    "badDst": false,
    "badSrc": "src",
    "badCount": null
}

var bytes = Array<byte>(8, 1)
var src = Array<byte>(8, 2)

fn show_load_u32() {
    var loadBytes = Array<byte>(8, 1)
    var view: Slice<byte> = loadBytes[:]
    try {
        var value: uint32 = view.load<uint32>(dyn.badOffset, Endian.LE)
        print(int(value))
    } catch panic (p) {
        print(p.message)
        print(p.code)
    }
}

fn show_load_u64() {
    var loadBytes = Array<byte>(8, 1)
    var view: Slice<byte> = loadBytes[:]
    try {
        var value: uint64 = view.load<uint64>(dyn.badOffset, Endian.LE)
        print(int(value))
    } catch panic (p) {
        print(p.message)
        print(p.code)
    }
}

fn show_append_from() {
    try {
        bytes.appendFrom(src[dyn.badOffset:2])
        print(bytes[0])
    } catch panic (p) {
        print(p.message)
        print(p.code)
    }
}

fn show_repeat_distance() {
    try {
        bytes.repeatFrom(dyn.badDst, 1)
        print(bytes[0])
    } catch panic (p) {
        print(p.message)
        print(p.code)
    }
}

fn show_repeat_from() {
    try {
        bytes.repeatFrom(1, dyn.badCount)
        print(bytes[0])
    } catch panic (p) {
        print(p.message)
        print(p.code)
    }
}

show_load_u32()
show_load_u64()
show_append_from()
show_repeat_distance()
show_repeat_from()

// anchor: bytes-int-arg-shared-core
var dyn: JSON.Object = JSON.parseObject(
    "{\"badOffset\":true,\"badDst\":false,\"badSrc\":\"src\",\"badCount\":null}"
)

var bytes = Array<u8>(8, 1)
var src = Array<u8>(8, 2)

fn show_load_u32() {
    var loadBytes = Array<u8>(8, 1)
    var view: Slice<u8> = loadBytes[:]
    try {
        var value: u32 = view.load<u32>(JSON.require<i64>(dyn, ["badOffset"]), Endian.LE)
        print((value as i64))
    } catch panic (p) {
        print(p.message)
        print(p.code)
    }
}

fn show_load_u64() {
    var loadBytes = Array<u8>(8, 1)
    var view: Slice<u8> = loadBytes[:]
    try {
        var value: u64 = view.load<u64>(JSON.require<i64>(dyn, ["badOffset"]), Endian.LE)
        print((value as i64))
    } catch panic (p) {
        print(p.message)
        print(p.code)
    }
}

fn show_append_from() {
    try {
        bytes.appendFrom(src[JSON.require<i64>(dyn, ["badOffset"]):2])
        print(bytes[0])
    } catch panic (p) {
        print(p.message)
        print(p.code)
    }
}

fn show_repeat_distance() {
    try {
        bytes.repeatFrom(JSON.require<i64>(dyn, ["badDst"]), 1)
        print(bytes[0])
    } catch panic (p) {
        print(p.message)
        print(p.code)
    }
}

fn show_repeat_from() {
    try {
        bytes.repeatFrom(1, JSON.require<i64>(dyn, ["badCount"]))
        print(bytes[0])
    } catch panic (p) {
        print(p.message)
        print(p.code)
    }
}

show_load_u32()
show_load_u64()
show_append_from()
show_repeat_distance()
show_repeat_from()

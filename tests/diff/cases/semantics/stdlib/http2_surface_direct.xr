// diff-backends: vm,aot
import http2

// Native HTTP/2 availability is backend-specific; this differential case
// freezes the public return type, not the host capability probe's value.
var available: bool = http2.supported()
print(typeName(available))

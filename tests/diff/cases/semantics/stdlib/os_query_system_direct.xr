import os
import path

print(len(os.platform) > 0)
print(len(os.arch) > 0)
print(len(os.sep) > 0)
print(len(os.eol) >= 1)
print(os.getpid() > 0)
print(os.uid() >= 0)
print(os.gid() >= 0)
print(os.cpuCount() >= 1)
print(os.ppid() > 0)
print(os.kill(os.getpid(), 0))
print(os.totalMemory() > 0)
print(os.freeMemory() > 0)
print(os.uptime() >= 0.0)
var load = os.loadavg()
print(len(load) == 3)
print(load[0] >= 0.0)
print(load[1] >= 0.0)
print(load[2] >= 0.0)
print(os.clock() >= 0.0)
print(os.getenv("XRAY_TEST_NONEXISTENT_VAR_12345") == null)
print(os.setenv("XRAY_AOT_OS_QUERY_TMP", "hello-aot"))
print(os.getenv("XRAY_AOT_OS_QUERY_TMP") == "hello-aot")
print(os.unsetenv("XRAY_AOT_OS_QUERY_TMP"))
print(os.getenv("XRAY_AOT_OS_QUERY_TMP") == null)
print(os.setenv("XRAY_AOT_OS_QUERY_ENV", "map-aot"))
var env = os.environ()
print(env["XRAY_AOT_OS_QUERY_ENV"] == "map-aot")
print(os.unsetenv("XRAY_AOT_OS_QUERY_ENV"))
print(os.setenv("XRAY_AOT_OS_QUERY_EMPTY", ""))
var envEmpty = os.environ()
print(envEmpty["XRAY_AOT_OS_QUERY_EMPTY"] == "")
print(os.unsetenv("XRAY_AOT_OS_QUERY_EMPTY"))
var cwd = os.getcwd()
print(len(cwd) > 0)
print(os.chdir(path.from(cwd)))
print(os.getcwd() == cwd)
print(len(os.hostname()) > 0)
print(len(os.tmpdir()) > 0)
var oldTmpdir = os.getenv("TMPDIR")
var oldTmp = os.getenv("TMP")
var oldTemp = os.getenv("TEMP")
os.setenv("TMPDIR", "")
os.setenv("TMP", "/tmp/xray_diff_tmpdir_fallback")
os.setenv("TEMP", "/tmp/xray_diff_tmpdir_wrong")
print(os.tmpdir() == "/tmp/xray_diff_tmpdir_fallback")
if (oldTmpdir != null) {
    os.setenv("TMPDIR", oldTmpdir ?? "")
} else {
    os.unsetenv("TMPDIR")
}
if (oldTmp != null) {
    os.setenv("TMP", oldTmp ?? "")
} else {
    os.unsetenv("TMP")
}
if (oldTemp != null) {
    os.setenv("TEMP", oldTemp ?? "")
} else {
    os.unsetenv("TEMP")
}
print(os.username() != null)
print(os.homedir() != null)
var oldHome = os.getenv("HOME")
os.setenv("HOME", "")
var fallbackHome = os.homedir()
print(fallbackHome != null)
print(len((fallbackHome ?? "")) > 0)
if (oldHome != null) {
    os.setenv("HOME", oldHome ?? "")
} else {
    os.unsetenv("HOME")
}

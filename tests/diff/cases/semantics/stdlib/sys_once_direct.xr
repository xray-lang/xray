// sys.OsOnce calls the first closure exactly once.

import sys

let counter = 0
let once = sys.OsOnce()

once.call(() -> { counter = counter + 1 })
once.call(() -> { counter = counter + 10 })
once.call(() -> { counter = counter + 100 })
print(counter)

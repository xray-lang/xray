import {
    ClusterConfig,
    ClusterInfo,
    ClusterNodeInfo,
    ClusterTlsOptions
} from cluster

fn accepts(
    config: ClusterConfig?,
    info: ClusterInfo?,
    node: ClusterNodeInfo?,
    tlsOptions: ClusterTlsOptions?
) -> bool {
    return true
}

print(accepts(null, null, null, null))

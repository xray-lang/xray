import parallel

const parallel_vm_batch_counter: Atomic<i64> = Atomic<i64>(0)

parallel.forEach(0..1000, (i) -> {
    parallel_vm_batch_counter.add(1, Ordering.Relaxed)
}, parallel.Options(4))

print(parallel_vm_batch_counter.load(Ordering.Relaxed))

class Box {
    value: i64

    constructor(value: i64) {
        this.value = value
    }
}

fn buildBox() -> Box {
    Coro.yield()
    return Box(41)
}

var task = go buildBox()
var box = await task
print(box.value + 1)

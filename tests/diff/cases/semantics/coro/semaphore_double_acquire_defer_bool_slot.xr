import { Semaphore } from sync

fn holdBoth(a: Semaphore, b: Semaphore) -> int {
    a.acquire()
    b.acquire()
    defer { b.release() }
    return 7
}

const a = Semaphore(1)
const b = Semaphore(1)
print(holdBoth(a, b))
print(a.release())

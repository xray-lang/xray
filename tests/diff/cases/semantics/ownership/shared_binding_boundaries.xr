// anchor: ownership-shared-binding-boundaries
shared const base: Array<int> = [1, 2]
shared const alias = base
print(alias.length)

shared let owner: Array<int> = [3, 4]
shared const frozen = move owner
print(frozen[1])

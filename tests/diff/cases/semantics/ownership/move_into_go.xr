// 116 boundary: local `var` ownership transferred into a goroutine via `move`.
// Deterministic via await; output must match across VM/JIT/AOT.
fn consume(data: Array<int>) -> int {
    return data.length
}

var job: Array<int> = [1, 2, 3]
var t = go consume(move job)
print(await t)

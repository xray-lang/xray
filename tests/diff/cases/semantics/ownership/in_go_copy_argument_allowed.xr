fn launch(data: in Array<int>) {
    let task = go fn(owned: Array<int>) -> int {
        owned.push(5)
        return owned.length
    }(copy(data))
    print(await task)
    print(data.length)
}

let values: Array<int> = [1, 2, 3, 4]
launch(values)

fn launch(data: Array<int>) {
    var originalLength = len(data)
    var task = go fn(buffer: Array<int>) -> int {
        buffer.push(5)
        return len(buffer)
    }(copy(data))
    print(await task)
    print(originalLength)
}

var values: Array<int> = [1, 2, 3, 4]
launch(values)

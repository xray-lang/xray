fn launch(data: Array<i64>) {
    var originalLength = len(data)
    var task = go fn(buffer: move Array<i64>) -> i64 {
        buffer.push(5)
        return len(buffer)
    }(copy(data))
    print(await task)
    print(originalLength)
}

var values: Array<i64> = [1, 2, 3, 4]
launch(values)

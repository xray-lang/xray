class Counter {
    value: int

    constructor() {
        this.value = 0
    }
}

fn stashLength(data: in Array<int>, counter: ref Counter) {
    counter.value = data.length
}

var values: Array<int> = [1, 2, 3, 4]
var counter = Counter()
stashLength(values, counter)
print(counter.value)

class Counter {
    value: i64

    constructor() {
        this.value = 0
    }
}

fn stashLength(data: Array<i64>, counter: ref Counter) {
    counter.value = len(data)
}

var values: Array<i64> = [1, 2, 3, 4]
var counter = Counter()
stashLength(values, ref counter)
print(counter.value)

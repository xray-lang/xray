class Counter {
    value: int

    constructor() {
        this.value = 0
    }
}

fn stashLength(data: Array<int>, counter: ref Counter) {
    counter.value = len(data)
}

var values: Array<int> = [1, 2, 3, 4]
var counter = Counter()
stashLength(values, ref counter)
print(counter.value)

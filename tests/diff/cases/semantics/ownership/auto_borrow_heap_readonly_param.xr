fn lenOf(a: Array<int>) -> int {
    return len(a)
}

fn first(m: Map<string, int>, k: string) -> int {
    return m[k]
}

var xs: Array<int> = [1, 2, 3]
print(lenOf(xs))
xs.push(4)
print(lenOf(xs))

var m: Map<string, int> = #{}
m.set("x", 7)
print(first(m, "x"))

// Generic value struct literals must lower to XI_AGG_NEW after monomorphization.
// The VM and AOT both rely on the concrete struct layout, not a constructor fallback.
struct Box<T> {
    value: T
}

var p = Box<int>{value: 41}
p.value = p.value + 1
print(p.value)

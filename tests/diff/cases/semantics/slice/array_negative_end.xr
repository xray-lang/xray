// anchor: slice-array-negative-end
let arr = [1, 2, 3, 4]

let endDrop: Span<int> = arr[:-1]
print(endDrop)
print(copy(arr[:-1]))
let hugeDrop: Span<int> = arr[:-999]
print(hugeDrop)
let reversed: Span<int> = arr[3:1]
print(reversed)

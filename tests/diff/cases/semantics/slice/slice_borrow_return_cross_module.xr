import { importedTail } from "./_borrow_return_lib"

fn run() {
    var owner: Array<byte> = [5, 6, 7, 8]
    var source: Slice<byte> = owner[:]
    var result = importedTail(source)
    print(len(result))
    print(result[0])
}

run()

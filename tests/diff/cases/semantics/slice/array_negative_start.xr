// anchor: slice-array-negative-start
let arr = [1, 2, 3, 4]

let tail: Span<int> = arr[-2:]
print(tail)
print(copy(arr[-2:]))
let hugeTail: Span<int> = arr[-999:]
print(hugeTail)
let pastEnd: Span<int> = arr[999:]
print(pastEnd)

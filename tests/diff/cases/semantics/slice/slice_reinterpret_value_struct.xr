struct Pair {
    first: u32
    second: u32
}

fn main() {
    var storage: Array<u8> = [1, 0, 0, 0, 2, 0, 0, 0]
    var bytes: Slice<u8> = storage[:]
    var pairs: Slice<Pair> = bytes.reinterpret<Pair>()
    print(len(pairs))
    print(pairs[0].first)
    print(pairs[0].second)
}

main()

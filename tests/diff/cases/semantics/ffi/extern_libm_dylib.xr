// FFI: manifest-declared libm symbols must resolve consistently in VM and AOT.
extern "C" {
    fn cos(x: f64) -> f64
    fn pow(base: f64, exp: f64) -> f64
}

print(unsafe { cos(0.0) })
print(unsafe { pow(3.0, 2.0) })

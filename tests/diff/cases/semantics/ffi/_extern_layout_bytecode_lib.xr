// Imported extern layout used by the task-208 bytecode CLI roundtrip gate.
extern "C" {
    export packed struct BytecodeHeader {
        tag: uint8
        length: uintsize
        delta: intsize
    }
}

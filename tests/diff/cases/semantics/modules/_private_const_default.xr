const DEFAULT_LIMIT: i64 = 192

export fn defaultLimit(limit: i64 = DEFAULT_LIMIT) -> i64 {
    return limit
}

export class PrivateConstDefault {
    static scale(value: i64, factor: i64 = DEFAULT_LIMIT / 64) -> i64 {
        return value * factor
    }
}

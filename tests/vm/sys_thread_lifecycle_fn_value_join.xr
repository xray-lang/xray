import sys

fn run() {
    const waitThread = fn(t: Thread<i64>) -> i64 {
        var v = t.join()
        print("fn-value joined " + string(v))
        return v
    }

    var t = sys.Thread.spawn(fn() -> i64 {
        return 42
    })
    print(waitThread(t))
}

run()

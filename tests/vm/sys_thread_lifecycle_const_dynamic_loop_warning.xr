import sys

fn run(flag: bool) {
    const maybe = flag
    var t = sys.Thread.spawn(fn() -> i64 {
        return 42
    })
    while (maybe) {
        print(t.join())
        break
    }
    print("const-dynamic-loop")
}

run(false)

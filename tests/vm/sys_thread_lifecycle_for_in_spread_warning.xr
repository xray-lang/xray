import sys

fn run() {
    var empty: Array<i64> = []
    var t = sys.Thread.spawn(fn() -> i64 {
        return 42
    })
    for (i in [...empty]) {
        print(t.join())
    }
    print("spread-empty")
}

run()

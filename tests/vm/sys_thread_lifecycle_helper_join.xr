import sys

fn waitThread(t: Thread<i64>) -> i64 {
    var v = t.join()
    print("helper joined " + string(v))
    return v
}

fn run() {
    var t = sys.Thread.spawn(fn() -> i64 {
        return 42
    })
    print(waitThread(t))
}

run()

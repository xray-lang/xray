import sys

fn makeThread() -> Thread<i64> {
    return sys.Thread.spawn(fn() -> i64 {
        return 42
    })
}

fn wrapThread() -> Thread<i64> {
    return makeThread()
}

var t = wrapThread()
print(t.join())

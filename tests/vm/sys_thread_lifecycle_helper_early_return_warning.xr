import sys

fn maybeJoin(t: Thread<i64>, skip: bool) {
    if (skip) {
        return
    }
    print(t.join())
}

fn run() {
    var t = sys.Thread.spawn(fn() -> i64 {
        return 42
    })
    maybeJoin(t, true)
    print("helper-skip")
}

run()

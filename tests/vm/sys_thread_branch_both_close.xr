import sys

fn closeOnBothBranches(flag: bool) {
    var t = sys.Thread.spawn(fn() -> i64 {
        return 42
    })
    if (flag) {
        print(t.join())
    } else {
        t.detach()
    }
}

closeOnBothBranches(true)

import sys

fn loopContinueJoin(flag: bool) {
    var t = sys.Thread.spawn(fn() -> i64 {
        return 42
    })
    while (true) {
        if (flag) {
            continue
        }
        print(t.join())
        break
    }
}

loopContinueJoin(false)

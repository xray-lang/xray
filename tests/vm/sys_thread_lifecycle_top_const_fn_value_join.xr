import sys

const waitThread = fn(t: Thread<int>) -> int {
    var v = t.join()
    print("top-fn-value joined " + string(v))
    return v
}

fn run() {
    var t = sys.Thread.spawn(fn() -> int {
        return 42
    })
    print(waitThread(t))
}

run()

import sys

fn closeOnlyInOneTernaryBranch(flag: bool) {
    var t = sys.Thread.spawn(fn() -> i64 {
        return 42
    })
    print(flag ? t.join() : 0)
    print("after-ternary")
}

closeOnlyInOneTernaryBranch(false)

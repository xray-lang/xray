import sys

fn wrapThread(t: Thread<int>) -> Thread<int> {
    return passThread(t)
}

fn passThread(t: Thread<int>) -> Thread<int> {
    return t
}

fn run() {
    var t = sys.Thread.spawn(fn() -> int {
        return 42
    })
    print(wrapThread(t).join())
}

run()

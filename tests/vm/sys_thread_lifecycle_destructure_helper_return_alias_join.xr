import sys

fn passThread(t: Thread<int>) -> Thread<int> {
    return t
}

fn run() {
    var t = sys.Thread.spawn(fn() -> int {
        return 42
    })
    var (joined,) = (passThread(t),)
    print(joined.join())
}

run()

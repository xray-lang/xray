import sys

fn closeInMatch(tag: i64) {
    var t = sys.Thread.spawn(fn() -> i64 {
        return 42
    })
    match (tag) {
        0 -> {
            t.detach()
        }
        _ -> {
            print(t.join())
        }
    }
}

closeInMatch(1)

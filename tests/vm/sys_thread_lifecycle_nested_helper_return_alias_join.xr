import sys

fn passThread(t: move Thread<int>) -> Thread<int> {
    return t
}

fn run() {
    var t = sys.Thread.spawn(fn() -> int {
        return 42
    })
    var inner = passThread(move t)
    print(passThread(move inner).join())
}

run()

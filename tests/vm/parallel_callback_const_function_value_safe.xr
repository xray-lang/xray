import parallel

const total = Atomic<i64>(0)

parallel.forEach(0..4, (i) -> {
    const addOne = fn(x: i64) -> i64 {
        return x + 1
    }
    total.add(addOne(i), Ordering.Relaxed)
}, parallel.Options(2))

print(total.load(Ordering.Relaxed))

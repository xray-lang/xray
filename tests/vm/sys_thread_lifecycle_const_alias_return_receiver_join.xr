import sys

fn passThread(t: move Thread<i64>) -> Thread<i64> {
    return t
}

fn run() {
    const aliasThread = passThread
    var t = sys.Thread.spawn(fn() -> i64 {
        return 42
    })
    print(aliasThread(move t).join())
}

run()

import sys

fn passThread(t: Thread<int>) -> Thread<int> {
    return t
}

fn run() {
    const aliasThread = passThread
    var t = sys.Thread.spawn(fn() -> int {
        return 42
    })
    print(aliasThread(t).join())
}

run()

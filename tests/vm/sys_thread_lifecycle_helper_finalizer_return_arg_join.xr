import sys

fn passThread(t: Thread<int>) -> Thread<int> {
    return t
}

fn joinThread(t: Thread<int>) -> int {
    return t.join()
}

fn run() {
    var t = sys.Thread.spawn(fn() -> int {
        return 42
    })
    print(joinThread(passThread(t)))
}

run()

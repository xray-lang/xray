import sys
import { Pipe } from sys

fn passPipe(p: Pipe) -> Pipe {
    return p
}

fn run() {
    const aliasPipe = passPipe
    var opened = sys.Pipe.open()
    if (opened == null) {
        print(false)
        return
    }
    print(aliasPipe(opened!).close())
}

run()

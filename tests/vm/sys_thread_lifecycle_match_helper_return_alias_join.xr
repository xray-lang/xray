import sys

fn passThread(t: move Thread<i64>) -> Thread<i64> {
    return t
}

fn run(tag: i64) {
    var t = sys.Thread.spawn(fn() -> i64 {
        return 42
    })
    var observed = tag
    var joined = passThread(move t)
    print(joined.join())
}

run(1)

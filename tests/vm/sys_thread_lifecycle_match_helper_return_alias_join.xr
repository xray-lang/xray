import sys

fn passThread(t: move Thread<int>) -> Thread<int> {
    return t
}

fn run(tag: int) {
    var t = sys.Thread.spawn(fn() -> int {
        return 42
    })
    var observed = tag
    var joined = passThread(move t)
    print(joined.join())
}

run(1)

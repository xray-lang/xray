import sys

fn passThread(t: Thread<int>) -> Thread<int> {
    return t
}

fn run(tag: int) {
    var t = sys.Thread.spawn(fn() -> int {
        return 42
    })
    var joined = match (tag) {
        0 -> passThread(t),
        _ -> passThread(t)
    }
    print(joined.join())
}

run(1)

import sys

fn closeBranchAlias(flag: bool) {
    var t = sys.Thread.spawn(fn() -> i64 {
        return 42
    })
    if (flag) {
        var u = t
        print(u.join())
    } else {
        var v = t
        v.detach()
    }
}

closeBranchAlias(true)

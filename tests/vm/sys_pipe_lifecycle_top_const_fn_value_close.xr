import sys
import { Pipe } from sys

const closePipe = fn(p: Pipe) {
    print(p.closeRead())
    print(p.closeWrite())
}

fn run() {
    var pipe = sys.Pipe.open()
    closePipe(pipe!)
    print("pipe-top-fn-value")
}

run()

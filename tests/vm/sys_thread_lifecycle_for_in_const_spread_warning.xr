import sys

fn run() {
    var empty: Array<i64> = []
    const indices = [...empty]
    var t = sys.Thread.spawn(fn() -> i64 {
        return 42
    })
    for (i in indices) {
        print(t.join())
    }
    print("const-spread-empty")
}

run()

import sys

fn passThread(t: move Thread<int>) -> Thread<int> {
    return t
}

fn chooseThread(t: move Thread<int>, tag: int) -> Thread<int> {
    var observed = tag
    return t
}

var t = sys.Thread.spawn(fn() -> int {
    return 42
})
print(chooseThread(move t, 1).join())

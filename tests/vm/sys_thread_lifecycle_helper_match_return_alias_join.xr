import sys

fn passThread(t: move Thread<i64>) -> Thread<i64> {
    return t
}

fn chooseThread(t: move Thread<i64>, tag: i64) -> Thread<i64> {
    var observed = tag
    return t
}

var t = sys.Thread.spawn(fn() -> i64 {
    return 42
})
print(chooseThread(move t, 1).join())

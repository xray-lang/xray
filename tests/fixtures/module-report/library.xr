var count: i64 = 40
var label: string = "ready"
print("library", count, label)

export fn next(tag: string) -> string {
    count = count + 1
    label = tag
    return "${label}:${count}"
}

export fn total() -> i64 { return count }

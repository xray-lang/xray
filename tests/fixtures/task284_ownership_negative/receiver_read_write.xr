class Counter {
    value: i64

    constructor(value: i64) {
        this.value = value
    }

    bump() {
        this.value = this.value + 1
    }
}

fn main() {
    var counter = Counter(1)
    counter.bump()
}

main()

// Function attributes / decorators
fn test_addition() {
    assertEqual(1 + 1, 2)
}

fn test_subtraction() {
    assertEqual(5 - 3, 2)
}

// Multiple test functions
fn test_array_operations() {
    let arr = [1, 2, 3]
    assertEqual(len(arr), 3)
    arr.push(4)
    assertEqual(len(arr), 4)
}

fn test_map_operations() {
    let m = { "a" => 1 }
    assertEqual(m.get("a"), 1)
    m.set("b", 2)
    assertEqual(m.size, 2)
}

fn test_string_methods() {
    let s = "hello"
    assertEqual(s.toUpperCase(), "HELLO")
    assertEqual(len(s), 5)
}

// Regular function without attribute
fn helper() {
    return 42
}

fn test_using_helper() {
    assertEqual(helper(), 42)
}
